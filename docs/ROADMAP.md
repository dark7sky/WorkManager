# WorkManager Roadmap

Last updated: 2026-07-21 (21:50 KST)

- [x] Work log invoiced marking (2026-07-21): the printable invoice PDF pulled every `billable` work log for the selected period, but nothing recorded that a log had already been invoiced, so regenerating an invoice for an overlapping range (or re-running it later) silently re-billed the same hours. Added a nullable `invoiced_at` column on `work_logs` (`backend/app/db.py`, `WorkLogPayload`/`CONFIG` in `backend/app/main.py`), a "청구 완료 표시" bulk button on the Performance screen that PATCHes every currently-billable log's `invoiced_at` to now (`frontend/src/screens/Performance.jsx`), and `billableWorkLogs` (`frontend/src/invoiceReport.js`) now excludes already-invoiced rows so the next invoice only picks up new hours. Regression tests: `test_work_log_invoiced_at_is_persisted_and_clearable` in `backend/tests/test_api.py`, `frontend/src/invoiceReport.test.js`.
- [x] Calendar feed/export reminder alarms (2026-07-21): the per-item reminder lead time (`reminder_minutes_before`) drives in-app browser notifications, but the webcal subscription feed and the standalone .ics export buttons emitted plain VEVENTs with no VALARM, so items imported into Google/Apple/Outlook Calendar carried no native reminder even when the user had explicitly set one. `build_calendar_feed` (`backend/app/main.py`) and `eventsToIcs`/`tasksToIcs`/`todosToIcs` (`frontend/src/ics.js`) now emit a `BEGIN:VALARM ... TRIGGER:-PT{n}M ... END:VALARM` block for timed items that have a reminder lead set. Tests: `backend/tests/test_api.py::test_calendar_feed_includes_valarm_for_items_with_reminder_lead`, `frontend/src/ics.test.js`.
- [x] Invoice business registration number (2026-07-21): the printable invoice PDF showed a "청구 대상" (bill-to) client name line but no business registration number, so invoices meant for formal/tax documentation still needed the number hand-written after printing. Added a `billing_biz_reg_number` per-user setting (Settings screen, alongside the existing billing client name), returned from `/api/settings/workflow` and `/api/achievements`, and rendered as a "사업자등록번호: ..." line under the client name on the invoice (`backend/app/main.py`, `frontend/src/screens/Settings.jsx`, `frontend/src/invoiceReport.js`, `frontend/src/screens/Performance.jsx`, tests in `backend/tests/test_api.py` and `frontend/src/invoiceReport.test.js`).
- [x] Calendar subscription feed includes todos (2026-07-21): the webcal subscription feed (`GET /api/calendar-feed/{token}.ics`) rendered events and task due dates as VEVENTs but never included todos, so a subscribed external calendar app silently missed all "할 일" deadlines even though todos already export/import ICS individually elsewhere. `build_calendar_feed` (`backend/app/main.py`) now also emits a VEVENT per todo with a `todo_date` (all-day or timed based on `todo_time`, memo as DESCRIPTION), matching the existing task/event loop pattern. Test extended: `test_calendar_feed_rotate_serves_ics_and_old_token_is_invalidated` in `backend/tests/test_api.py`.
- [x] Export selected tasks only (2026-07-21): the Tasks screen's PDF/CSV/Excel/ICS export buttons always exported the entire filtered list even when the user had multi-selected specific rows via the bulk-action checkboxes, so there was no way to export just a handful of picked items. Added `selectExportRows` (`frontend/src/taskFilters.js`) so each export/print helper uses the current checkbox selection when non-empty and falls back to the filtered list otherwise (`frontend/src/screens/Tasks.jsx`), with tooltips indicating selection-scoped export. Tests in `frontend/src/taskFilters.test.js`.
- [x] CSV import duplicate detection for events/todos/work logs (2026-07-21): tasks' CSV import already skipped rows that duplicated an existing task or another row in the same import batch (`dedupeImportedTasks`), but Calendar's event CSV import and Today's todo/work-log CSV imports had no equivalent check, so re-importing a previously exported CSV (or the same file twice) silently created full duplicates. Added `dedupeImportedEvents`/`dedupeImportedTodos`/`dedupeImportedLogs` (`frontend/src/csv.js`, generalized via a shared `dedupeImportedItems` helper) matching on title+date (or content+date for logs) and wired them into `importCsv` (`frontend/src/screens/Calendar.jsx`) and `importTodosCsv`/`importLogsCsv` (`frontend/src/screens/Today.jsx`), each now reporting a "중복 건너뜀" count alongside parse errors. Tests in `frontend/src/csv.test.js`.
- [x] Bulk promote-to-task for calendar events (2026-07-21): todos and work logs both had a bulk "promote selected to task" action, but the Calendar screen only offered a single-item promote button, so converting several events into tasks meant repeating the action one at a time. Added an "업무로 전환" button to the events bulk-action bar (`frontend/src/screens/Calendar.jsx`) wired to a new `onBulkPromote` handler in `frontend/src/App.jsx` that reuses the existing `buildTaskFromEventPayload` conversion, matching the pattern already used by `onBulkPromoteTodo`/`onBulkPromoteLog`.
- [x] Global cross-entity search (2026-07-21): every screen had its own local search box scoped to that screen's entity only, so finding a task/event/todo/log from a different screen meant navigating there first and guessing which list it lived in. Added a global search modal (`frontend/src/components/GlobalSearch.jsx`, pure matcher `frontend/src/globalSearch.js`) reachable via a sidebar button or `Ctrl/⌘+/` (`frontend/src/App.jsx`, `frontend/src/components/AppShell.jsx`, `frontend/src/components/KeyboardShortcuts.jsx`) that searches titles/content across tasks, events, todos, and work logs at once and jumps to/opens the selected result. Test: `frontend/src/globalSearch.test.js`.
- [x] Copyable internal task deep-link (2026-07-21): sharing a specific task with a teammate meant telling them the title to search for — there was no quick way to send a link that opens that exact task. Added a "링크" button on each task row that copies an internal `?page=tasks&taskId=<id>` URL (`frontend/src/screens/Tasks.jsx` `copyTaskLink`, `frontend/src/taskFilters.js` `taskDeepLink`), and the app now consumes that param on load to auto-open the task's edit modal once the signed-in user's tasks are fetched (`frontend/src/App.jsx`). This is distinct from the existing public unauthenticated share-link feature (`/api/tasks/{id}/share`). Test: `frontend/src/taskFilters.test.js::taskDeepLink builds an internal task URL with the taskId query param`.
- [x] AI-applied changes recorded with a reason in the audit log (2026-07-21): applying an AI suggestion (QuickCapture, AI 어시스턴트, Performance 추천) via `/api/ai/apply` produced the exact same audit log entry as a manual edit — just a "변경 필드" field list — so there was no way to tell an AI-driven change from a manual one, or see why the AI made it. `/api/ai/apply` now tags every create/update it performs with `source: "ai"` plus, when the suggestion carried one, its `reason` text, threaded through `create_item`/`update_item` into `audit()` metadata (`backend/app/main.py`); the frontend now forwards `item.reason` when applying a suggestion (`frontend/src/App.jsx`, `frontend/src/components/QuickCapture.jsx`, `frontend/src/screens/Performance.jsx`); the Audit Log screen renders an "AI 자동 수정: <reason>" line for these entries (`frontend/src/screens/AuditLog.jsx` `metadataText`). Test: `backend/tests/test_api.py::test_ai_apply_records_audit_log_with_ai_reason`.
- [x] Audit Log Excel export (2026-07-21): Tasks, Calendar, and Today screens already had a one-click Excel (.xls) export button alongside CSV, but the Audit Log screen only offered CSV and a printable PDF report — compliance/finance users handing an audit trail to auditors had to convert the CSV manually. Added `auditLogsToExcelXml`/`auditLogExcelFilename` (`frontend/src/xlsx.js`, reusing the existing `auditHeaders`/`auditRows` from `frontend/src/csv.js`) and an "Excel 내보내기" button in the Audit Log toolbar (`frontend/src/screens/AuditLog.jsx`), closing the last export-format gap across list screens. Tests in `frontend/src/xlsx.test.js`.
- [x] Bulk "업무로 전환" for work logs (2026-07-21): the Today screen's todo bulk-action bar already let users select several todos and convert them all to tasks in one click (`onBulkPromoteTodo`), and every work log row already had a single-item "업무로 전환" button, but the work-log bulk-action bar never got the matching bulk button — promoting several log entries into tasks required clicking each row individually. Added `onBulkPromoteLog` (`frontend/src/App.jsx`, reusing `buildTaskFromLogPayload`) and a matching "업무로 전환" button in the work-log selection toolbar (`frontend/src/screens/Today.jsx`), closing the last bulk-action asymmetry across the four entity types.
- [x] Per-item reminder lead time (2026-07-21): due/start-time device notifications for tasks, events, and todos only ever honored one global "N분 전" lead time per entity, configured in Settings — there was no way to ask for an earlier or later heads-up on a specific item (e.g. a client call you want 30 minutes' notice for vs. a routine todo where 5 minutes is enough). Added an optional `reminder_minutes_before` column on `tasks`/`events`/`todos` (`backend/app/db.py`, validated `ge=0, le=1440` on each Pydantic payload in `backend/app/main.py`), an "알림 시점(분 전)" input on the task form, event form, and todo quick-add/inline-edit (`frontend/src/components/TaskForm.jsx`, `frontend/src/screens/Calendar.jsx`, `frontend/src/screens/Today.jsx`), and `tasksDueForAlert`/`eventsDueForAlert`/`todosDueForAlert` now use the item's own value when set, falling back to the existing global default otherwise (`frontend/src/taskAlerts.js`, `frontend/src/eventAlerts.js`, `frontend/src/todoAlerts.js`), tests in each `*Alerts.test.js` and `backend/tests/test_api.py::test_reminder_minutes_before_is_persisted_and_validated_on_task_event_todo`.
- [x] Overdue task auto-escalation (2026-07-21): overdue tasks only ever showed a passive "지연" badge count with no automated follow-up — a task could sit at low/normal priority indefinitely after its due date passed. Added `escalate_overdue_tasks()` (`backend/app/main.py`), called on every `GET /api/tasks` list fetch: non-done tasks overdue by more than 24 hours and not already `priority=high` are bumped to `high` and recorded in `audit_logs` (`action=auto_escalate`), test `test_overdue_task_is_auto_escalated_to_high_priority_on_list` in `backend/tests/test_api.py`.
- [x] QuickCapture AI-fallback warning (2026-07-21): the AI 어시스턴트 screen shows a warning banner when `/api/ai/parse` falls back to local rule-based parsing, but QuickCapture (Ctrl/⌘+K), the more frequently used entry point to the same endpoint, silently dropped `item.warning` — users could unknowingly apply a possibly-wrong local-rules guess with no indication it wasn't AI-analyzed. Added the same `.ai-warning` banner to QuickCapture's preview list (`frontend/src/components/QuickCapture.jsx`).
- [x] Gantt drag-to-reparent (2026-07-21): the Gantt hierarchy already supported reparenting via the edit form's 상위 업무 dropdown, the bulk-action-bar's "상위 업무 지정", and per-row "최상위로", but there was no direct drag-and-drop way to nest a task under another — every reparent required opening a form or multi-selecting first. Added `canReparentTask()` (cycle-safe: rejects self and descendants, `frontend/src/taskHierarchy.js`) and native HTML5 drag-and-drop on Gantt rows — dragging a task's title onto another row calls the existing `onBulkSetParent([id], targetId)` PATCH path with a dashed drop-target highlight (`frontend/src/screens/Tasks.jsx`, `frontend/src/styles.css`), tests in `frontend/src/taskHierarchy.test.js`.
- [x] Event custom fields (2026-07-21): tasks, todos, and work logs all let users attach ad-hoc label/value custom fields (e.g. 고객사, 계약 번호), but calendar events had no equivalent — a four-way parity gap. Added a `custom_fields` column on `events` (`backend/app/db.py`), the same `custom_fields` field/validator on `EventPayload` (`backend/app/main.py`), and the same add/remove custom-field editor UI plus an agenda-row count badge on the event form (`frontend/src/screens/Calendar.jsx`); regression test `test_event_custom_fields_are_persisted_and_blank_labels_dropped` in `backend/tests/test_api.py`.
- [x] Work log custom fields (2026-07-21): tasks and todos both let users attach ad-hoc label/value custom fields (e.g. 고객사, 계약 번호) from their create *and* edit forms, but work logs (오늘 한 일) had no equivalent at all — a three-way parity gap. Added a `custom_fields` column on `work_logs` (`backend/app/db.py`), the same `custom_fields` field/validator on `WorkLogPayload` (`backend/app/main.py`), and the same add/remove custom-field editor UI on both the quick-add and inline-edit work log forms (`frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`), plus carrying it through log duplication (`frontend/src/logDuplicate.js`); regression test `test_work_log_custom_fields_are_persisted_and_blank_labels_dropped` in `backend/tests/test_api.py`.
- [x] Session device label in active-sessions list (2026-07-21): the 설정 화면's 활성 세션 list only showed "현재 세션"/"다른 세션" with timestamps, giving a user no way to tell which listed session was their phone vs. a browser they'd forgotten about on a shared computer before deciding what to revoke. Added a `user_agent` column on `sessions`, captured on demo/Google login (`backend/app/main.py`), and a lightweight browser/OS parser `_describe_user_agent()` that turns it into a label like "Chrome · Windows" or "Safari · iPhone", returned as `device` from `/api/auth/sessions` (`backend/app/auth.py`, `backend/app/db.py`); the Settings session list now shows the label next to 현재/다른 세션 (`frontend/src/screens/Settings.jsx`), regression tests `test_sessions_include_parsed_device_label` and `test_session_without_user_agent_has_no_device_label` in `backend/tests/test_api.py`.
- [x] Bulk "상위 업무 지정" (set parent) for tasks (2026-07-21): the Gantt hierarchy already supported reparenting a single task via the edit form's 상위 업무 dropdown and un-nesting via the per-row "최상위로" button, but there was no way to move several selected tasks under a new parent (or back to top level) at once — the bulk-action-bar had priority/color/tag/postpone/duplicate/archive/delete but no hierarchy action. Added `taskBulkParentOptions()` (excludes every selected id and all their descendants to keep the choice cycle-safe, `frontend/src/taskHierarchy.js`), a "상위 업무 지정" select + button in the bulk-action-bar reusing the existing per-task `PATCH parent_id` validation (`frontend/src/screens/Tasks.jsx`, `frontend/src/App.jsx`), tests in `frontend/src/taskHierarchy.test.js`.
- [x] Bulk "convert to task" for todos (2026-07-21): converting a to-do into a task (`onPromoteTodo`) only ever worked one row at a time — the Today screen's todo bulk-action bar had complete/tag/postpone/priority/color/duplicate/archive/delete, but no way to promote several selected to-dos at once. Added `onBulkPromoteTodo` in `App.jsx` (creates a task via `buildTaskFromTodoPayload` and deletes the source to-do for each selected id, then refreshes) and a "업무로 전환" button in the todo bulk-action bar (`frontend/src/screens/Today.jsx`).
- [x] Task public share link (2026-07-21): there was no way to send a client/coworker a view of a single task without giving them a login — every route required an authenticated session cookie. Added a `public_token` column on `tasks` (unique, nullable), `POST`/`DELETE /api/tasks/{id}/share` to generate/revoke a token, and an unauthenticated rate-limited `GET /api/public/tasks/{token}` returning a read-only subset of fields (`backend/app/db.py`, `backend/app/main.py`). Frontend: a "공유 링크 만들기" control in the task edit form to generate/copy/revoke the link, and a new public `PublicTask.jsx` screen routed at `/public/tasks/{token}` (mirroring the existing `/changelog` public-route pattern in `App.jsx`) (`frontend/src/api.js`, `frontend/src/components/TaskForm.jsx`, `frontend/src/screens/PublicTask.jsx`, `frontend/src/App.jsx`, `frontend/src/styles.css`), regression test `test_task_share_link_exposes_read_only_view_and_can_be_revoked` in `backend/tests/test_api.py`.
- [x] Bulk archive action (2026-07-21): per-item "보관" archiving existed on every task/todo/event/work-log row, and the Archive card in Settings even had bulk *unarchive*, but none of the four bulk-action-bars (shown after multi-selecting rows) had a bulk-archive button — only bulk delete/duplicate/tag/priority/color/postpone were wired, so archiving several items at once required doing it one row at a time. Added an `onBulkArchive`/`onBulkArchiveTodo`/`onBulkArchiveLog` handler running the existing per-item archive endpoint over `Promise.all` for the selected ids, and a "보관" button in each bulk-action-bar (`frontend/src/screens/Tasks.jsx`, `frontend/src/screens/Calendar.jsx`, `frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`); no backend change needed.
- [x] Event → task promotion (2026-07-21): both todos and work logs already had an "업무로 전환" (convert to task) button, but events (일정) — the entity closest to tasks by field count (priority/checklist/estimate/color/link_url) — had no equivalent, so a user who scheduled something as an event but later needed to track it as a trackable task (progress/dependencies) had no one-click path. Added `buildTaskFromEventPayload()` mirroring `buildTaskFromTodoPayload` (`frontend/src/eventDuplicate.js`), an "업무로 전환" button in the event edit form and mobile agenda row (`frontend/src/screens/Calendar.jsx`), wired to `onPromoteEvent` in `frontend/src/App.jsx`, tests in `frontend/src/eventDuplicate.test.js`.
- [x] Performance goal overachievement display (2026-07-21): the 성과 화면 기간 목표 tracker computed progress percentages capped at 100% for both the progress bar and the displayed number, so a user who logged 150% of their goal saw "100%" with no way to tell they'd exceeded it. `goalProgress()` now returns an uncapped `*Percent` for the displayed figure alongside a separate `*BarPercent` capped at 100 for the bar width, and rows that reach/exceed their goal get a green `goal-achieved` highlight (`frontend/src/performanceReport.js`, `frontend/src/screens/Performance.jsx`, `frontend/src/styles.css`), tests in `frontend/src/performanceReport.test.js`.
- [x] Archive list pagination (2026-07-21): the four 보관함 (Archive) cards in Settings fetched their entire archived-item list in one request with no limit, unlike the 감사 로그 screen which already paginates with a "더 보기" button — an account with a large archive would load everything up front. Added `limit`/`offset` query params to `/api/{tasks,todos,events,work_logs}/archived` (`backend/app/main.py`), and updated `ArchiveSection.jsx` to fetch 50 items at a time with a "더 보기" button mirroring `AuditLog.jsx`'s pattern (`frontend/src/components/ArchiveSection.jsx`, `frontend/src/api.js`), regression test `test_archived_tasks_list_supports_limit_and_offset` in `backend/tests/test_api.py`.
- [x] Gantt subtask "최상위로" (promote to top level) button (2026-07-21): the Gantt hierarchy already let a user reassign a task's parent (including clearing it) via the full edit form's 상위 업무 dropdown, cycle-safe on both frontend (`taskParentOptions`) and backend (`validate_task_links`), but un-nesting a subtask — the single most common hierarchy edit — required opening that modal every time, unlike postpone/duplicate/archive/pin which are all one-click row actions. Added a "최상위로" button next to 보관 on subtask rows (`depth>0`) that PATCHes `parent_id: null` directly, reusing existing backend validation (`frontend/src/screens/Tasks.jsx`, `frontend/src/App.jsx`).
- [x] Work log quick-entry AI priority/estimate recommendation (2026-07-21): task/todo/event forms all gained an "AI 우선순위·예상시간 추천" button, but the Today screen's work-log (오늘 한 일) quick-entry — the last of the four entities — still only offered "AI 태그 추천". Added `recommendLogEstimate()` mirroring `recommendTodoEstimate`, calling `api.aiPreview` with the current log draft and filling the 우선순위/예상 소요 시간 fields from the response, with a matching button next to the tag-recommend controls (`frontend/src/screens/Today.jsx`).
- [x] Todo quick-entry AI priority/estimate recommendation (2026-07-21): the "AI 우선순위·예상시간 추천" button was added to the full task form but the Today screen's Todo quick-entry — which already has an "AI 태그 추천" button — had no equivalent way to get an AI estimate for priority/effort while typing a todo. Added `recommendTodoEstimate()` calling the existing `api.aiPreview` with the current draft title and filling the 우선순위/예상 소요 시간 fields from the response, with an "AI 우선순위·예상시간 추천" button next to the tag-recommend controls (`frontend/src/screens/Today.jsx`).
- [x] Task form AI priority/estimate recommendation (2026-07-21): the AI natural-language parser has long inferred `priority` and `estimated_minutes` from free text (`/api/ai/parse`, used by the quick-capture bar), but the full task create/edit form only offered "AI 태그 추천" (tag suggestions) — there was no way to get an AI estimate for priority/effort while filling in the form manually. Added an "AI 우선순위·예상시간 추천" button that calls the existing `api.aiPreview` with the current title/description and fills the 우선순위/예상 소요 시간 inputs from the response (`frontend/src/components/TaskForm.jsx`).
- [x] Audit log filter reset button (2026-07-21): the earlier filter-reset-button-parity pass covered Tasks/Calendar/Today but missed the 감사 로그 (Audit Log) screen, which had a search box and an entity-type dropdown with no unified one-click reset (only a narrow inline button that cleared just the date-range fields). Added `filtersActive`/`resetFilters` clearing query+entity+date range together, rendering the same "필터 초기화" button as the other screens (`frontend/src/screens/AuditLog.jsx`).
- [x] Filter reset button parity (2026-07-21): the Tasks screen already had a "필터 초기화" button that appears once any filter (search/status/tags/priority) is applied and clears them all in one click, but Calendar and Today's todo/work-log lists had matching search/tag/priority filter state and save/apply/delete filter-preset buttons with no equivalent one-click reset — users had to clear each filter individually. Added `eventFiltersActive`/`resetEventFilters` in `Calendar.jsx` and `todoFiltersActive`/`resetTodoFilters` + `logFiltersActive`/`resetLogFilters` in `Today.jsx`, each rendering the same "필터 초기화" button next to the existing filter-preset controls (`frontend/src/screens/Calendar.jsx`, `frontend/src/screens/Today.jsx`).
- [x] Calendar loading indicator (2026-07-21): the Tasks screen shows a skeleton and Today shows a "동기화 중…" pill while data is being fetched/refreshed, but the Calendar screen rendered its grid with no indication data was still loading, silently showing an empty-looking calendar on first load or slow networks. Threaded the existing `dataLoading` state into `Calendar` as a `loading` prop and added the same "동기화 중…" `status-pill` next to the month/week/year title (`frontend/src/screens/Calendar.jsx`, `frontend/src/App.jsx`).
- [x] Bulk-delete undo parity (2026-07-21): single-item delete on tasks/events/todos/work logs already showed an "실행 취소" (undo) toast restoring the item from trash, but all four screens' bulk-delete actions (multi-select "삭제") had no undo at all, unlike every other bulk action (complete/postpone/priority/tag/color) which supports undo — the riskier bulk delete was the one action that couldn't be reversed. Added an `undoBulkDelete` helper mirroring `undoDelete` that restores every deleted id from trash, wired into all four `onBulkDelete*` handlers (`frontend/src/App.jsx`).
- [x] Performance period-over-period comparison (2026-07-20): the 성과 screen showed a period's stats and a goal-progress bar but no way to tell if the period was better or worse than the last one without manually re-running the report with shifted dates. Added `previousPeriodRange`/`periodComparison` (`frontend/src/performanceReport.js`) — an equal-length immediately-preceding period fetched via a second `/api/achievements` call — and a ▲/▼ percent-change badge next to the 완료 업무/기록 시간/완료 할 일/일정 stat tiles (`frontend/src/screens/Performance.jsx`), tests in `frontend/src/performanceReport.test.js`.
- [x] Work log timer pause/resume (2026-07-20): the "타이머 시작/중지" work-log timer on the Today screen could only be started and stopped, so any break taken while the timer was running got counted as worked time — no way to exclude time spent away from the task. Added `pauseWorkLogTimer`/`resumeWorkLogTimer` to `workLogTimer.js` (tracks `pausedAt`/`totalPausedMs`, elapsed calculation freezes while paused and excludes prior paused spans) and a "일시정지/재개" button next to the existing start/stop controls, tests in `frontend/src/workLogTimer.test.js` (`frontend/src/screens/Today.jsx`).
- [x] Today work-log estimate overrun badge (2026-07-19): the Tasks/Gantt row already showed a "N시간 초과" badge (`taskEstimateOverrun`, `frontend/src/taskLogs.js`) when logged time exceeded a task's estimate, but the Today screen's own work-log rows carried both `estimated_minutes` and `duration_minutes` on the same record and only rendered them as two neutral badges with no over-estimate warning at all. Reused `taskEstimateOverrun` directly on each log's own fields (no cross-record aggregation needed) to render the same `task-estimate-overrun` badge, tests in `frontend/src/taskLogs.test.js` (`frontend/src/screens/Today.jsx`).
- [x] Due-alert snooze (2026-07-19): task/event/todo due-alert device notifications fired once and, if missed or dismissed, gave no way to be reminded again — a real gap for a "commercial-grade" scheduling app where missing a deadline notification is costly. Added a "10분 후 다시 알림" action button on each of the three due-alert `showNotification` calls; a new `notificationclick` handler in the service worker (`frontend/public/sw.js`) posts a message back to open app windows, which App.jsx relays into `snoozeAlert` (`frontend/src/notificationSnooze.js`) to clear the shown-flag and mark a 10-minute snooze window that the existing 60s polling loop honors via `isSnoozed`, tests in `frontend/src/notificationSnooze.test.js`.
- [x] Trash "select all" (2026-07-19): the Tasks/Gantt screen, Calendar's mobile agenda list, and the Today screen's todo/work-log lists all had a "전체 선택" checkbox to select every visible row for bulk actions, but the Trash (휴지통) screen's bulk restore/purge only supported per-row checkboxes, so recovering or purging many deleted items at once meant checking each one individually. Reused the existing `allIdsSelected`/`toggleSelectAllIds` helpers (`frontend/src/taskFilters.js`) in `frontend/src/components/TrashSection.jsx`.
- [x] Settings "모든 데이터 삭제" (wipe all data) (2026-07-18): Settings already had JSON export and merge/replace restore, but there was no direct way to wipe an account's data short of hand-crafting an empty backup file and running restore in "replace" mode — undiscoverable and not a real feature. Added a `POST /api/data/wipe` endpoint (reuses the same per-table `DELETE FROM ... WHERE user_id=?` the replace-import path already used, cascading to comments/attachments via existing FK `ON DELETE CASCADE`) gated by a `confirm: "DELETE"` body field, and a "모든 데이터 삭제" danger card in Settings requiring the user to type DELETE before enabling the button (`backend/app/main.py`, `frontend/src/screens/Settings.jsx`, `frontend/src/api.js`, test in `backend/tests/test_api.py`).
- [x] Work log CSV billable column round-trip (2026-07-18): `workLogsToCsv` exports a "청구 가능" (billable) column, but `parseWorkLogsCsv` never read it back, so re-importing an exported work-log CSV silently dropped the billable flag on every row even though every other exported column round-tripped. Added `iBillable`/`log.billable` parsing to `parseWorkLogsCsv`, mirroring how `parseEventsCsv` already restores `google_is_all_day` (`frontend/src/csv.js`, test in `frontend/src/csv.test.js`).
- [x] Work log template priority parity (2026-07-18): task/event/todo templates (`buildTaskTemplate`/`buildEventTemplate`/`buildTodoTemplate`) save and restore `priority`, but work log templates (`buildLogTemplate`) only carried content/tags/color/duration/estimate, so saving a work log with a non-default priority as a template and reapplying it silently reset the priority to "보통". Added `priority` to `buildLogTemplate`/`applyLogTemplate` and threaded it through the log template save/apply handlers (`frontend/src/logTemplates.js`, `frontend/src/screens/Today.jsx`, tests in `frontend/src/logTemplates.test.js`).
- [x] Template estimated-minutes parity (2026-07-18): task/event/todo/work-log create forms all have an "예상 소요 시간(분)" input, but none of the four localStorage-backed templates (`taskTemplates.js`, `eventTemplates.js`, `todoTemplates.js`, `logTemplates.js`) saved or restored `estimated_minutes`, so applying a saved template always left the estimate blank even if the source item had one. Added `estimated_minutes` to each `build*Template`/`apply*Template` pair and wired it through each screen's save/apply-template handlers (`frontend/src/components/TaskForm.jsx`, `frontend/src/screens/Calendar.jsx`, `frontend/src/screens/Today.jsx`), tests in each `*Templates.test.js`.
- [x] Event template priority/checklist parity (2026-07-18): task and todo templates (`buildTaskTemplate`/`buildTodoTemplate`) save and restore `priority` and `checklist`, but event templates (`buildEventTemplate`) only carried title/location/color/tags, so saving an event with a checklist or non-default priority as a template and reapplying it silently dropped both fields. Added `priority`/`checklist` to `buildEventTemplate`/`applyEventTemplate` and threaded them through `saveAsTemplate`/`applyTemplate` in the event form (`frontend/src/eventTemplates.js`, `frontend/src/screens/Calendar.jsx`, tests in `frontend/src/eventTemplates.test.js`).
- [x] Work log CSV link column (2026-07-18): tasks, events, and todos all export/import a "링크" (`link_url`) CSV column, and work logs have carried an editable `link_url` field for a while (edit form, `ExternalLink` badge), but the work log CSV export/import never picked it up, so a work log's related link couldn't be backed up or bulk-imported like every other entity. Added the "링크" column to `workLogHeaders`/`workLogsToCsv`/`parseWorkLogsCsv` (`frontend/src/csv.js`, tests in `frontend/src/csv.test.js`).
- [x] Performance timeline CSV estimated-minutes column (2026-07-18): the task/event/todo/work-log CSV exports and the printable reports all include an "예상 소요시간" estimate column, but the Performance screen's activity-timeline CSV export (`timelineToCsv`) dropped the field entirely, so a period's exported report couldn't be totaled for estimated effort. Added `estimated_minutes` to each timeline entry built in `/api/achievements` and to `timelineToCsv`'s output column (`backend/app/main.py`, `frontend/src/csv.js`, tests in `backend/tests/test_api.py` and `frontend/src/csv.test.js`).
- [x] AI quick-capture estimate/checklist parsing for events and todos (2026-07-18): the AI natural-language parser's `estimated_minutes`/`checklist` extraction only ever ran on the task branch of `rule_parse()`, even though both fields have been supported on events and todos (form, CSV, badges) for a while — an input like "회의 1시간 30분 예상" silently dropped the estimate for an event. Mirrored the existing task-branch `_estimated_minutes()`/`_checklist()` calls into the event and todo branches, and updated the remote-AI system prompt's field description to cover all three entities (`backend/app/ai.py`, tests in `backend/tests/test_ai.py`).
- [x] AI "오늘 할 일 추천" includes todos (2026-07-18): the AI assistant's "오늘 할 일 추천" (today's things to do) recommendation button, despite its name, only ever queried and ranked tasks — todos were completely excluded from the recommendation engine even though they'd reached full field parity (priority, due date, estimated minutes) with tasks on every other axis. `ai.recommendations()` now merges active (incomplete) todos with active tasks, sorted together by due/todo date and priority, tagging each item with `entity: 'task'|'todo'`; the recommendation list UI shows an entity badge per item (`backend/app/ai.py`, `backend/app/main.py`, `frontend/src/screens/AIAssistant.jsx`, tests in `backend/tests/test_ai.py`).
- [x] Event CSV estimated-minutes column (2026-07-18): tasks and todos both export/import an "예상 소요시간(분)" CSV column, and events gained an `estimated_minutes` field earlier the same day, but the event CSV export/import never picked it up, so an event's estimated duration couldn't be backed up or bulk-imported like the other two entities. Added the column to `eventHeaders`/`eventsToCsv`/`parseEventsCsv` (`frontend/src/csv.js`, tests in `frontend/src/csv.test.js`).
- [x] Calendar single-event postpone (2026-07-18): tasks, todos, and work logs all had a per-row/per-item "하루 미루기" button, and Calendar already supported bulk (multi-select) postpone, but a single event could only be moved a day via drag-and-drop on the grid — there was no button for it in the event edit form, unlike every other entity. Added a "하루 미루기" button to the event edit modal's action row reusing the existing `postponeEventDates` helper (`frontend/src/screens/Calendar.jsx`).
- [x] Work log CSV priority column (2026-07-18): tasks/events/todos all export/import a "우선순위" CSV column, and work logs gained a `priority` field earlier the same day, but the work log CSV export/import never picked it up, so a work log's priority couldn't be backed up or bulk-imported like every other entity. Added the "우선순위" column to `workLogHeaders`/`workLogsToCsv`/`parseWorkLogsCsv` (`frontend/src/csv.js`, tests in `frontend/src/csv.test.js`).
- [x] Work log per-row postpone (2026-07-18): todos already had an individual "하루 미루기" button on each row, but work logs only supported postponing a date through the multi-select bulk-action bar, so fixing one log's date meant selecting it first. Added a per-row postpone button to the work log list reusing the existing `postponeLogDate` helper (`frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`).
- [x] Checklist text search parity (2026-07-18): the Tasks screen's search and the Ctrl/Cmd+K command palette already matched query text against a task's checklist item text, but the Calendar/event search, the Today screen's todo/work-log search, and the command palette's event/todo/log entries all ignored checklist text, so content typed only into a checklist item couldn't be found by search. Added the same checklist-text matching to `filterEventsByQuery` (`frontend/src/eventSearch.js`), `filterTodosByQuery`/`filterLogsByQuery` (`frontend/src/todaySearch.js`), and the event/todo/log branches of `searchItems` (`frontend/src/commandPalette.js`), with regression tests in each file's test.
- [x] Today todo bulk "duplicate" (2026-07-18): the Tasks screen's bulk-action bar already had a bulk "복제" button, but the Today screen's todo bulk-action bar (완료 처리/태그 추가/연기/우선순위 변경/삭제) had no equivalent even though each todo row already has an individual "복제" button — duplicating several similar todos at once still meant clicking 복제 on each row individually. Added a bulk "복제" button reusing the existing `buildTodoDuplicatePayload` (`frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`).
- [x] Task bulk "duplicate" (2026-07-18): the Tasks screen's bulk-action bar already had 완료 처리/태그 추가/연기/우선순위 변경/삭제 for selected rows, and each row already had an individual "복제" button, but there was no way to duplicate several selected tasks at once — creating a batch of similar tasks meant clicking 복제 on each row individually. Added a bulk "복제" button reusing the existing `buildTaskDuplicatePayload` (`frontend/src/screens/Tasks.jsx`, `frontend/src/App.jsx`).
- [x] Gantt subtask collapse/expand (2026-07-17): the Tasks screen's gantt list rendered every subtask nested under its parent with no way to hide the subtree, so a task with many subtasks pushed unrelated top-level tasks far down the list. Added a chevron toggle on rows with children (`orderTasksHierarchically` in `frontend/src/taskHierarchy.js` now accepts a `collapsedIds` set and reports `hasChildren` per row; state persisted per-device via `frontend/src/taskCollapse.js`, wired into `frontend/src/screens/Tasks.jsx`).
- [x] Today screen "N" shortcut work-log parity (2026-07-17): the Today screen's global `N` keyboard shortcut focused the todo quick-add input, but the work-log quick-add input right below it (오늘 한 일) had no shortcut of its own even though it's one of the same 4 CONFIG-driven entities. Added `Shift+N` to focus the work-log input, mirroring `N`'s todo behavior (`frontend/src/App.jsx`, documented in `frontend/src/components/KeyboardShortcuts.jsx`).
- [x] Task/Calendar bulk "tag add" undo parity (2026-07-17): the Today screen's todo/work-log bulk "tag add" actions already showed an "실행 취소" (undo) toast, but the Tasks and Calendar bulk "tag add" actions applied the tag with no way to undo, unlike their sibling bulk actions (complete/postpone/priority-change/delete) which all support undo. Captured prior tags before the bulk update and passed an undo callback as `mutate`'s 4th argument for both `onBulkAddTag` handlers (`frontend/src/App.jsx`), matching the existing `onBulkAddTagTodo`/`onBulkAddTagLog` pattern.
- [x] Today todo bulk "priority change" (2026-07-17): the Tasks screen's and Calendar's bulk-action bars already let selected items' priority be changed all at once, but the Today screen's todo bulk-action bar (complete/tag/postpone/delete) had no priority-change equivalent even though todos carry the same `priority` field and already have a single-item priority editor and priority filter/sort. Added an `onBulkPriorityTodo` handler mirroring `onBulkPriority`'s pattern (`frontend/src/App.jsx`) and wired a matching "우선순위 변경" form with undo support into the Today screen's todo bulk-action bar (`frontend/src/screens/Today.jsx`).
- [x] Work log bulk "postpone" (2026-07-17): the Tasks, Today-todo, and Calendar bulk-action bars already let selected items' dates be shifted forward by N days at once, but the Today screen's work-log bulk-action bar (tag-add, delete) had no postpone equivalent even though logs carry the same single-date `log_date` field. Added a `postponeLogDate` helper mirroring `postponeTodoDate` (`frontend/src/ganttDrag.js`) and wired a matching "연기" form with undo support into the work-log bulk-action bar (`frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`, test in `frontend/src/ganttDrag.test.js`).
- [x] Calendar bulk priority change (2026-07-17): the Tasks screen's bulk-action bar already let selected tasks have their priority changed all at once, but Calendar's bulk-action bar (tag-add, postpone, delete) had no priority-change equivalent even though events carry the same `priority` field. Added an `onBulkPriority` prop mirroring `onBulkPostpone`'s pattern into `frontend/src/screens/Calendar.jsx`'s bulk-action bar (same Flag icon + select control as Tasks.jsx) and wired it into `frontend/src/App.jsx`'s `<Calendar>` instantiation with undo support.
- [x] Today screen printable PDF reports (2026-07-17): the Tasks and Calendar screens could already export a filtered list as a printable PDF-style report, but the Today screen's todo and work-log lists only had CSV export with no way to print the currently filtered list. Added a "PDF" button next to each CSV export button on the Today screen, reusing the same window.open + print() pattern (`frontend/src/todoReport.js`, `frontend/src/workLogReport.js`, wired into `frontend/src/screens/Today.jsx`, tests `frontend/src/todoReport.test.js`/`frontend/src/workLogReport.test.js`).
- [x] Work log timer global indicator (2026-07-17): starting the work-log timer on the Today screen and then navigating to another screen (Tasks, Calendar, …) left the timer running with no visible indication anywhere else, so it was easy to forget it was on. Added a banner in `frontend/src/components/AppShell.jsx` that polls the existing `workLogTimer.js` storage every second and, whenever a timer is running and the user isn't on the Today screen, shows the elapsed time plus a button that jumps back to Today to stop it (`frontend/src/styles.css`).
- [x] Task dependent-tasks display (2026-07-17): the task edit form's "선행 업무" picker let you choose which other tasks must finish before this one, but there was no way to see the reverse — which tasks list *this* one as a prerequisite — so completing or rescheduling a task gave no visibility into what it would unblock. Added `directDependentTasks` (`frontend/src/taskHierarchy.js`, reusing the existing `dependentTaskIds` cycle-prevention graph walk, tests added) and a read-only "후속 업무" list under the dependency picker when editing an existing task (`frontend/src/components/TaskForm.jsx`).
- [x] Task board view CSS (2026-07-17): the Tasks screen's "보드" (kanban) view toggle rendered task cards with no layout styling at all — `.task-board`/`.task-board-column`/`.task-board-card` had never been added to `styles.css`, so the board fell back to plain stacked divs instead of status columns. Added the missing kanban column/card styles, including drag-over highlight and card hover state (`frontend/src/styles.css`).
- [x] Today todo bulk "postpone" (2026-07-17): the Tasks (Gantt) screen's bulk-action bar already had a "연기" control to shift every selected task's dates forward by N days, but the Today screen's todo bulk-action bar (complete/tag/delete) had no equivalent, so postponing several todos meant editing each one's date individually. Added a `postponeTodoDate` helper mirroring `postponeTaskDates` (`frontend/src/ganttDrag.js`) and wired a matching "연기" form with undo support into the Today screen's todo bulk-action bar (`frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`, test in `frontend/src/ganttDrag.test.js`).
- [x] Calendar mobile agenda "select all" (2026-07-17): the Tasks screen and the Today screen's todo/work-log lists already had a "전체 선택" checkbox to select every visible row for bulk actions, but the Calendar screen's mobile agenda list only supported per-row checkboxes, so deleting several events at once meant checking each one individually. Reused the existing `allIdsSelected`/`toggleSelectAllIds` helpers (`frontend/src/taskFilters.js`) in `frontend/src/screens/Calendar.jsx`.
- [x] Today screen bulk-select "select all" (2026-07-17): the Tasks/Gantt screen already had a "전체 선택" checkbox to select every filtered task for bulk actions, but the Today screen's todo and work-log bulk-select lists had no equivalent, so multi-selecting required checking each row individually. Reused the existing `allIdsSelected`/`toggleSelectAllIds` helpers (`frontend/src/taskFilters.js`) in `frontend/src/screens/Today.jsx` for both the todo list and the work-log list.
- [x] CSV/ICS import failure feedback uses toasts, not `alert()` (2026-07-17): every successful CSV/ICS import (tasks/events/todos/work logs) already reported via the app's themed, non-blocking toast (`notify`), but a failed import (zero rows parsed, or row-level errors) fell back to native `window.alert()` in four screens — jarring in dark mode and blocking on mobile/PWA. Threaded the existing `notify` prop into `Tasks.jsx` and `Today.jsx` (already present in `Calendar.jsx`) and replaced every `window.alert(...)` call site with `notify(..., 'error')` (`frontend/src/screens/Tasks.jsx`, `frontend/src/screens/Today.jsx`, `frontend/src/screens/Calendar.jsx`, `frontend/src/App.jsx`).
- [x] AI quick-capture effort/duration parsing (2026-07-17): the AI natural-language parser (local rules and remote prompt) never populated `estimated_minutes` (tasks) or `duration_minutes`/`billable` (work logs) even though the manual forms fully support them, so effort data typed via AI quick-capture was silently dropped. Added `_estimated_minutes`/`_duration_minutes` regex helpers wired into the task/work_log branches of `rule_parse`, and documented the fields in the remote-AI system prompt (`backend/app/ai.py`, tests in `backend/tests/test_ai.py`).
- [x] Calendar printable PDF report (2026-07-17): the Tasks screen could already export a filtered list as a printable PDF-style report, but the Calendar screen only had ICS/CSV export with no way to print the currently filtered event list. Added a "PDF" button to the Calendar toolbar reusing the same window.open + print() pattern (`frontend/src/eventReport.js`, wired into `frontend/src/screens/Calendar.jsx`, test `frontend/src/eventReport.test.js`).
- [x] Calendar event form inline validation (2026-07-17): the task edit form already showed inline red-border/error-text validation for empty title and due-date-before-start-date directly on the offending field, but the calendar event form only surfaced the same class of errors (empty title, end time before/equal to start time) as a single generic banner at the bottom of the form. Added `validateEventForm` to `frontend/src/formValidation.js` and wired the same `fieldErrors`/`invalid`/`aria-invalid`/`field-error` pattern into the event form's title/end time inputs (`frontend/src/screens/Calendar.jsx`).
- [x] Comment count badges for todos/work logs/events (2026-07-17): tasks already showed a "댓글 N개" badge on Gantt rows, but todos, work logs, and calendar events all have the same comment-thread feature with no list-view indicator, so a comment could sit unnoticed until the edit form was opened. Extended the same `comment_count` aggregate (`backend/app/main.py`'s `list_endpoint`) to `todos`/`work_logs`/`events`, and added the matching badge to the Today screen's todo/work-log rows and the Calendar screen's agenda rows (`frontend/src/screens/Today.jsx`, `frontend/src/screens/Calendar.jsx`), test `test_todo_and_work_log_and_event_list_include_comment_count` in `backend/tests/test_api.py`.
- [x] Work log file attachments (2026-07-17): tasks, calendar events, and todos all got real file attachments, but work logs (오늘 한 일) were the last of the four entities with only multi-link URL attachments and no way to attach an actual file. Reused the `task_attachments`/`todo_attachments` pattern for work logs — a `work_log_attachments` table, `/api/work_logs/{id}/attachments` upload/list/download/delete endpoints, `frontend/src/api.js` helpers, and an upload/download/delete section in the Today screen's work log edit row (`backend/app/db.py`, `backend/app/main.py`, `frontend/src/screens/Today.jsx`, test `test_work_log_attachments_upload_download_delete_and_size_limit` in `backend/tests/test_api.py`).
- [x] Task comment count badge (2026-07-17): tasks already had a 댓글 (comment) thread, but the Gantt row list gave no indication a task had any comments at all — unlike the checklist/estimate/link badges, the only way to know was opening the edit form. `GET /api/tasks` now attaches a `comment_count` per task via one aggregate query over `task_comments` (`backend/app/main.py`'s `list_endpoint`), surfaced as a "댓글 N개" badge next to the other row badges (`frontend/src/screens/Tasks.jsx`), regression test `test_task_list_includes_comment_count` in `backend/tests/test_api.py`.
- [x] Todo file attachments (2026-07-17): tasks and calendar events both got real file attachments, but todos (할 일) only had multi-link URL attachments, so a task-shaped checklist item needing a screenshot or document still had nowhere to put it. Reused the `task_attachments`/`event_attachments` pattern for todos — a `todo_attachments` table, `/api/todos/{id}/attachments` upload/list/download/delete endpoints, `frontend/src/api.js` helpers, and an upload/download/delete section in the Today screen's todo edit row (`backend/app/db.py`, `backend/app/main.py`, `frontend/src/screens/Today.jsx`, tests in `backend/tests/test_api.py`).
- [x] Approval nav badge (2026-07-17): pending completion-approval and schedule-change-approval tasks were only visible after opening the Tasks screen — no indicator anywhere else. The sidebar/bottom-nav "업무" badge now adds the pending-approval count to the existing overdue count (and swaps its label to "승인 대기" when there's no overdue but there is a pending approval), via a new pure `pendingApprovalCount` helper (`frontend/src/taskFilters.js`, wired into `frontend/src/App.jsx`'s `navBadges`, tests in `frontend/src/taskFilters.test.js`).
- [x] Event file attachments (2026-07-17): events only supported a related-link field (like tasks did before file attachments were added), so sharing meeting materials/screenshots required an external cloud link. Reused the proven `task_attachments` pattern for events — an `event_attachments` table, `/api/events/{id}/attachments` upload/list/download/delete endpoints, and an upload/download/delete section in the calendar event form (`backend/app/db.py`, `backend/app/main.py`, `frontend/src/screens/Calendar.jsx`'s `EventForm`, `frontend/src/api.js`, tests in `backend/tests/test_api.py`).
- [x] Task file attachments (2026-07-17): tasks only supported URL link attachments, so sharing a document or screenshot required an external cloud link. Added real file attachments — a `task_attachments` table (base64-encoded blob, 5MB/file and 20/task limits), `/api/tasks/{id}/attachments` upload/list/download/delete endpoints, and an upload/download/delete section in the task edit form (`backend/app/db.py`, `backend/app/main.py`, `frontend/src/components/TaskForm.jsx`, `frontend/src/api.js`, tests in `backend/tests/test_api.py`).
- [x] Task list select-all (2026-07-17): the 업무 관리 (Tasks) Gantt view's bulk action bar (complete/tag/postpone/priority/delete) required checking every row individually, unlike a "select all" checkbox in commercial task tools. Added a "전체 선택" checkbox in the Gantt header that selects/clears every currently filtered task in one click (`frontend/src/taskFilters.js` new `allIdsSelected`/`toggleSelectAllIds` helpers, `frontend/src/screens/Tasks.jsx`, `frontend/src/styles.css`, tests in `frontend/src/taskFilters.test.js`).
- [x] API request timeout (2026-07-17): `frontend/src/api.js`'s `request()` had no client-side timeout or abort, so a hung/slow backend left save/load buttons stuck in their "처리 중" state indefinitely with no recovery short of a page reload. Added a 20-second internal `AbortController` timeout (combined with any caller-supplied `signal`, e.g. `Performance.jsx`'s achievements fetch, via `AbortSignal.any`), surfacing a distinct "요청 시간이 초과되었습니다. 다시 시도해 주세요." message instead of the generic network-error copy (`frontend/src/api.js`, tests in `frontend/src/api.test.js`).
- [x] Invoice client name (2026-07-17): the printable invoice PDF showed billable hours/amount but no "bill to" line, so every invoice still needed the client name hand-written on the printout. Added a `billing_client_name` per-user setting (Settings screen, alongside the existing billing hourly rate), returned from `/api/settings/workflow` and `/api/achievements`, and rendered as a "청구 대상: ..." line at the top of the invoice (`backend/app/main.py`, `frontend/src/screens/Settings.jsx`, `frontend/src/invoiceReport.js`, `frontend/src/screens/Performance.jsx`).
- [x] Invoice PDF export (2026-07-17): billable minutes/hourly rate/billable amount were all tracked (Performance stats, CSV, Markdown) but there was no client-ready invoice document — pulling billable work into an invoice meant rebuilding a table by hand. Added a "청구서 PDF" button on the Performance screen that opens a printable invoice (billable work log rows for the selected period, total hours, and hourly-rate × hours amount) via `window.print()`, reusing the existing `data.work_logs`/`GET /api/achievements` response (`frontend/src/invoiceReport.js`, wired into `frontend/src/screens/Performance.jsx`).
- [x] Markdown performance report billable amount (2026-07-17): the report shared `billable_minutes` with the Performance screen/CSV but omitted `billable_amount`, so exported reports needed manual math against the billing rate. Added the same "청구 예상 금액" line to `performanceReportMarkdown` when an hourly rate is set (`frontend/src/performanceReport.js`).
- [x] Work log ICS export (2026-07-17): the Tasks, Calendar, and Today-todo lists could all already export ICS calendar files (`tasksToIcs`/`eventsToIcs`/`todosToIcs`), but Today's work-log ("오늘 한 일") list only had CSV/PDF export despite `work_logs` carrying the same `log_date`/`log_time` schedule fields as todos. Added `logsToIcs`/`logIcsFilename` mirroring `todosToIcs` (all-day VEVENT if no `log_time`, timed VEVENT sized by `duration_minutes` if present, `[기록]` prefix using `content`) and wired an `exportLogsIcs` handler + ICS toolbar button into `Today.jsx`'s work-log list next to the existing PDF/CSV buttons (`frontend/src/ics.js`, `frontend/src/screens/Today.jsx`, tests in `frontend/src/ics.test.js`). This closes the export/print-parity axis across all four entity lists (Tasks/Calendar/Today-todo/Today-log).

See docs/IMPROVEMENT_PLAN.md for the current real-use readiness plan (Wave 1: P0-P4, done; Wave 2: AI summary evidence and team/assignee feature removal, done; Wave 3: correctness audit and bug fixes, done; Wave 4: undo-delete toast and a browser-verified layout fix, done; Wave 5: live user feedback queue, done; Wave 6: visual design refresh and dark-mode color fixes, done; Wave 7: stability and feature items 33-46, mostly done — see plan for per-item status).

## Wave 7 highlights (2026-07-11 ~ 2026-07-12)

- [x] Backup restore: /api/import with preview → merge/replace apply, Settings upload UI; restored events never touch Google Calendar.
- [x] Ctrl/⌘+K command palette: unified search over tasks/events/todos/logs plus screen navigation, on top of the existing AI capture.
- [x] Weekly review preset (지난주 리뷰) on the Performance screen reusing summary + Markdown export.
- [x] Event-start notifications 15 minutes ahead while the app is open.
- [x] Tag management in Settings: usage counts, bulk rename/merge/remove across all four tables; each tag chip now has a shortcut button that jumps to the Tasks screen pre-filtered by that tag (2026-07-16).
- [x] Calendar CSV import to match existing task/todo/work-log CSV round-trip (2026-07-16).
- [x] Audit log date-range filter: `/api/audit-logs?start=&end=` plus a date range picker in the Audit Log screen toolbar, so the 180-day retention window can actually be browsed instead of only the latest 200 entries (2026-07-16).
- [x] Compose healthcheck for the api container; audit-log retention (180d default); scheduled backup container with verify (33/35/36, partly via the Codex loop).
- [x] Server error visibility panel (37): Settings shows the last 5 unhandled server errors with a manual 새로고침 button to reload without a full page refresh.
- [x] Gantt bar drag (39), calendar event drag (40, now covers both month and week views — 2026-07-12).
- [x] Mobile form polish (42): the shared `.form-actions` button row (task/event forms, delete confirm dialogs) let flexbox shrink buttons below their content width on narrow viewports, wrapping Korean labels mid-word (e.g. "휴지통으 로 이동"). Verified with a 390px headless-browser screenshot of the task edit form; fixed by wrapping the row and giving buttons a fixed content-based size (2026-07-12).
- [x] Skeleton loading (41): Tasks/Gantt list shows shimmering skeleton rows (reusing the existing `.skeleton` pattern from Settings) instead of the empty-state message while `dataLoading` is true and no tasks have arrived yet.
- [x] Task templates (44): save the current new-task form as a named template (title/priority/recurrence/tags/duration) and prefill a new task from a saved template; localStorage-only (`frontend/src/taskTemplates.js`).
- [x] Dependency picker search (2026-07-16): the "선행 업무" checkbox list in the task edit form had no way to filter a long task list — added a search box (shown once there are more than 5 options) that filters by title while keeping already-checked-but-filtered-out items in the DOM so their state survives form submission (`frontend/src/taskHierarchy.js`'s `matchesDependencyFilter`, `frontend/src/components/TaskForm.jsx`).
- [x] Notification quiet hours (2026-07-16): the today-digest, event-alert, and todo-alert notifications fired at any hour with no way to silence them overnight while the PWA stayed open — added a "무음 시간대" toggle + start/end time in Settings (`frontend/src/eventAlerts.js`'s `isWithinQuietHours`/`loadQuietHours`, guarding all three `showNotification` call sites in `frontend/src/App.jsx`).

## Real-Use Readiness (2026-07-10)

- [x] Repair mojibake in AI settings UI and backend AI status strings; align PUT /api/settings/ai response with GET.
- [x] Keep AI provider/model/API key drafts matched per provider in the Settings screen so switching vendors does not blend settings.
- [x] Optional per-user approval workflow toggle so personal use skips self-approval of completed work and schedule changes.
- [x] Today screen: completion toggle limited to the check icon to prevent accidental taps marking tasks done.
- [x] Local state updates for todo/work-log mutations instead of full refetch (P2).
- [x] Calendar week view with month/week switcher (P2).
- [x] Performance report presets and Markdown export (P3).
- [x] Work log to task linking UI on the Today screen (P3).
- [x] Backup verification script (scripts/verify_backup.py) and app version display in Settings (P4).
- [x] E2e smoke test: login-bypass session token exercising full create/read/update/delete/restore round trip for every core entity (P4).
- [x] AI performance summary shows a structured evidence list (which logs/tasks/events it used), not just prose (Wave 2).
- [x] Undo action on delete toasts for tasks, events, todos, and work logs (Wave 4).
- [x] Ctrl/Cmd+K quick capture: natural-language entry from any screen via the existing AI preview/apply pipeline (Wave 4).
- [x] AI parse/apply handles multiple actions from one input, previewed and applied one at a time (Wave 5).
- [x] Calendar event end time auto-follows start+1h until manually edited; day-cell events sort chronologically (Wave 5).
- [x] PWA orientation locked to portrait (Wave 5).
- [x] Mobile bottom nav fits its 7-item CSS again — Audit Log moved into a Settings section (Wave 5).
- [x] Read-only sample login ("샘플로 미리 보기") with seeded demo data for pre-signup preview (Wave 5).
- [x] Visual design refresh: consistent radius/shadow/motion tokens across cards, buttons, and inputs; pill-shaped progress bars; fixed six hardcoded-color dark-mode bugs found via screenshot verification (Wave 6).

## Task Management & Reporting

- [x] Fix user-reported task edit save failure: saving changes from the 업무/Tasks screen currently shows an error and must be diagnosed, covered by a regression test, and fixed before adding more task-editing features.
- [x] Add Gantt-style task hierarchy editing: tasks must support parent/child placement, moving existing tasks under another task, promoting a child back to top level, and safely preventing invalid cycles.
- [x] Basic dependency blocker visibility in the task management screen.
- [x] Overdue task visibility in the task management screen.
- [x] Basic approval workflow for completed work in the task management screen.
- [x] Basic approval workflow for schedule changes.
- [x] Approval queue filters for completed-work and schedule-change review.
- [x] Repo-root backend regression coverage for task edit save flows.
- [x] Basic reminder rules for upcoming and overdue work in the task management screen.
- [x] Skip-recurrence for recurring tasks (parity with the existing todo skip-recurrence), so an occurrence that shouldn't be done (e.g. a holiday) can be advanced without a fake completion.
- [x] Basic priority filtering in the task management screen.
- [x] Basic CSV task export for customer and management reporting.
- [x] PDF reporting exports for customer and management reporting.
- [x] Audit log browsing and filtering in the app UI.
- [x] Feature request status management in the Changelog screen for the Codex improvement loop.
- [x] Audit log retention limit: entries older than a configurable window (default 180 days, `AUDIT_LOG_RETENTION_DAYS`) are pruned on server startup instead of growing unbounded (Wave 7 item 36).
- [x] docker-compose.yml healthcheck for the api container (`/api/ready`), with backup/web waiting on `condition: service_healthy`, matching the existing portainer-stack.yml pattern (Wave 7 item 35).
- [x] Subtask completion summary ("하위 업무 n/m 완료") shown on parent rows in the task list (Wave 7 item 37).
- [x] Gantt task list sorts sibling rows by start date (falling back to due date, then title) instead of alphabetically, so the visible order matches the actual schedule (Wave 8 item 38).
- [x] Todo template checklist parity (2026-07-18): task templates already saved/restored a checklist, but todo templates only carried title/priority/recurrence/tags, and the Today screen's todo quick-add form had no checklist editor at all, so a new todo could only get sub-steps by saving then reopening it in edit mode. Added a checklist editor to the todo quick-add form and extended `buildTodoTemplate`/`applyTodoTemplate` (`frontend/src/todoTemplates.js`) to carry checklists, mirroring `taskTemplates.js` (`frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`).
- [x] Task dependency picker in the task form: `dependency_ids` had full backend cycle-prevention and blocker display, but no UI ever let a user set it — the form now includes a "선행 업무" multi-select (Wave 9 item 39).
- [x] Estimated-time overrun badge on the task list row: `taskEstimateOverrun`/`taskWorkLogsTotalMinutes` (`taskLogs.js`) already flagged overruns inside the "관련 업무 기록" modal, but a user had to open that modal per task to notice one. `Tasks.jsx` now receives the `logs` list from `App.jsx` and shows a "N시간 초과" badge directly on the gantt/board row when logged time exceeds the estimate, reusing the existing helpers and CSS class (Wave 10 item 40, 2026-07-16).
- [x] Event templates: tasks and today's quick todos already had localStorage-based save/apply/delete templates, but the calendar event form had no equivalent, so recurring meetings had to be retyped each time. Added the same pattern for events (title/location/color/tags, `frontend/src/eventTemplates.js`, wired into `Calendar.jsx`'s `EventForm`, 2026-07-14).

- [x] Task unblock notification: the Tasks screen already surfaced a "선행 업무 대기" banner/filter for blocked tasks, but nothing told a user when a blocking dependency finally finished, so a now-actionable task stayed invisible until the Tasks screen was reopened. Added `newlyUnblockedTasks` (`frontend/src/taskFilters.js`) and a browser-notification effect in `App.jsx` matching the existing task/event/todo alert pattern, 2026-07-17.

## Backlog (identified 2026-07-11, not yet implemented)

- [x] Global search across tasks/events/todos/work logs from a single command palette or search box — already covered by the Ctrl/⌘+K command palette (`commandPalette.js` `searchItems`/`searchScreens`, wired into `QuickCapture`), found already implemented while auditing this backlog.
- [x] Bulk task actions: multi-select checkboxes in the Gantt list with a bulk action bar to complete or delete several tasks at once (2026-07-11), plus bulk tag add for selected tasks (2026-07-12).
- [x] Configurable reminder digest: a Settings toggle lets the existing daily device notification widen from "due today only" to "overdue + due within 2 days", stored locally per device (`reminderDigestTasks` in `taskFilters.js`). Server-side scheduled email/push digests remain out of scope (no outbound email infra yet).
- [x] Task list sort order: the Gantt/task list only ever sorted siblings by start date. Added a "정렬" dropdown (일정순/우선순위순/진행률순/제목순) on the Tasks screen; hierarchy grouping (parent above children) is unchanged, only sibling order within a group changes (`TASK_SORT_COMPARATORS` in `taskHierarchy.js`, 2026-07-12).
- [x] Filter reset on the Tasks screen: search/status/priority/tag filters each had to be cleared one at a time. A "필터 초기화" button now appears in the toolbar whenever any filter differs from the default and resets all of them at once (`hasActiveTaskFilters`/`DEFAULT_TASK_FILTERS` in `taskFilters.js`, 2026-07-12).
- [x] Calendar ICS export: WorkManager had JSON/CSV/PDF export but no interoperable calendar format, so events couldn't be taken into Google Calendar/Outlook/Apple Calendar. Added a client-side "ICS" button on the Calendar screen toolbar that exports the currently tag-filtered event list as a standard `.ics` file (`eventsToIcs`/`icsFilename` in `frontend/src/ics.js`, wired into `Calendar.jsx`, 2026-07-12).
- [x] AI settings connection test: saving an AI provider/key in Settings gave no feedback on whether the credentials actually worked until the next real AI parse silently fell back to local rules. Added a "연결 테스트" button that makes one minimal live call to the configured provider and reports success or a specific failure reason (bad key vs. server error) (`ai.test_connection` + `POST /api/settings/ai/test` in the backend, wired into `Settings.jsx`, 2026-07-12).
- [x] Saved task filter presets: re-entering the same search/status/priority/tag filter combination on the Tasks screen every session was repetitive. Added "필터 저장" / "저장된 필터" dropdown / "필터 삭제" controls that name and persist the current filter combination to localStorage (same pattern as task templates), so a saved view can be reapplied in one click (`frontend/src/taskFilterPresets.js`, wired into `Tasks.jsx`, 2026-07-13).
- [x] Task duplication: starting a new task similar to an existing one meant retyping title/dates/priority/tags from scratch, since recurrence rules only cover exact repeats. Added a "복제" button on each Gantt row that copies the task (title suffixed "(복사본)", schedule, priority, tags, parent, dependencies) into a new todo with progress/approval state reset (`buildTaskDuplicatePayload` in `frontend/src/taskFormPayload.js`, wired into `Tasks.jsx`/`App.jsx`, 2026-07-12).

- [x] Trash search/filter: the 휴지통 (trash) section only ever listed every soft-deleted row with no way to narrow it down, which got unwieldy once several tasks/events/todos/logs were deleted. Added a search box (title/content) and a type filter (업무/일정/오늘 할 일/업무 기록) to `TrashSection.jsx`, backed by a pure `filterTrashItems`/`trashTables` module with tests (`frontend/src/trashFilter.js`, 2026-07-13).
- [x] "Due this week" task status filter: the Tasks screen status filter only had "지연 업무" (overdue) to spot at-risk work, with no quick way to see everything coming due in the next 7 days for weekly planning. Added a `due_this_week` option to the existing status filter (`filterTasks` in `frontend/src/taskFilters.js`, wired into `Tasks.jsx`, 2026-07-13).
- [x] Task sort persistence: the Tasks screen "정렬" dropdown (일정순/우선순위순/진행률순/제목순) reset to 일정순 on every reload/revisit, losing a user's preferred order. Added `loadTaskSort`/`saveTaskSort` (localStorage, same pattern as the existing filter presets) in `frontend/src/taskHierarchy.js`, wired into `Tasks.jsx`, 2026-07-13.
- [x] Performance report CSV export: the 성과 (Performance) screen could only export the period report as Markdown, so pulling the activity timeline into a spreadsheet for management reporting meant retyping it. Added a "CSV 내보내기" button next to the existing Markdown export that downloads the same date/tag-filtered timeline as CSV (`timelineToCsv`/`timelineCsvFilename` in `frontend/src/csv.js`, wired into `Performance.jsx`, 2026-07-13).
- [x] Audit log CSV export: the 감사 로그 screen could only be read on-screen, with no way to pull it out for compliance review or an external auditor. Added a "CSV 내보내기" button that downloads the current search/entity-filtered results (timestamp/action/entity/id/detail) as CSV, reusing the export pattern from Tasks/Performance (`auditLogsToCsv`/`auditLogCsvFilename` in `frontend/src/csv.js`, wired into `AuditLog.jsx`, 2026-07-13).
- [x] AI multi-item parsing only split requests on newlines, so a single-line numbered list like "오늘해야할일 1.AAA 2. BBB 3. CCC" (user request #16) was parsed as one item instead of three. `rule_parse_multi` now also splits numbered list markers (`1.`/`2)`) within a line, and each new task/todo has its tags auto-matched against existing tasks' titles/tags via `_match_tags`; the remote-AI prompt was updated with the same instructions (`backend/app/ai.py`, 2026-07-13).
- [x] AI task checklist extraction only worked via the remote AI prompt; the local rule-based fallback (`rule_parse`, used when no API key is configured) always split any numbered list into separate tasks via `rule_parse_multi`, so it could never populate a single task's `checklist`. Added a `단계`/`체크리스트`/`하위 항목`/`세부 항목`/`순서로` keyword trigger: when present, the numbered list is kept as one task and parsed into `data.checklist` instead of being split into multiple items (`_checklist`/`_numbered_segments` in `backend/app/ai.py`, 2026-07-14).
- [x] Work log time tracking: work logs had no notion of how long a piece of work took, so Performance reports could count activity but not effort. Added an optional `duration_minutes` field to work logs (backend `work_logs` table/`WorkLogPayload`), a minutes input on the Today screen's log entry/edit forms, and a "기록된 소요 시간" total in the Performance screen's stat tiles and Markdown report (`formatDuration` in `frontend/src/performanceReport.js`, 2026-07-13).
- [x] Task CSV import: WorkManager could export tasks to CSV/PDF but had no way to bring a bulk-prepared task list back in, so onboarding a backlog of existing work meant creating each task by hand. Added a "CSV 가져오기" button on the Tasks screen that reads a file in the same column format as the existing export (제목/우선순위/시작일/기한/태그/메모), skips rows without a title, and bulk-creates the rest via the existing `POST /api/tasks` (`parseTasksCsv` in `frontend/src/csv.js`, wired into `Tasks.jsx`/`App.jsx`, 2026-07-13).
- [x] Clear completed todos on the Today screen: the 오늘 할 일 quick-todo list had no bulk cleanup, so completed items had to be deleted one at a time to keep the list tidy. Added a "완료된 항목 정리 (n)" button that appears once at least one visible todo is completed and deletes all of them (reusing the existing delete-confirmation dialog) in one action (`Today.jsx`/`App.jsx`, 2026-07-13).
- [x] Task pinning: the Tasks screen sort dropdown (일정순/우선순위순/진행률순/제목순) always reordered the whole list, so an important task could scroll out of easy reach depending on the chosen sort. Added a "고정" star button on each Gantt row that keeps pinned tasks first among their siblings regardless of `sortBy`, backed by a localStorage set (same pattern as filter presets/task templates) in `frontend/src/taskPins.js`, applied inside `orderTasksHierarchically` in `frontend/src/taskHierarchy.js`, 2026-07-13.
- [x] Task estimated vs. actual time: work logs already tracked `duration_minutes`, but tasks had no planned-effort field to compare it against, so Performance reports could show time spent but never whether it matched the plan. Added a nullable `estimated_minutes` column on tasks (backend `TaskPayload`/`CONFIG`/`db.py` migration), an input on the task form, an "예상 Xh" badge on Gantt rows, and an "완료 업무 예상 소요 시간" stat/report line next to the existing "기록된 소요 시간" total (`backend/app/main.py` achievements summary, `frontend/src/performanceReport.js`, `frontend/src/screens/Performance.jsx`, 2026-07-13).
- [x] Calendar CSV export: the Calendar screen only offered an ICS download; Tasks/Performance/Audit Log all also had a CSV export for spreadsheet/reporting use, but events didn't. Added a "CSV" button next to the existing ICS export that downloads the current tag-filtered event list (제목/시작/종료/종일 여부/장소/태그/메모) as CSV (`eventsToCsv`/`eventCsvFilename` in `frontend/src/csv.js`, wired into `Calendar.jsx`, 2026-07-13).
- [x] Todo carryover: the Today screen's 오늘 할 일 list only ever fetched today's todos, so an incomplete todo from a past date silently disappeared from view instead of rolling forward like it does in other daily planners. Added a banner ("지난 할 일 n개가 남아 있습니다") with a one-click "오늘로 이월" button that bulk-updates those todos' `todo_date` to today (`overdueIncompleteTodos` in `frontend/src/todoCarryover.js`, wired into `Today.jsx`/`App.jsx`, 2026-07-13).
- [x] "No due date" task status filter: tasks left without a start/due date slipped through both the default 진행 업무 view and the 지연 업무 (overdue) filter, so they were easy to forget entirely. Added a `no_due_date` option to the existing status filter (`filterTasks` in `frontend/src/taskFilters.js`, wired into `Tasks.jsx`, 2026-07-13).
- [x] Fix task-delete parent/child data divergence: deleting a task with subtasks only cleared the hierarchy visually (the frontend already treated an orphaned `parent_id` as top-level), but the DB row still pointed `parent_id` at the now-deleted parent, so restoring that parent from trash silently re-nested the children again. `DELETE /api/tasks/{id}` now clears `parent_id` on direct children in the same transaction (audited as an `update`), and the delete-confirmation dialog tells the user how many subtasks will be promoted (`backend/app/main.py` delete_endpoint, `frontend/src/App.jsx`, regression test `test_deleting_parent_task_promotes_children_to_top_level` in `backend/tests/test_e2e_smoke.py`, 2026-07-13).
- [x] Todo recurrence: unlike tasks, the 오늘 할 일 quick-todo list had no repeat option, so daily/weekly chores had to be re-typed every day. Added a nullable `recurrence_rule` (매일/매주) on todos with the same idempotent spawn-on-completion pattern as recurring tasks (`spawn_recurring_todo` in `backend/app/main.py`, `backend/app/db.py` migration), a recurrence dropdown on the Today screen's add/edit todo forms, and a regression test `test_completing_recurring_todo_spawns_next_occurrence` in `backend/tests/test_api.py`, 2026-07-13.
- [x] Keyboard shortcuts help: WorkManager had a Ctrl/⌘+K quick-capture shortcut but no discoverable list of shortcuts, so it was easy to forget it existed. Pressing "?" (or clicking a new "?" button in the sidebar) opens a modal listing the app's keyboard shortcuts (`frontend/src/components/KeyboardShortcuts.jsx`, wired into `App.jsx`/`AppShell.jsx`, 2026-07-13).
- [x] Task audit history link: the 감사 로그 (audit log) screen could only be searched manually, so checking a single task's change history meant guessing its id in the search box. Added an "이력" button on each Gantt row that opens the audit log pre-filtered to that task (`onViewHistory` in `Tasks.jsx`, `focus` prop in `AuditLog.jsx`, wired into `App.jsx`, 2026-07-13).
- [x] Calendar event search: the Calendar screen could only narrow events by tag, with no way to find a specific event by title/location/memo text the way Tasks and 휴지통 already could. Added a "제목, 장소, 메모 검색" search box next to the existing tag filter (`filterEventsByQuery` in `frontend/src/eventSearch.js`, wired into `Calendar.jsx`, 2026-07-13).
- [x] Calendar event duplication: Tasks already had a one-click "복제" button to copy a similar item, but events had no equivalent, so recreating a similar one-off event meant retyping title/time/location/tags from scratch. Added a "복제" button in the event edit form that creates a new event titled "(사본)" with the same time/location/tags (`buildEventDuplicatePayload` in `frontend/src/eventDuplicate.js`, wired into `Calendar.jsx`, 2026-07-13).
- [x] Overdue task count badge on navigation: the app had no at-a-glance indicator of how many tasks were overdue outside the Tasks screen itself. Added a red count badge on the "업무" sidebar/mobile-nav item, computed via the existing `summarizeDueReminders` overdue count and passed from `App.jsx` into `AppShell.jsx` as `navBadges` (2026-07-13).
- [x] Todo duplication: Tasks and Calendar events already had a one-click "복제" button, but the Today screen's quick-todo list had no equivalent, so recreating a similar recurring or one-off todo meant retyping it from scratch. Added a duplicate button on each todo row that copies title/tags/recurrence into a new todo dated today with completion reset (`frontend/src/todoDuplicate.js`, wired into `Today.jsx`/`App.jsx`, 2026-07-13).
- [x] Task comment thread: tasks only ever had a single `description`/memo field, with no way to leave multiple timestamped progress notes over the life of a task (distinct from the audit log, which tracks field changes, not free-text discussion). Added a `task_comments` table and `/api/tasks/{id}/comments` GET/POST/DELETE endpoints, plus an inline "댓글" panel in the task edit form that fetches/adds/deletes comments for the open task (`backend/app/main.py`, `backend/app/db.py`, `frontend/src/components/TaskForm.jsx`, regression test `test_task_comments_are_user_scoped_and_persisted` in `backend/tests/test_api.py`, 2026-07-14).
- [x] Task comment editing: the comment thread above only supported add/delete, so fixing a typo or updating a stale note meant deleting the comment and losing its original timestamp. Added `PATCH /api/tasks/{id}/comments/{comment_id}` (nullable `edited_at` column on `task_comments`) and made each comment in the "댓글" panel click-to-edit inline, showing "(수정됨)" once edited (`backend/app/main.py`, `backend/app/db.py`, `frontend/src/components/TaskForm.jsx`/`api.js`, regression test extended in `test_task_comments_are_user_scoped_and_persisted`, 2026-07-15).
- [x] Calendar event recurrence: tasks and todos both had a `recurrence_rule` (daily/weekly), but locally-created calendar events had no repeat option at all — the existing `events.recurrence` column only ever held Google's own RRULE strings mirrored during sync, never something a user could set for a local event. Added a "반복"(매일/매주/매월) + "반복 종료일" control to the new-event form that expands into individual event rows up to the end date (capped at 52 occurrences) via a pure `expandRecurringEvent` (`frontend/src/eventRecurrence.js`, with tests), wired into `Calendar.jsx`'s `EventForm` and `App.jsx`'s bulk-create `onCreate` handler, 2026-07-13.
- [x] Todo priority: tasks had low/normal/high priority but the Today screen's quick-todo list had no way to flag something as more important than the rest, so urgent todos blended in with routine ones. Added a nullable `priority` column on todos (backend `TodoPayload`/`CONFIG`/`db.py` migration, default `normal`), a priority select on the add/edit todo forms, a left-border accent + "우선순위 높음" badge for high-priority rows, and priority carried through on recurrence spawn and duplication (`backend/app/main.py`, `frontend/src/screens/Today.jsx`, `frontend/src/todoDuplicate.js`, 2026-07-13).
- [x] Notification history: device notifications (today's task digest, event start alerts) were one-shot — once dismissed there was no way to see what had fired earlier. Added a localStorage-backed history (`frontend/src/notificationHistory.js`) populated whenever either existing notification effect fires, shown as a "최근 알림 기록" list with a clear button in Settings' "오늘 업무 알림" section, 2026-07-14.
- [x] Today todo list priority sort: todos displayed a priority badge but never used it to order the list, so a high-priority item stayed buried unless manually pinned. Added a secondary sort by `priority` (high → normal → low) after the existing pin-tier sort in `orderTodosByPin` (`frontend/src/todoPins.js`, 2026-07-13).
- [x] Todo CSV export: Tasks/Calendar/Audit Log all had a CSV export button, but the Today screen's quick-todo list had no way to pull it into a spreadsheet. Added a "CSV 내보내기" button next to the existing "완료된 항목 정리" action that downloads the current search/tag-filtered todo list (제목/완료 여부/우선순위/반복/날짜/태그) (`todosToCsv`/`todoCsvFilename` in `frontend/src/csv.js`, wired into `Today.jsx`, 2026-07-13).
- [x] Today todo priority filter: the Today screen's 오늘 할 일 list could only be narrowed by tag or free-text search, with no way to isolate just the high-priority items even though priority sort/badges already existed. Added a "모든 우선순위/높음/보통/낮음" select next to the existing search/tag filter (`filterTodosByPriority` in `frontend/src/todaySearch.js`, wired into `Today.jsx`, 2026-07-13).
- [x] Task related link: tasks had a free-text 메모(description) field but no structured way to attach a reference URL (design doc, ticket, meeting notes), so links ended up pasted inline in the memo where they were easy to miss. Added a nullable `link_url` column on tasks (backend `TaskPayload`/`CONFIG`/`db.py` migration, http(s)-only validation), a "관련 링크" input on the task form, and a clickable "관련 링크" badge (opens in a new tab) on Gantt rows when a task has one (`backend/app/main.py`, `frontend/src/components/TaskForm.jsx`, `frontend/src/taskFormPayload.js`, `frontend/src/screens/Tasks.jsx`, 2026-07-13).
- [x] Work log timer: work logs already had a `duration_minutes` field, but it had to be typed in by hand after the fact, so recorded time was often a rough guess. Added a "타이머 시작/중지" control on the Today screen's 오늘 한 일 quick-entry form that tracks elapsed time client-side (localStorage-persisted, same pattern as `taskPins.js`) and fills `duration_minutes` automatically on stop (`frontend/src/workLogTimer.js`, wired into `frontend/src/screens/Today.jsx`, 2026-07-13).
- [x] Task checklist: tasks already supported parent/child hierarchy for splitting big work into separate tasks, but had no lightweight way to track sub-steps within a single task as it progressed. Added a nullable `checklist` JSON column on tasks (backend `TaskPayload`/`CONFIG`/`db.py` migration, item shape `{id,text,done}`, also decoded during merged-resource validation in `merged_resource_for_validation`), an add/check/delete checklist editor in the task form, and a "체크리스트 n/m" progress badge on Gantt rows (`backend/app/main.py`, `frontend/src/components/TaskForm.jsx`, `frontend/src/taskFormPayload.js`, `frontend/src/screens/Tasks.jsx`, 2026-07-13).
- [x] One-click task postpone: rescheduling an overdue task required opening the edit form and retyping both dates, even though the Gantt bar already supported drag-to-reschedule. Added a "미루기" button on overdue Gantt rows that shifts the due date (and start date, preserving duration) forward by one day via the existing `onReschedule`/`api.updateTask` path (`postponeTaskDates` in `frontend/src/ganttDrag.js`, wired into `frontend/src/screens/Tasks.jsx`, 2026-07-13).
- [x] Today nav badge: the sidebar/mobile-nav already showed a red overdue-task count badge on "업무", but "오늘" had no equivalent, so knowing how many todos were still pending today required opening the Today screen. Added a badge showing the incomplete-todo count on the "오늘" nav item, reusing the same `nav-badge` styling and generalizing `AppShell`'s badge prop to carry a per-item label for the mobile aria description (`pendingTodoCount` in `frontend/src/App.jsx`, `frontend/src/components/AppShell.jsx`, 2026-07-13).
- [x] Configurable event alert lead time: the "곧 시작" event notification was hard-coded to fire exactly 15 minutes before an event started, which was too short for events needing travel time. Added a "일정 시작 전 알림 시점" select (5/10/15/30분) to the Settings screen's 오늘 업무 알림 card, backed by a pure `eventsDueForAlert`/`loadEventAlertLeadMinutes` in `frontend/src/eventAlerts.js` with tests, wired into `frontend/src/App.jsx`'s existing event-alert effect, 2026-07-13.
- [x] Todo pinning on the Today screen: the Tasks screen already had a "고정" star button to keep an important task at the top of its group regardless of sort order, but the Today screen's 오늘 할 일 quick-todo list had no equivalent, so an urgent todo could get buried among routine ones. Added the same pin button/pattern to each todo row, backed by a localStorage set (`frontend/src/todoPins.js`, mirroring `taskPins.js`) applied via `orderTodosByPin`, wired into `frontend/src/screens/Today.jsx`, 2026-07-13.
- [x] Task ICS export: the Calendar screen could already export events as ICS, but the Tasks screen only supported CSV/PDF, so business deadlines couldn't be pulled into an external calendar app the way events could. Added an "ICS" button to the Tasks toolbar that exports the currently filtered tasks' due dates as all-day VEVENT entries (`tasksToIcs`/`taskIcsFilename` in `frontend/src/ics.js`, wired into `frontend/src/screens/Tasks.jsx`, 2026-07-13).
- [x] Calendar ICS import: the Calendar screen could export events to ICS (and Tasks could import CSV) but had no way to bring an external calendar (Google/Outlook/Apple) into WorkManager, so switching tools meant retyping every event. Added an "ICS 가져오기" button that parses an uploaded `.ics` file's VEVENTs and bulk-creates matching events via the existing array-payload `onCreate` path (`parseIcs` in `frontend/src/ics.js`, wired into `frontend/src/screens/Calendar.jsx`, 2026-07-13).
- [x] Today screen text search: Tasks/Calendar/Trash all had a search box narrowing their lists by text, but the Today screen's 오늘 할 일/업무 기록 lists could only be narrowed by tag, with no way to jump straight to a specific todo or log entry by typing its title/content. Added a "할 일, 업무 기록 검색" input next to the existing tag filter, reusing the shared `.search` input style (`filterTodosByQuery`/`filterLogsByQuery` in `frontend/src/todaySearch.js`, wired into `frontend/src/screens/Today.jsx`, 2026-07-13).
- [x] Todo → task promotion: the Today screen's 오늘 할 일 quick-todo list had a duplicate button but no way to turn a quick todo into a full-fledged task once it turned out to need scheduling/priority tracking/dependencies, so growing a todo into a task meant retyping it in the Tasks screen and deleting the original by hand. Added an "업무로 전환" button on each todo row that creates a task carrying over the title/tags/priority/completion state and deletes the source todo (`buildTaskFromTodoPayload` in `frontend/src/todoDuplicate.js`, wired into `frontend/src/screens/Today.jsx`/`frontend/src/App.jsx`, 2026-07-13).
- [x] Task progress numeric entry: the Gantt row progress control was a drag-only `<input type="range">`, imprecise on touch screens and hard to set to an exact value. Added a small editable number input next to the slider sharing the same `draft`/`saveProgress` state (`frontend/src/screens/Tasks.jsx`, 2026-07-13).
- [x] Event related link: tasks already had a "관련 링크" field for reference URLs, but calendar events had no equivalent, so a meeting link or shared doc URL for an event had no structured place to live besides the free-text memo. Added a nullable `link_url` column on events (backend `EventPayload`/`CONFIG`/`db.py` migration, same http(s)-only validation as tasks), a "관련 링크" input on the event form, and a clickable badge on the mobile agenda list when an event has one (`backend/app/main.py`, `backend/app/db.py`, `frontend/src/screens/Calendar.jsx`, 2026-07-13).
- [x] Calendar public holiday markers: the Calendar screen had no notion of non-working days, so spotting a public holiday while scheduling meant checking an external calendar. Added a rule-based (not hardcoded-date-table) fixed-date Korean holiday lookup (`holidayNameForDate`/`holidayNameForKey` in `frontend/src/holidays.js`, with tests) covering 신정/삼일절/어린이날/현충일/광복절/개천절/한글날/성탄절, highlighted in both the month and week calendar grids (`frontend/src/screens/Calendar.jsx`, `frontend/src/styles.css`). Lunar-calendar holidays (설날, 추석) and 대체공휴일 substitute-holiday rules need real lunar-calendar data and are out of scope here — noted as a follow-up.
- [x] Lunar-calendar holiday markers (follow-up to the item above): 설날/추석/부처님오신날 shift on the solar calendar each year, so the fixed month/day holiday rule never caught them, silently under-reporting non-working days on the Calendar screen. Added a `LUNAR_HOLIDAY_DATES` lookup table of KASI-published solar-calendar dates for 2024-2028 (연휴 days included) to `frontend/src/holidays.js`, checked before the fixed-date table in `holidayNameForDate`; 대체공휴일 substitute-holiday rules remain out of scope (2026-07-14).
- [x] Substitute holiday (대체공휴일) rule (follow-up to the two items above): fixed-date and lunar holidays were both recognized, but a holiday landing on a weekend (어린이날) or a Sunday (삼일절/광복절/개천절/한글날/성탄절/부처님오신날/설날·추석 연휴) got no substitute weekday, so the Calendar screen under-reported real non-working days around those clashes. Added a rule-based `substituteHolidaysForYear` in `frontend/src/holidays.js` that builds the year's holiday set, detects the weekend/Sunday clash per holiday group, and assigns the next non-holiday day as `대체공휴일`, wired into `holidayNameForDate` (regression tests in `frontend/src/holidays.test.js`, 2026-07-14).
- [x] Performance report tag breakdown: the 성과 screen already tracked `duration_minutes`/`estimated_minutes` and tags on every item, but reports only ever showed flat totals, so there was no way to see which tag/project time actually went to. Added a `tag_breakdown` (per-tag tracked minutes + completed task count) to `GET /api/achievements`, a "태그별 소요 시간" bar-list section on the Performance screen, and a matching section in the Markdown report export (`backend/app/main.py`, `frontend/src/screens/Performance.jsx`, `frontend/src/performanceReport.js`, 2026-07-13).
- [x] Task related work logs: work logs already had an optional `task_id` link (settable from the Today screen), but the Tasks screen had no way to see which work logs were tied to a given task, so reviewing the effort actually logged against a task meant scanning the Today/work-log history by hand. Added a "기록" button on each Gantt row that opens a modal listing the linked work logs (date, content, duration) and their total tracked time (`taskWorkLogs`/`taskWorkLogsTotalMinutes` in `frontend/src/taskLogs.js`, wired into `frontend/src/screens/Tasks.jsx`/`frontend/src/App.jsx`, using the existing generic `GET /api/work_logs` endpoint, 2026-07-13).
- [x] Calendar mobile agenda range bug (reported via changelog request #20 — "웹에서는 안 보이는데 모바일에서는 구글캘린더 옛날 항목들이 보임"): the desktop calendar grid only ever shows events overlapping the currently visible month/week cells, but the mobile `.mobile-agenda` list (`Calendar.jsx`'s `sorted`) was built straight from the tag/query-filtered full event list with no date-range restriction, so every synced Google Calendar event within the backend's history window (including old past ones) showed up on mobile while desktop correctly stayed scoped to the current view. `sorted` (and therefore ICS/CSV export) is now derived from the same per-cell `eventsByDay` map the desktop grid already uses, deduplicated by event id (`frontend/src/screens/Calendar.jsx`, 2026-07-14).
- [x] Trash 7-day cleanup (reported via changelog request #21 — "휴지통 30일 지난 항목 삭제 말고 7일이상 지난 항목 삭제도 만들어줘"): `DELETE /api/trash` already accepted an arbitrary `older_than_days` (1–3650) but the UI only ever called it with a hard-coded 30, so there was no way to purge more aggressively. Added a "7일 지난 항목 정리" button next to the existing 30-day one in the Settings 휴지통 section, with a dynamic confirm dialog message and toast reflecting whichever retention period was chosen (`frontend/src/components/TrashSection.jsx`), plus a backend regression test covering a custom retention cutoff (`backend/tests/test_api.py::test_purge_trash_supports_custom_retention_days`, 2026-07-14).
- [x] Active session management: a Google-login session was long-lived, but there was no way to see which devices/browsers were logged in or to kick out a session that shouldn't be trusted (e.g. a shared computer). Added `GET /api/auth/sessions` (per-user list with created/last-seen/expiry and a `current` flag) and `DELETE /api/auth/sessions/{id}` (revokes any *other* session; revoking the current one is rejected — use logout instead), surfaced as an "활성 세션" section on the Settings screen (`backend/app/auth.py`, `backend/app/main.py`, `frontend/src/screens/Settings.jsx`, `frontend/src/api.js`), with regression tests covering listing/revocation scoping and cross-user isolation (`backend/tests/test_api.py`, 2026-07-14).
- [x] Bulk task postpone: the Tasks screen's bulk action bar (multi-select) could complete, tag, or delete several tasks at once, but shifting a whole batch of overdue/at-risk tasks forward still meant rescheduling each one individually (the single-row "미루기" button and Gantt drag both only affect one task). Added a "연기" control to the bulk action bar — enter a day count and it shifts every selected task's start/due dates forward by that many days, reusing the existing `postponeTaskDates` (`frontend/src/screens/Tasks.jsx`, `frontend/src/App.jsx`, 2026-07-14).
- [x] Todo related link: tasks and calendar events already had a "관련 링크" field for reference URLs, but the Today screen's quick-todo list had no equivalent, so a link relevant to a todo had no structured place besides retyping it into the title. Added a nullable `link_url` column on todos (backend `TodoPayload`/`CONFIG`/`db.py` migration, same http(s)-only validation as tasks/events), a link input on the add/edit todo forms, and a clickable "관련 링크" badge on todo rows, carried through recurrence spawn, duplication, and todo→task promotion (`backend/app/main.py`, `backend/app/db.py`, `frontend/src/screens/Today.jsx`, `frontend/src/todoDuplicate.js`, 2026-07-14).
- [x] Trash bulk restore: the Tasks screen's Gantt rows already had multi-select checkboxes and a bulk action bar (complete/tag/postpone/delete), but the 휴지통 (Trash) section only ever restored one item at a time, making recovery from an accidental multi-delete tedious. Added the same checkbox + bulk-action-bar pattern to `TrashSection.jsx` — selecting several trashed items surfaces a "선택 복원" button that restores them all in one action (`frontend/src/components/TrashSection.jsx`, `frontend/src/styles.css`, 2026-07-14).
- [x] Work log CSV export: Tasks/Calendar/Todos/Audit Log/Performance timeline all had a CSV export, but the Today screen's 업무 기록 (work log) list was the last one missing it, so pulling logged work into a spreadsheet meant retyping it by hand. Added a "CSV 내보내기" button next to the work-log timer that downloads the currently filtered logs (날짜/내용/소요 시간/연결 업무/태그) as CSV, matching the existing per-screen export pattern (`workLogsToCsv`/`workLogCsvFilename` in `frontend/src/csv.js`, wired into `frontend/src/screens/Today.jsx`, 2026-07-14).
- [x] Task recurrence badge: the task form already let a task repeat daily/weekly/monthly, but the Gantt row list gave no indication a task was part of a recurring series (unlike the existing checklist/estimate/link badges), so it was easy to forget which tasks would keep respawning. Added a "매일/매주/매월 반복" badge next to the other row badges when `task.recurrence_rule` is set (`frontend/src/screens/Tasks.jsx`, `frontend/src/styles.css`, 2026-07-14).
- [x] Task recurrence end date: recurring tasks (daily/weekly/monthly) had no way to stop — completing one always spawned the next occurrence forever, which is unworkable for a fixed-duration project or a task someone wants to eventually retire. Added a nullable `recurrence_end_date` column on tasks (backend `TaskPayload`/`CONFIG`/`db.py` migration), a "반복 종료일" date input on the task form shown only while a recurrence rule is selected, and a check in `spawn_recurring_task` that stops creating further occurrences once the next start/due date would fall after the end date (marking the series as spawned so it doesn't retry) (`backend/app/main.py`, `frontend/src/components/TaskForm.jsx`, `frontend/src/taskFormPayload.js`, regression test `test_recurring_task_stops_spawning_past_recurrence_end_date` in `backend/tests/test_api.py`, 2026-07-14).
- [x] Todo recurrence end date: tasks just gained a `recurrence_end_date` to stop endless recurrence spawning, but the Today screen's quick-todo recurrence (daily/weekly) had no equivalent, so a recurring todo also spawned forever with no way to retire it. Added the same nullable `recurrence_end_date` column on todos (backend `TodoPayload`/`CONFIG`/`db.py` migration), a "반복 종료일" date input on the Today screen's add/edit todo forms shown only while a recurrence rule is selected, and a matching check in `spawn_recurring_todo`, carried through duplication (`frontend/src/todoDuplicate.js`) (`backend/app/main.py`, `frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`, regression test `test_recurring_todo_stops_spawning_past_recurrence_end_date` in `backend/tests/test_api.py`, 2026-07-14).
- [x] Todo memo: tasks and calendar events already had a free-text memo/description field, but the Today screen's quick-todo list only ever had a title, so any extra detail (why it matters, where, who to contact) had no structured place besides cramming it into the title. Added a nullable `memo` column on todos (backend `TodoPayload`/`CONFIG`/`db.py` migration), a memo input on the add/edit todo forms, shown on todo rows, and carried through recurrence spawn (`spawn_recurring_todo`), duplication, and todo→task promotion (mapped to the task's `description` field) (`backend/app/main.py`, `backend/app/db.py`, `frontend/src/screens/Today.jsx`, `frontend/src/todoDuplicate.js`, regression test `test_todo_memo_is_persisted_and_carries_to_recurrence_spawn` in `backend/tests/test_api.py`, 2026-07-14).
- [x] Task search now matches checklist items: the Tasks screen search box only ever matched title/memo/tags, so a task whose checklist held the only mention of a search term (e.g. a sub-step name) was invisible to search even though the checklist feature had already shipped. `filterTasks` now also searches each checklist item's text (`frontend/src/taskFilters.js`, test added in `frontend/src/taskFilters.test.js`, 2026-07-14).
- [x] Todo monthly recurrence: tasks already supported daily/weekly/monthly recurrence (with month-end anchor rollover), but todos were stuck at daily/weekly only, so a monthly work item (e.g. "월간 보고서 제출") couldn't be modeled as a recurring todo. Widened `TodoPayload.recurrence_rule` to accept `"monthly"`, added `recurrence_anchor_day`/`recurrence_anchor_month_end` columns to todos (mirroring tasks) so `spawn_recurring_todo` correctly rolls a month-end todo to the next month's last day, and added a "매월" option to the Today screen's add/edit todo recurrence selects (`backend/app/main.py`, `backend/app/db.py`, `frontend/src/screens/Today.jsx`, regression tests `test_completing_monthly_todo_spawns_next_month_occurrence`/`test_month_end_monthly_todo_returns_to_month_end` in `backend/tests/test_api.py`, 2026-07-14).
- [x] Bulk task priority change: the Tasks screen's bulk action bar could already complete/tag/postpone/delete several selected tasks at once, but changing priority for a batch still meant opening each task's edit form individually. Added a priority select + "우선순위 변경" button to the bulk action bar that applies the chosen priority to every selected task in one action (`frontend/src/screens/Tasks.jsx`, `frontend/src/App.jsx`, 2026-07-14).
- [x] Calendar event color coding: every calendar event rendered in the same blue chip regardless of kind (meeting/personal/deadline), so visually telling event types apart at a glance was impossible. Added a nullable `color` column on events (backend `EventPayload`/`CONFIG`/`db.py` migration, validated against a fixed palette), a "색상" select on the event form, and colored month/week calendar chips (`backend/app/main.py`, `backend/app/db.py`, `frontend/src/eventColors.js` with tests, `frontend/src/screens/Calendar.jsx`, `frontend/src/eventDuplicate.js`, regression test `test_event_color_is_persisted_and_validated` in `backend/tests/test_api.py`, 2026-07-14).
- [x] Task Gantt color coding: Gantt bars were colored only by row index (`bar-${i%4}`), so there was no way to visually group tasks by project/type the way calendar events already could since the event color feature shipped. Added a nullable `color` column on tasks reusing the same validated palette (backend `TaskPayload`/`CONFIG`/`db.py` migration), a "색상" select on the task form (`frontend/src/components/TaskForm.jsx`), and Gantt bar/progress-fill tinting that falls back to the index-based color when unset (`frontend/src/screens/Tasks.jsx`, `frontend/src/taskFormPayload.js`, regression test `test_task_color_is_persisted_and_validated` in `backend/tests/test_api.py`, 2026-07-14).
- [x] Command palette search now matches task checklist items: the Tasks screen search box was recently widened to search checklist item text, but the Ctrl/⌘+K global search (`commandPalette.js`) still only checked title/memo/tags, so a word that only appeared inside a checklist item was findable in one search box but not the other. Added checklist item text to the task search body in `searchItems` (`frontend/src/commandPalette.js`, test added in `frontend/src/commandPalette.test.js`, 2026-07-14).
- [x] Task attachment links: tasks only ever supported a single `link_url` reference field, so a task needing multiple reference URLs (spec doc, ticket, design file) had nowhere structured to put the extras besides the free-text memo. Added a nullable `links` JSON column on tasks (backend `TaskPayload`/`CONFIG`/`db.py` migration, item shape `{id,url,label}`, http(s)-only validated, same pattern as the existing `checklist` column), an add/remove "첨부 링크" list editor in the task form, and an attachment-count badge on Gantt rows (`backend/app/main.py`, `backend/app/db.py`, `frontend/src/components/TaskForm.jsx`, `frontend/src/taskFormPayload.js`, `frontend/src/screens/Tasks.jsx`, regression test `test_task_links_are_persisted_and_invalid_urls_dropped` in `backend/tests/test_api.py`, 2026-07-14).
- [x] Gantt timeline window navigation: the Tasks screen's Gantt chart always showed a fixed 14-day window starting today, so a task scheduled outside that range rendered no bar at all and there was no way to look at past or future weeks. Added "◀ / 오늘 / ▶" navigation buttons in the Gantt header that shift the visible window by 14 days (`windowOffset` state), with bar positions and drag-reschedule math updated to compute against the shifted `viewStart` instead of the fixed today (`frontend/src/screens/Tasks.jsx`, browser-verified via Playwright screenshots, 2026-07-14).
- [x] Work log related link: tasks and todos both had a `link_url` field for attaching a reference URL, but work logs (오늘 한 일) had no equivalent, so linking a log entry to a PR/doc/ticket meant pasting the URL inline in the free-text content. Added a nullable `link_url` column on work logs (backend `WorkLogPayload`/`CONFIG`/`db.py` migration, http(s)-only validation, same pattern as tasks/todos), an input on the Today screen's work-log quick-add and inline-edit forms, and a clickable "관련 링크" chip on log rows (`backend/app/db.py`, `backend/app/main.py`, `frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`, regression test `test_work_log_link_url_is_persisted_and_validated` in `backend/tests/test_api.py`, 2026-07-14).
- [x] Event attachment links: tasks already had a `links` JSON column for attaching multiple reference URLs, but calendar events only ever supported a single `link_url`, so an event needing several reference links (agenda, doc, ticket) had nowhere structured to put the extras. Added a nullable `links` JSON column on events (backend `EventPayload`/`CONFIG`/`db.py` migration, same `{id,url,label}` shape and http(s)-only validation as tasks), an add/remove "첨부 링크" list editor in the event form (reusing `normalizedLinks` from `frontend/src/taskFormPayload.js`), and an attachment-count badge on the mobile agenda list (`backend/app/main.py`, `backend/app/db.py`, `frontend/src/screens/Calendar.jsx`, regression test `test_event_links_are_persisted_and_invalid_urls_dropped` in `backend/tests/test_api.py`, 2026-07-14).
- [x] Todo color coding: tasks and calendar events could already be color-coded for at-a-glance grouping, but the Today screen's quick-todo list had no equivalent, so distinguishing todo types visually meant relying on tags/priority alone. Added a nullable `color` column on todos reusing the same validated palette as tasks/events (backend `TodoPayload`/`CONFIG`/`db.py` migration), a "색상" select on the add/edit todo forms, and a colored left-border accent on todo rows (`backend/app/main.py`, `backend/app/db.py`, `frontend/src/screens/Today.jsx`, `frontend/src/todoDuplicate.js`, regression test `test_todo_color_is_persisted_and_validated` in `backend/tests/test_api.py`, 2026-07-14).
- [x] Work log attachment links: tasks and calendar events already had a multi-link `links` JSON column, but work logs (오늘 한 일) still only supported a single `link_url`, so a log entry needing several reference links (PR, doc, ticket) had nowhere structured to put the extras. Added a nullable `links` JSON column on work logs (backend `WorkLogPayload`/`CONFIG`/`db.py` migration, same `{id,url,label}` shape and http(s)-only validation as tasks/events), factored the previously copy-pasted per-table link validation into a shared `_clean_links` helper, and added an add/remove "첨부 링크" list editor plus an attachment-count badge to the Today screen's work log add/edit forms (`backend/app/main.py`, `backend/app/db.py`, `frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`, regression test `test_work_log_links_are_persisted_and_invalid_urls_dropped` in `backend/tests/test_api.py`, 2026-07-14).
- [x] Todo attachment links: tasks, calendar events, and work logs all had a multi-link `links` JSON column, but the Today screen's quick-todo list still only supported a single `link_url`, leaving todos as the one entity type without multi-attachment support. Added a nullable `links` JSON column on todos (backend `TodoPayload`/`CONFIG`/`db.py` migration, reusing the shared `_clean_links` validator), an add/remove "첨부 링크" list editor plus attachment-count badge on the Today screen's todo edit form, and carried `links` through todo duplication (`backend/app/main.py`, `backend/app/db.py`, `frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`, `frontend/src/todoDuplicate.js`, regression test `test_todo_links_are_persisted_and_invalid_urls_dropped` in `backend/tests/test_api.py`, 2026-07-14).
- [x] AI local-rules task priority inconsistency: the no-API-key fallback parser (`rule_parse`) recognized "긴급/중요/급함" to auto-set priority "high" for events and todos, but the task-creation branch only checked "긴급/급함", silently dropping "중요"-only task requests to normal priority. Aligned the task branch with the event/todo branches (`backend/app/ai.py`, regression test `test_task_extracts_priority_for_important_keyword` in `backend/tests/test_ai.py`, 2026-07-14).
- [x] Modal save keyboard shortcut: every entity form (task/event/todo/work-log create/edit) required a mouse click on the "저장" button to save — no keyboard way to submit, unlike commercial productivity tools (Linear, Notion, Gmail) that support Cmd/Ctrl+Enter to save-and-close. Added the shortcut once in the shared `Modal` component's existing keydown handler (submits the modal's inner `<form>`), covering every entity form in one change, plus a new row in the keyboard shortcuts help panel (`frontend/src/components/Modal.jsx`, `frontend/src/components/KeyboardShortcuts.jsx`, 2026-07-14).

- [x] Work log duplication: tasks, calendar events, and todos all had a one-click "복제" button, but work logs (오늘 한 일) were the last entity type without one, so recording a recurring log entry (e.g. daily standup, a repeated maintenance note tied to the same task) meant retyping content/tags/link every time. Added a duplicate button on each log row that copies content/tags/task link/duration/attachment links into a new log dated today (`frontend/src/logDuplicate.js`, wired into `frontend/src/screens/Today.jsx`/`frontend/src/App.jsx`, test in `frontend/src/logDuplicate.test.js`, 2026-07-14).
- [x] Work log color coding: tasks, calendar events, and todos could all be color-coded, but work logs (오늘 한 일) were the last entity type without it, so visually grouping log entries by kind meant relying on tags alone. Added a nullable `color` column on work logs reusing the same validated palette (backend `WorkLogPayload`/`CONFIG`/`db.py` migration), a "색상" select on the Today screen's work-log add/edit forms, a colored left-border accent on log rows, and color carried through on duplication (`backend/app/main.py`, `backend/app/db.py`, `frontend/src/screens/Today.jsx`, `frontend/src/logDuplicate.js`, regression test `test_work_log_color_is_persisted_and_validated` in `backend/tests/test_api.py`, 2026-07-14).

- [x] Todo time-of-day: 오늘 할 일(todo)은 날짜만 지정할 수 있어 하루 중 정확한 처리 시각을 남길 수 없었습니다. `todos` 테이블에 nullable `todo_time`(HH:MM) 컬럼을 추가하고(backend `TodoPayload`/`CONFIG`/`db.py` 마이그레이션), Today 화면의 할 일 추가/수정 폼에 시간 입력을 추가했으며, 같은 날짜 안에서는 시간순으로 정렬되고 목록에 시각 배지가 표시됩니다. 반복 할 일의 다음 회차 생성과 복제 시에도 시간이 함께 복사됩니다(`backend/app/main.py`, `frontend/src/screens/Today.jsx`, `frontend/src/todoDuplicate.js`, 2026-07-14).
- [x] Calendar screen task due-date visibility: the Calendar screen only ever rendered `events`, so a task's due date never showed up next to same-day meetings/events, forcing a separate check of the Tasks screen for schedule conflicts. Month and week grid cells now also render a small badge for each not-yet-done task due that day, clicking it opens that task's edit modal directly (`tasksByDay` in `frontend/src/screens/Calendar.jsx`, wired via `onOpenTask` in `frontend/src/App.jsx`, `.cal-task-due` in `frontend/src/styles.css`, 2026-07-14).
- [x] Work log pinning: tasks and todos both had a "고정" star button to keep an important item at the top of the list, but the Today screen's work log (오늘 한 일) list had no equivalent, so an important log entry could scroll out of view. Added the same pin button/pattern backed by a localStorage set (`frontend/src/logPins.js`, mirroring `todoPins.js`), applied via `orderLogsByPin`, wired into `frontend/src/screens/Today.jsx`, 2026-07-14.
- [x] Todo templates: tasks already had a "템플릿으로 저장"/템플릿 선택 flow (`taskTemplates.js`) for reusing common task setups, but the Today screen's quick-todo list had no equivalent, so a recurring daily/weekly todo (e.g. "아침 메일 확인") had to be retyped from scratch every time. Added the same localStorage template pattern for todos — title/priority/recurrence/tags — with a template select, "템플릿으로 저장", and "템플릿 삭제" control on the quick-add form (`frontend/src/todoTemplates.js`, tests in `frontend/src/todoTemplates.test.js`, wired into `frontend/src/screens/Today.jsx`, 2026-07-14).
- [x] Event pinning: tasks, todos, and work logs all had a localStorage-backed "pin to top" star toggle (`taskPins.js`/`todoPins.js`/`logPins.js`), but calendar events had no equivalent, so an important upcoming event could get buried in the chronological mobile agenda list. Added `frontend/src/eventPins.js` (same pattern, `wm-pinned-events` key) and wired a pin star button into the Calendar screen's mobile agenda rows, ordering pinned events first (`frontend/src/screens/Calendar.jsx`, tests in `frontend/src/eventPins.test.js`, 2026-07-14).

- [x] Todo time-of-day notification: `todo_time` on 오늘 할 일 was previously display/sort-only, unlike calendar events which already fire a lead-time browser notification before `start_at`. Added `todosDueForAlert` (`frontend/src/todoAlerts.js`, mirroring `eventAlerts.js`) and a matching per-minute check effect in `frontend/src/App.jsx` so a todo with a set time now triggers the same browser notification as events, using the existing configurable alert lead-time setting, 2026-07-14.
- [x] Today screen search covers memo/tags: the "할 일, 업무 기록 검색" box on the Today screen only matched a todo's title or a log's content, unlike the Tasks screen search which already matches title+description+tags+checklist text. Extended `filterTodosByQuery`/`filterLogsByQuery` (`frontend/src/todaySearch.js`) to also match a todo's memo/tags and a log's tags, 2026-07-14.
- [x] Work log templates: tasks, todos, and calendar events all had a localStorage-backed "템플릿으로 저장"/템플릿 선택 flow, but work logs (오늘 한 일) were the last entity type without one, so a recurring log entry (daily standup, a repeated maintenance note) had to be retyped every time. Added the same pattern for work logs — content/tags/color/duration — with a template select, "템플릿으로 저장", and "템플릿 삭제" control on the quick-add form (`frontend/src/logTemplates.js`, tests in `frontend/src/logTemplates.test.js`, wired into `frontend/src/screens/Today.jsx`, 2026-07-14).
- [x] AI quick-capture field coverage: manual forms had grown a rich set of optional fields (color, link_url, todo_time, priority) across tasks/events/todos/work_logs, but the natural-language AI parser (both the local rule-based fallback and the remote-provider system prompt) never populated any of them, so an AI-created item silently dropped color/link/time/priority even when the input text said "빨간색으로 표시" or included a URL. Added Korean color-word and URL extraction (`_color`/`_link` in `backend/app/ai.py`), explicit-time + 긴급/중요 priority detection for todos, and updated the remote-AI system prompt to teach these optional fields (`backend/app/ai.py`, tests added in `backend/tests/test_ai.py`, 2026-07-14).
- [x] AI quick-capture multi-link coverage: all four entities support a `links` array (multiple {url, label} entries) via the manual form, but the AI parser only ever extracted a single `link_url`, so an item created from text with two or more URLs (e.g. a doc link and a meeting link) silently lost every URL after the first. Added `_links` (`backend/app/ai.py`) to collect every URL found and populate `data.links` when 2+ are present, keeping `link_url` as the first for back-compat, and updated the remote-AI system prompt to request the same, tests added in `backend/tests/test_ai.py`, 2026-07-14.
- [x] Event priority: tasks and todos both had low/normal/high priority, but calendar events had no equivalent, so an important meeting couldn't be visually distinguished from a routine one. Added a nullable `priority` column on events (backend `EventPayload`/`CONFIG`/`db.py` migration, reusing the same enum as tasks/todos), a "우선순위" select on the event form, a left-border accent on high-priority calendar chips, and a "우선순위 높음" badge in the mobile agenda list; the local-rules and remote-AI natural-language parsers also now detect 긴급/중요/급함 wording for events (`backend/app/main.py`, `backend/app/db.py`, `backend/app/ai.py`, `frontend/src/screens/Calendar.jsx`, regression tests `test_event_priority_is_persisted_and_validated` in `backend/tests/test_api.py` and `test_event_extracts_priority` in `backend/tests/test_ai.py`, 2026-07-14).

- [x] Work log time-of-day: todos already had a `todo_time` field for a precise processing time, but work logs (오늘 한 일) only had a date, so recording when during the day something happened had no structured place. Added a nullable `log_time` column on work logs (backend `WorkLogPayload`/`CONFIG`/`db.py` migration, same `HH:MM` pattern validation as `todo_time`), a time input on the Today screen's work-log quick-add/edit forms, chronological sort within the same day (`/api/today`'s work_logs ordering), a time badge on log rows, and carried through on duplication (`backend/app/main.py`, `backend/app/db.py`, `frontend/src/screens/Today.jsx`, `frontend/src/logDuplicate.js`, regression test `test_work_log_time_is_persisted_validated_and_orders_today_endpoint` in `backend/tests/test_api.py`, 2026-07-14).

- [x] Today CSV import for quick todos: the Tasks screen already had a "CSV 가져오기" button, but the Today screen's 오늘 할 일 list only had CSV export, so bulk-adding several todos meant typing each one in by hand. Added a matching "CSV 가져오기" button that reads the same column format as the existing todo export (제목/완료 여부/우선순위/반복/날짜/태그), skips rows without a title, and bulk-creates the rest via the existing `POST /api/todos` (`parseTodosCsv` in `frontend/src/csv.js`, wired into `frontend/src/screens/Today.jsx`/`App.jsx`, 2026-07-14).
- [x] AI event recurrence parity: the calendar event form supported 매일/매주/매월 recurrence with an end date (`eventRecurrence.js`), but the AI assistant's local-rules parser and remote-AI prompt never recognized a repeat phrase, so an AI-created event was always one-off even when the input said e.g. "매주 정기 회의 8월 30일까지". Added `_event_recurrence`/`_strip_recurrence_phrase` to `backend/app/ai.py`'s event branch (covers both `rule_parse` and `rule_parse_multi`) plus a remote-AI prompt instruction, and taught `POST /api/ai/apply` to expand a recurring event into one row per occurrence server-side via `expand_recurring_event` in `backend/app/main.py` (mirrors the frontend's `expandRecurringEvent`, capped at 52 occurrences), 2026-07-14.
- [x] Today CSV import for work logs: the Today screen's 오늘 할 일 (todos) list had both CSV export and import, but 오늘 한 일 (work logs) only had export, so bulk-adding several log entries meant typing each one in by hand. Added a matching "CSV 가져오기" button reading the same column format as the existing work-log export (날짜/내용/소요 시간(분)/연결 업무/태그, skipping the unparsed 연결 업무 column and rows without content), bulk-creating the rest via the existing `POST /api/work_logs` (`parseWorkLogsCsv` in `frontend/src/csv.js`, wired into `frontend/src/screens/Today.jsx`/`App.jsx`), 2026-07-14.
- [x] Today bulk todo actions: the Tasks screen's Gantt rows had multi-select checkboxes with a bulk action bar (complete/tag/postpone/priority/delete), but the Today screen's 오늘 할 일 Todo list only supported acting on one item at a time (plus two fixed group actions: clear-completed and carry-over). Added the same checkbox + bulk-action-bar pattern to the Todo list — select several todos to bulk complete, add a tag, or delete them in one action (`frontend/src/screens/Today.jsx`, `frontend/src/App.jsx` onBulkCompleteTodo/onBulkDeleteTodo/onBulkAddTagTodo, `frontend/src/styles.css`), 2026-07-14.
- [x] Calendar bulk event delete: tasks and todos both had multi-select checkboxes with a bulk action bar, but the Calendar screen's event list (mobile agenda) only supported deleting one event at a time via its edit modal. Added the same checkbox + bulk-action-bar pattern to the agenda list — select several events and delete them in one action, reusing the existing delete-confirmation and undo-toast plumbing (`frontend/src/screens/Calendar.jsx`, `frontend/src/App.jsx` onBulkDelete, `frontend/src/styles.css`), 2026-07-14.
- [x] Calendar event overlap warning: creating or editing an event that overlapped another existing event's time range saved silently with no indication of the double-booking (Google Calendar sync conflicts were already surfaced via `resolve-conflict`, but local schedule overlaps never were). Added a non-blocking warning banner in the event form that lists any existing events whose time range overlaps the one being saved (`frontend/src/eventOverlap.js` with tests, wired into `EventForm` in `frontend/src/screens/Calendar.jsx`), 2026-07-14.
- [x] Trash permanent delete: 휴지통 could only restore items or bulk-purge by a 7/30-day age cutoff, with no way to immediately drop a single unwanted item for good. Added `DELETE /api/trash/{table}/{item_id}` (permanent delete of one already-soft-deleted, user-scoped row) plus a per-row "영구 삭제" button and a "선택 영구 삭제" bulk action alongside the existing bulk restore (`backend/app/main.py`, `frontend/src/api.js`, `frontend/src/components/TrashSection.jsx`, regression test `test_purge_single_trash_item_is_immediate_and_user_scoped` in `backend/tests/test_api.py`, 2026-07-14).
- [x] Calendar subscription feed: the Tasks/Calendar screens could only export a one-time ICS snapshot, so keeping an external calendar app (Google/Apple/Outlook) in sync meant re-exporting and re-uploading the file by hand every time something changed. Added a per-user calendar feed: `GET /api/settings/calendar-feed` (status), `POST /api/settings/calendar-feed/rotate` (issues a new token, invalidating any previous one), `DELETE /api/settings/calendar-feed` (disable), and an unauthenticated `GET /api/calendar-feed/{token}.ics` that renders that user's events and task due dates as ICS on every fetch — the token is SHA-256 hashed at rest the same way session tokens are (`backend/app/auth._hash`). Settings screen gained a "캘린더 구독 피드" card to issue/copy/rotate/disable the subscription URL (`backend/app/main.py`, `frontend/src/api.js`, `frontend/src/screens/Settings.jsx`, regression test `test_calendar_feed_rotate_serves_ics_and_old_token_is_invalidated` in `backend/tests/test_api.py`, 2026-07-14).
- [x] Performance screen daily activity trend chart: the 성과 screen only showed a tag-time breakdown bar and text stat tiles, with no way to see how activity was distributed across the selected date range (busy days vs. quiet days) at a glance. Added a bar-chart "일별 활동 추이" section that buckets the existing activity timeline by day (`dailyActivityTrend` in `frontend/src/performanceReport.js`, with tests), hidden for ranges over 62 days since daily granularity stops being useful at that scale, wired into `frontend/src/screens/Performance.jsx`, 2026-07-14.
- [x] New-item keyboard shortcut: creating a task or event always required a mouse click on the "새 업무"/"새 일정" button (Cmd/Ctrl+K only opens the AI quick-capture text parser, not a blank form). Added an "N" shortcut in the global keydown handler that opens the create form on the Tasks/Calendar screens (clicks the page header's primary action button) and focuses the todo quick-add input on the Today screen (`frontend/src/App.jsx`, keyboard shortcuts help row in `frontend/src/components/KeyboardShortcuts.jsx`, 2026-07-14).
- [x] Task estimate vs. actual overrun indicator: tasks already had an `estimated_minutes` field and the "관련 업무 기록" modal already summed the actual minutes logged against a task, but the two numbers were never compared, so a task running over its planned time had no visual signal. Added `taskEstimateOverrun` (`frontend/src/taskLogs.js`, tests in `frontend/src/taskLogs.test.js`) and wired it into the task-logs modal to show an "N분 초과"/"N분 여유" line, highlighted red when over estimate (`frontend/src/App.jsx`, `.task-estimate-overrun` in `frontend/src/styles.css`), 2026-07-14.
- [x] Task checklist item reordering: the task form's checklist editor could add/edit/delete/toggle items but only ever displayed them in the order they were added, so fixing the order of steps meant deleting and retyping them. Added up/down move buttons per checklist row backed by a pure `moveChecklistItem` (`frontend/src/taskFormPayload.js`, tests in `frontend/src/taskFormPayload.test.js`), wired into `frontend/src/components/TaskForm.jsx`, 2026-07-14.
- [x] Checklist-driven progress sync: task progress was a fully manual number disconnected from the checklist, so completing sub-items never moved it. Added a "체크리스트로 진행률 계산" button on the task edit form that fills the 진행률 field from the checklist's completion ratio (`checklistProgress` in `frontend/src/taskFormPayload.js`, tests in `frontend/src/taskFormPayload.test.js`, wired into `frontend/src/components/TaskForm.jsx`), 2026-07-14.
- [x] Recurring todo skip: a daily/weekly/monthly recurring todo could only advance to its next occurrence by being marked completed, so a day that should be skipped entirely (e.g. a rest-day workout todo) had to be force-completed or left overdue. Added `POST /api/todos/{id}/skip-recurrence` (advances `todo_date` via the existing `next_recurrence_date` helper without touching `completed`, rejecting non-recurring todos or ones past their `recurrence_end_date`) and a "다음 회차로 건너뛰기" button on each recurring todo row (`frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`, `frontend/src/api.js`, regression tests in `backend/tests/test_api.py`), 2026-07-14.
- [x] Add a work log directly from a task: the Gantt "기록 보기" (view logs) modal only ever showed a read-only list of a task's linked work logs, so logging new work against that task meant leaving the Tasks screen for Today and picking the task from a dropdown. Added a small inline add-log form (content + optional minutes) to the existing modal, reusing the same `api.createLog` payload shape as Today.jsx's quick-entry form (`taskLogDraft`/`addTaskLog` in `frontend/src/App.jsx`), 2026-07-15.
- [x] Calendar priority filter: the Tasks screen has a full 우선순위 filter dropdown (전체/높음/보통/낮음), and events already carry a `priority` field with visual badges/accents, but the Calendar screen had no way to filter the month/week grid or mobile agenda list down to just high-priority events. Added a matching priority filter next to the existing tag filter (`filterEventsByPriority` in `frontend/src/eventSearch.js`, wired into `frontend/src/screens/Calendar.jsx`), 2026-07-15.
- [x] Command palette todo memo search: Ctrl/⌘+K quick search already looked inside task descriptions/checklist text and event descriptions, but todo memo text (added 2026-07-14) was never included, so a todo findable only by its memo content couldn't be found by search at all. Added the todo `memo` field to the searched body text (`ITEM_SOURCES` in `frontend/src/commandPalette.js`, test in `frontend/src/commandPalette.test.js`), 2026-07-15.
- [x] Performance period goal tracking: the 성과 screen showed completed-task/tracked-minute totals for a period but had no target to compare them against, so knowing whether a period's output was "enough" required manual mental math. Added a localStorage-backed "기간 목표" form (completed-task target, tracked-minutes target) and progress bars comparing the current period's stats against the saved goal (`loadPerformanceGoal`/`savePerformanceGoal`/`goalProgress` in `frontend/src/performanceReport.js`, wired into `frontend/src/screens/Performance.jsx`, tests in `frontend/src/performanceReport.test.js`), 2026-07-15.
- [x] Work log "연결 업무" includes completed tasks: the Today screen's 한 일 (work log) add/edit forms only listed non-done tasks in the "연결 업무" dropdown, so a log entry couldn't be linked to a task that had already been marked complete. Added a "완료된 업무" optgroup appended below the active-task options so completed tasks remain selectable at the bottom of the list (`linkableDoneTasks` in `frontend/src/screens/Today.jsx`), 2026-07-16.
- [x] Changelog AI summary for aged entries (requested via changelog request #25 — "changelog가 너무 길어졌어. 일주일 지난 목록은 AI도움받아서 자동으로 종합해서 요약해서 기록,표시하게 해줘"): the merged public/admin changelog list (static seed history + DB-backed completed-request entries) only ever grew, showing every entry individually with no way to compress old history. Entries newer than 7 days still render individually; older entries are grouped by year-month and summarized via a new `smart_changelog_summary` (`backend/app/ai.py`, following the existing remote-AI-with-local-rules-fallback pattern) behind a public `POST /api/public/changelog-summary` endpoint, with the summary cached client-side in localStorage keyed by group content (`frontend/src/changelogSummary.js`, tests; wired into `frontend/src/screens/Changelog.jsx`), 2026-07-16.

- [x] Bulk task-action undo: Tasks screen's bulk-delete already had a "실행 취소" undo toast (via trash restore), but bulk-complete and bulk-priority-change had no way to recover from an accidental multi-select action since nothing was deleted/trashed. `onBulkComplete`/`onBulkPriority` in `frontend/src/App.jsx` now snapshot the prior status/progress/priority of the selected tasks and pass an `undo` callback into the existing generic `mutate(...)` undo plumbing (already used by delete paths; `Toast.jsx` already renders the "실행 취소" button), 2026-07-16.
- [x] Google Calendar last-sync visibility: the backend already tracked a per-calendar sync timestamp in `google_sync_state.updated_at` (updated on every push/pull), but nothing surfaced it — after a sync toast disappeared there was no way to tell whether/when the calendar last synced successfully, even though sync is manual, not scheduled. `google_calendar.last_sync_at(user_id, calendar_id)` now reads that column and `GET /api/google/status` includes it; the Settings screen's Google 캘린더 card shows "마지막 동기화: …" (or "아직 동기화한 적이 없습니다") next to the sync button (`backend/app/google_calendar.py`, `backend/app/main.py`, `frontend/src/screens/Settings.jsx`, regression test `test_google_status_reports_last_sync_at` in `backend/tests/test_api.py`), 2026-07-16.
- [x] Work log timer start-time fix: the Today screen's work-log timer (`frontend/src/workLogTimer.js`) filled in `duration_minutes` when stopped, but never populated the "시각" (log_time) input, so a log created via the timer silently lost the actual clock time work started at, unlike a manually typed log. Added `startTimeString(startedAt)` and wired it into `stopTimer` in `frontend/src/screens/Today.jsx` so stopping the timer also fills the time field, tests in `frontend/src/workLogTimer.test.js`, 2026-07-16.
- [x] Bulk-action undo coverage completed: the prior bulk-undo entry above only covered Tasks' `onBulkComplete`/`onBulkPriority` — Tasks' `onBulkPostpone` and Today's `onBulkCompleteTodo`/`onBulkAddTagTodo`/`onCarryOverTodos` still had no recovery path for an accidental multi-select action. All four now snapshot prior field values (task start/due dates; todo completed/tags/todo_date) and pass an `undo` callback into the same `mutate(...)` plumbing (`frontend/src/App.jsx`), 2026-07-16.
- [x] Calendar CSV import: Tasks/Todos/Work logs already had CSV import to round-trip a bulk-edited export back into the app, but the Calendar screen only had CSV export and ICS import — a spreadsheet-edited event list had no way back in except manual re-entry. Added a "CSV 가져오기" button next to the existing ICS import that reads the same column format as the existing CSV export (제목/시작/종료/종일 여부/장소/태그/메모) and bulk-creates events via the existing `onCreate` batch path (`parseEventsCsv` in `frontend/src/csv.js`, wired into `Calendar.jsx`, 2026-07-16).
- [x] Work log billable flag: WorkManager tracked logged time (`duration_minutes`) but had no way to mark which of it was client-billable, so pulling billable hours for an invoice meant re-reading every log's tags/memo by hand — a gap for a commercial work-tracking product. Added a nullable `billable` column on work logs (backend `WorkLogPayload`/`CONFIG`/`db.py` migration), a "청구 가능" checkbox on the Today screen's log add/edit forms plus a row badge, a `billable_minutes` total in `GET /api/achievements`, and a matching stat tile/Markdown line/CSV column on the Performance screen (`backend/app/main.py`, `frontend/src/screens/Today.jsx`, `frontend/src/screens/Performance.jsx`, `frontend/src/performanceReport.js`, `frontend/src/csv.js`, regression test `test_work_log_billable_is_persisted_and_included_in_achievements_summary` in `backend/tests/test_api.py`, 2026-07-16).
- [x] Billing hourly rate: the billable-minutes total added above still had to be converted into an invoice amount by hand. Added a per-user `billing_hourly_rate` setting (`WorkflowSettingsPayload`, stored in `app_settings`, `GET`/`PUT /api/settings/workflow`), a "청구 시급" input on the Settings screen, and a computed `billable_amount` in `GET /api/achievements` shown as a "청구 예상 금액" stat tile on the Performance screen when a rate is set (`backend/app/main.py`, `frontend/src/screens/Settings.jsx`, `frontend/src/screens/Performance.jsx`, regression test `test_billing_hourly_rate_setting_computes_billable_amount` in `backend/tests/test_api.py`, 2026-07-16).
- [x] Work log CSV billable amount column: the CSV export added earlier only marked rows "청구 가능" (Y/N) without the actual invoice amount, so exporting to a spreadsheet still required multiplying duration by the hourly rate by hand. `workLogsToCsv` now takes an optional hourly rate and adds a "청구 금액(원)" column, computed per billable row from `duration_minutes`; the Today screen loads the saved `billing_hourly_rate` via `GET /api/settings/workflow` on mount and passes it into the export (`frontend/src/csv.js`, `frontend/src/screens/Today.jsx`, tests in `frontend/src/csv.test.js`, 2026-07-17).
- [x] Task form inline validation: the task create/edit form relied purely on the browser's native HTML5 required-tooltip and a single form-level banner for validation errors, with no red-border/inline error text pinpointing which field was wrong. Added `validateTaskForm` (`frontend/src/formValidation.js`, tests) returning per-field error messages for a blank title and a due-date-before-start-date, wired into `frontend/src/components/TaskForm.jsx` to add an `invalid` class + `aria-invalid` + inline `<small class="field-error">` under the offending input, 2026-07-17.
- [x] Mobile month calendar weekday sizing (reported via changelog request #26 — "월간 캘린더 글씨 크기를 줄여서 월~금이 보이게, 요일/날짜 칸 너비 안 맞음"): the mobile month grid crammed all 7 weekday columns (including 토/일) into a horizontally-scrolling 720px-wide grid, so event text was tiny and users had to scroll sideways just to see weekdays; the header row and date-cell grid also risked drifting out of alignment. Scoped a mobile-only rule to `.cal-view-month` that hides the 일/토 columns (`nth-child`) and switches both `.weekdays` and `.calendar-grid` to a matching `repeat(5,1fr)` track so the header and cells stay pixel-aligned by construction, plus smaller event/day-number font sizes so Mon–Fri fits one screen without horizontal scroll (`frontend/src/styles.css`), 2026-07-17.
- [x] Event comment thread: tasks already had a timestamped comment thread (add/edit/delete, distinct from the single overwritten memo field), but events/todos/work logs had no equivalent — a calendar meeting had no way to log running notes ("고객사에 확인 요청, 금요일 회신 예정") without losing history. Added the same pattern to events first: `event_comments` table (`backend/app/db.py`), `GET/POST /api/events/{id}/comments` and `PATCH/DELETE /api/events/{id}/comments/{comment_id}` (`backend/app/main.py`), and a "댓글" panel in the event edit form ported from `TaskForm.jsx` (`frontend/src/screens/Calendar.jsx`, `frontend/src/api.js`), regression test `test_event_comments_are_user_scoped_and_persisted` in `backend/tests/test_api.py`, 2026-07-17. Todos/work logs remain a follow-up candidate.
- [x] Todo comment thread (follow-up to the event comment thread above): todos still only had a single overwritten `memo` field, so a Today Todo had no way to log running notes with history the way tasks and events already did. Added the same pattern for todos: `todo_comments` table (`backend/app/db.py`), `GET/POST /api/todos/{id}/comments` and `PATCH/DELETE /api/todos/{id}/comments/{comment_id}` (`backend/app/main.py`), and a self-contained `TodoComments` panel rendered inside the Today screen's inline todo edit row (`frontend/src/screens/Today.jsx`, `frontend/src/api.js`), regression test `test_todo_comments_are_user_scoped_and_persisted` in `backend/tests/test_api.py`, 2026-07-17.
- [x] Work log comment thread (final follow-up to the comment-thread pattern above): tasks/events/todos all had a timestamped comment thread, but work logs — the one entity type left — still only had a single `content` field, with no way to add running notes after the fact without editing the original entry. Added `work_log_comments` table (`backend/app/db.py`), `GET/POST /api/work_logs/{id}/comments` and `PATCH/DELETE /api/work_logs/{id}/comments/{comment_id}` (`backend/app/main.py`), and a `WorkLogComments` panel in the Today screen's inline work-log edit row (`frontend/src/screens/Today.jsx`, `frontend/src/api.js`), regression test `test_work_log_comments_are_user_scoped_and_persisted` in `backend/tests/test_api.py`, 2026-07-17.
- [x] Audit log comment/attachment entity labels: the 감사 로그 screen's `actionLabels`/`entityLabels` dictionaries were never updated as comment threads and attachments were added to tasks/events/todos/work logs, so those audit rows (and the "대상" filter dropdown) showed raw identifiers like `task_comment`, `event_attachment`, `session`, `settings` instead of Korean labels. Added the missing entity labels (comment/attachment ×4, session, settings, feature_requests, tags, backup) and action labels (`import`, `rename`) to `frontend/src/screens/AuditLog.jsx`, 2026-07-17.
- [x] Task "선행 업무 대기" (blocked) status filter: the Tasks screen already showed a dependency-blocked-tasks summary banner and a per-row blocker badge, but there was no way to filter the list down to just those blocked tasks — finding them meant scanning the whole list for the badge. Added a `blocked` option to the existing status filter, reusing `taskBlockingDependencies` (`frontend/src/taskFilters.js`, wired into `frontend/src/screens/Tasks.jsx`, test added in `frontend/src/taskFilters.test.js`, 2026-07-17).
- [x] Work log bulk actions: the Tasks Gantt list, the Today screen's Todo list, and the Calendar agenda list all had a multi-select checkbox + bulk action bar (delete/tag/etc.), but the Today screen's "오늘 한 일" work log list was the last of the four list views without one, so batch cleanup or tagging of several log entries still meant editing them one at a time. Added the same row-checkbox + bulk action bar pattern (bulk delete, bulk tag add) to the work log list, mirroring the existing todo bulk-action implementation (`frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`, 2026-07-17).
- [x] Calendar year view: the Calendar screen only ever supported month/week views, so seeing an overview of the whole year (or jumping to a distant month) meant paging one month at a time — a standard view in commercial scheduling products that WorkManager was missing. Added a "년" view showing all 12 months as mini calendars with a dot indicator on days that have events, clicking a month jumps straight into its month view (`frontend/src/calendarYear.js` with tests, wired into `frontend/src/screens/Calendar.jsx`, `frontend/src/styles.css`, 2026-07-17).
- [x] Todo saved filter presets: the Tasks screen let users save/apply/delete named filter presets (query + tags + priority + status), but the Today screen's Todo list only had live filter controls with no way to save a recurring combination (e.g. "high priority + urgent tag"), an inconsistency between two otherwise-parallel screens. Added `frontend/src/todoFilterPresets.js` (mirroring `taskFilterPresets.js`) with save/apply/delete wired into the Today screen's filter bar (`frontend/src/screens/Today.jsx`, `frontend/src/styles.css`, tests in `frontend/src/todoFilterPresets.test.js`, 2026-07-17).
- [x] Biweekly recurrence: tasks, todos, and calendar events all supported daily/weekly/monthly recurrence but had no 14-day option, so work that recurs every other week (e.g. 격주 정기 회의) had to be recreated by hand each cycle. Added a `biweekly` recurrence rule across `TaskPayload`/`TodoPayload` validation, `next_recurrence_date`/`_advance_event_datetime` (backend), `expandRecurringEvent` (frontend), the AI local-rules parser and remote-AI prompt (`backend/app/ai.py`), and every recurrence dropdown/CSV mapping (`frontend/src/components/TaskForm.jsx`, `frontend/src/screens/Today.jsx`, `frontend/src/screens/Calendar.jsx`, `frontend/src/screens/Tasks.jsx`, `frontend/src/csv.js`), regression tests in `backend/tests/test_api.py`/`test_ai.py` and `frontend/src/eventRecurrence.test.js`, 2026-07-17.
- [x] Attachment count badge: tasks/events/todos/work logs all supported file attachments, but list views gave no indication an item had any (unlike comments, which already showed a "댓글 n개" badge), so checking for an attached file meant opening every item. The generic per-table list endpoint now also joins each entity's attachment table to compute `attachment_count` (`backend/app/main.py`, alongside the existing `comment_count` join), surfaced as a "첨부파일 n개" badge on Gantt rows (Tasks), the mobile agenda list (Calendar), and todo/work-log rows (Today), regression test `test_task_todo_work_log_event_list_include_attachment_count` in `backend/tests/test_api.py`, 2026-07-17.
- [x] Task templates now save/restore the checklist: tasks/todos/events/work logs all had a localStorage template save/apply flow, but task templates (`buildTaskTemplate`/`applyTaskTemplate` in `frontend/src/taskTemplates.js`) only ever carried title/priority/recurrence/tags/duration — the checklist state open in `TaskForm.jsx` was silently dropped, so a repeatable task setup like "설계 검토 → 코드 리뷰 → QA → 배포" lost its sub-steps every time it was reused from a template. `buildTaskTemplate` now also captures the checklist (text only, `done` reset to false, capped at 50 items) and `applyTaskTemplate` regenerates fresh ids on apply; wired into `saveAsTemplate`/`applyTemplate` in `frontend/src/components/TaskForm.jsx`, tests added in `frontend/src/taskTemplates.test.js`, 2026-07-17.

- [x] Recurring event series edit ("this and following occurrences"): recurring events (daily/weekly/biweekly/monthly) were expanded into fully independent rows at creation time with no link between them, so retitling a recurring meeting or moving its location meant editing every future occurrence one at a time. Added a nullable `recurrence_group_id` column stamped on all rows created from one recurrence definition (`backend/app/db.py`, `frontend/src/eventRecurrence.js`), a `PATCH /api/events/series/{group_id}?from_start_at=...` endpoint that applies a title/description/location/tags/link/color/priority diff to that occurrence and every future one sharing the group id (dates are never touched by the series patch), and an "이 일정과 이후 반복 일정에 모두 적용" checkbox in the event edit form (`frontend/src/screens/Calendar.jsx`, `frontend/src/App.jsx`), regression test `test_event_series_update_applies_to_future_occurrences_only` in `backend/tests/test_api.py`, 2026-07-17.
- [x] Task "오늘 마감" (due today) status filter: the Tasks screen status filter already had "지연 업무" (overdue) and "이번 주 마감" (due this week), but no way to isolate just today's deadlines for daily planning — finding them meant scanning the whole "이번 주 마감" list for today's date. Added a `due_today` option to the existing status filter, between 지연 업무 and 이번 주 마감 in both `filterTasks` (`frontend/src/taskFilters.js`) and the Tasks screen's 상태 select (`frontend/src/screens/Tasks.jsx`), test added in `frontend/src/taskFilters.test.js`, 2026-07-17.

- [x] Calendar saved filter presets: Tasks and Today already let users save/apply/delete named filter presets (query + tags + priority), but Calendar had the same search/tag/priority filter controls with no way to save a recurring combination, an inconsistency between three otherwise-parallel screens. Added `frontend/src/eventFilterPresets.js` (mirroring `todoFilterPresets.js`) with save/apply/delete wired into the Calendar screen's filter bar (`frontend/src/screens/Calendar.jsx`, tests in `frontend/src/eventFilterPresets.test.js`, 2026-07-17).
- [x] Today work log saved filter presets: the Tasks screen, the Today screen's todo list, and the Calendar screen all had saved filter presets, but the Today screen's 업무 기록 (오늘 한 일) list — with the same search/tag/청구 가능 filter controls — was the last of the four list views without one, so a frequently used combination (e.g. 청구 가능 + a specific tag) had to be re-set every time. Added `frontend/src/logFilterPresets.js` (mirroring `todoFilterPresets.js`, storing billable instead of priority) with save/apply/delete wired into the Today screen's work log filter bar (`frontend/src/screens/Today.jsx`, tests in `frontend/src/logFilterPresets.test.js`, 2026-07-17).
- [x] Today todo/work-log sort controls: the Tasks screen already had a persisted 정렬 (sort) dropdown (일정순/우선순위순/진행률순/제목순), but the Today screen's 할 일 and 업무 기록 lists had no sort control at all — items only ever appeared pin-then-priority ordered with no way to reorder by title/time/duration. Added a sort `<select>` to each list, backed by new comparator/persistence helpers in `frontend/src/todoPins.js` (priority/title/time) and `frontend/src/logPins.js` (none/time/duration/content), wired into `frontend/src/screens/Today.jsx`'s `shownTodos`/`shownLogs`, tests added in `todoPins.test.js`/`logPins.test.js`, 2026-07-17.
- [x] Calendar year view task-due indicator: the year view's per-day dot only lit up for calendar events (`yearEventCounts`), so a month with only task deadlines and no calendar events showed no indicator at all — inconsistent with month/week view, which already lists task due dates alongside events via `tasksByDay`. Added `yearTaskCounts` (reusing the existing `tasksDueByDay` map) and a distinct amber `.has-tasks` underline (stacked with the existing blue `.has-events` underline when both apply), plus a "업무 마감 N건" tooltip (`frontend/src/screens/Calendar.jsx`, `frontend/src/styles.css`), 2026-07-17.

- [x] Calendar bulk tag add: Tasks and Today's todo/work-log bulk-action bars could already add a tag to every selected item at once, but the Calendar screen's event bulk-action bar only offered delete, even though events already carry tags and support filtering by them. Added a matching "태그 추가" form to the event bulk-action bar (`frontend/src/screens/Calendar.jsx` `bulkAddTagEvents`, `frontend/src/App.jsx` `onBulkAddTag`, reusing the existing `withAddedTag` helper), 2026-07-17.
- [x] Calendar event search now matches tags: Tasks/Today's todo and work-log search already matched title+description+tags, but the Calendar event search box (`filterEventsByQuery`) only matched title/location/description, so an event findable only by a tag couldn't be found by search even though events fully support tags elsewhere (tag chips, tag filter, tag bulk-add). Added `event.tags` to the searched text and updated the search placeholder to mention 태그 (`frontend/src/eventSearch.js`, test in `frontend/src/eventSearch.test.js`), 2026-07-17.
- [x] Calendar/Today view-history buttons: the Tasks screen's Gantt rows already had a "이력" button that jumps to the 감사 로그 (audit log) screen pre-filtered to that task's own change history, but Calendar events and Today's todos/work logs had no equivalent — finding a specific item's edit history meant scrolling the full audit log by hand. Added a matching "이력" button to the event edit form and to each todo/work-log row, reusing the same `{entity, query}` audit-focus navigation as Tasks (`frontend/src/screens/Calendar.jsx` `EventForm`, `frontend/src/screens/Today.jsx`, wired via `onViewHistory`/`onViewHistoryTodo`/`onViewHistoryLog` in `frontend/src/App.jsx`), 2026-07-17.
- [x] Keyboard screen-navigation shortcuts: shortcuts existed for creating items (N/Shift+N) and quick input (Ctrl/⌘+K), but there was no keyboard way to switch between sidebar screens (오늘/업무/일정/성과/AI/감사/변경/설정) — every screen switch required a mouse click on the sidebar. Added number-key shortcuts 1–8 mapped to sidebar order, and listed them in the shortcuts help panel (`frontend/src/App.jsx`, `frontend/src/components/KeyboardShortcuts.jsx`), 2026-07-17.
- [x] Todo checklist: tasks already had a checklist for sub-steps, but todos — the entity closest to tasks (both have priority/recurrence/tags/link_url/color/templates) — had no equivalent, so a multi-step todo (e.g. 출장 준비, 장보기) couldn't track its sub-items. Added a nullable `checklist` column to `todos` (`backend/app/db.py`), the same `TodoPayload` validator as tasks (factored into a shared `_clean_checklist` helper in `backend/app/main.py`), and an add/toggle/remove checklist editor + completion-count badge in the Today screen's inline todo edit row (`frontend/src/screens/Today.jsx`); checklist items carry through duplicate (reset to undone) and promote-to-task (`frontend/src/todoDuplicate.js`), regression test `test_todo_checklist_is_persisted_and_sanitized` in `backend/tests/test_api.py`, 2026-07-17.
- [x] Calendar event checklist: tasks and todos both had a checklist for sub-steps, but the backend already carried an `events.checklist` column and the event form even had unused state/handlers for it (`checklistText`/`toggleChecklistItem`/etc.) that were never rendered or included in the save payload, so calendar events silently lacked the feature outright. Added the same add/toggle/reorder/remove checklist editor used by tasks to the event edit form and wired `checklist` into the save payload (`frontend/src/screens/Calendar.jsx`), regression test `test_event_checklist_is_persisted_and_sanitized` in `backend/tests/test_api.py`, 2026-07-18.
- [x] Recurring event series delete: the recurring-event series edit feature let a user apply a title/location/etc. change to "this and following occurrences" at once, but deleting still only ever removed one occurrence — cleaning up a whole recurring series meant deleting every future row by hand. Added `DELETE /api/events/series/{group_id}?from_start_at=...` (mirrors the existing series-patch endpoint, soft-deletes + Google-tombstones each future occurrence sharing the `recurrence_group_id`, factored via a shared `_soft_delete_event` helper reused by the single-item delete route) and a matching "이 일정과 이후 반복 일정에 모두 적용" checkbox in the event delete confirmation dialog (`backend/app/main.py`, `frontend/src/screens/Calendar.jsx`, `frontend/src/App.jsx`, `frontend/src/api.js`), regression test `test_event_series_delete_removes_only_future_occurrences` in `backend/tests/test_api.py`, 2026-07-18.
- [x] Todo checklist item reordering: task and calendar-event checklists both got up/down move buttons (`moveChecklistItem`), but the Today screen's todo checklist editor never adopted the same reusable helper, so fixing a todo's checklist order meant deleting and re-adding items. Wired `moveChecklistItem` (from `frontend/src/taskFormPayload.js`) into the todo checklist editor with the same ▲/▼ buttons used by tasks/events (`frontend/src/screens/Today.jsx`), 2026-07-18.
- [x] Calendar sync-conflict nav badge: the Tasks and Today sidebar/bottom-nav items already showed count badges for things needing attention (overdue/pending-approval, incomplete todos), but Calendar had no nav badge at all even though unresolved Google Calendar sync conflicts (`event.sync_state === 'conflict'`, surfaced per-event and via `ConflictPanel`) are exactly that kind of attention-needed signal — a conflict could sit unnoticed until the user happened to open Calendar. Added a `calendar` entry to `navBadges` counting conflicted events (`frontend/src/App.jsx`), reusing `AppShell`'s existing generic per-item badge rendering with no changes needed there, 2026-07-18.
- [x] Work log checklist: tasks/events/todos all had a checklist for sub-steps, but work logs (업무 기록) — which often record multi-step completed work — had no equivalent field at all. Added a nullable `checklist` column to `work_logs` (`backend/app/db.py`), a `WorkLogPayload.checklist` validator reusing the shared `_clean_checklist` helper and added to the `work_logs` CONFIG allowed-fields set (`backend/app/main.py`), an add/toggle/reorder/remove checklist editor + completion-count badge in the Today screen's inline log edit row (`frontend/src/screens/Today.jsx`); checklist items carry through duplicate (reset to undone) and promote-to-task (`frontend/src/logDuplicate.js`), regression test `test_work_log_checklist_is_persisted_and_sanitized` in `backend/tests/test_api.py`, 2026-07-18.

- [x] Todo CSV monthly recurrence round-trip (2026-07-18): todos support daily/weekly/biweekly/monthly recurrence end-to-end (Today screen selects, backend `next_recurrence_date`), but `frontend/src/csv.js`'s `todoRecurrenceLabels`/`todoRecurrenceLabelToValue` maps were never updated when `monthly` was added, so exporting a monthly-recurring todo left the "반복" CSV column blank and re-importing it silently dropped the recurrence back to `null`. Added the missing `매월`/`monthly` mapping in both directions, round-trip test added in `frontend/src/csv.test.js`.
- [x] CSV checklist summary column (2026-07-18): tasks/events/todos/work logs all support a checklist, shown as an "n/m" progress badge in every list view, but none of the four CSV exports surfaced it at all — there was no way to see which exported rows still had open checklist items without opening each one. Added a shared `checklistSummary` helper and a "체크리스트" column (done/total, e.g. "2/3") to `tasksToCsv`/`eventsToCsv`/`todosToCsv`/`workLogsToCsv` (`frontend/src/csv.js`); import intentionally ignores the column since checklist items aren't flat CSV-representable. Tests added in `frontend/src/csv.test.js`.
- [x] Printable PDF report checklist summary column (2026-07-18): the CSV exports above just got a "체크리스트" completion column, but the sibling printable PDF reports for all four entities (`tasksToPrintableReport`, `eventsToPrintableReport`, `todosToPrintableReport`, `workLogsToPrintableReport`) still had no checklist column at all, so a printed/PDF report gave no visibility into open checklist items. Added the same "체크리스트" (n/m) column to all four report tables (`frontend/src/taskReport.js`, `frontend/src/eventReport.js`, `frontend/src/todoReport.js`, `frontend/src/workLogReport.js`), tests added in each report's test file.
- [x] Calendar bulk duplicate (2026-07-18): tasks/todos/work logs all had a bulk "복제" button in their multi-select toolbar, but the Calendar screen's selection toolbar only offered add-tag/postpone/priority/delete — duplicating several selected events still meant clicking each row's individual 복제 button one at a time. Added a bulk "복제" button reusing the existing `buildEventDuplicatePayload` (`frontend/src/screens/Calendar.jsx`, `frontend/src/App.jsx`).
- [x] Todo per-row postpone (2026-07-18): the Tasks screen had a per-row "미루기" button on overdue Gantt rows, and Today's todo list had a bulk (multi-select) postpone, but there was no way to push a single todo's date forward without either selecting it first or editing the form. Added a "하루 미루기" button to each todo row's action list, reusing the existing `postponeTodoDate` helper, with undo support (`frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`).
- [x] Todo estimated-minutes tracking (2026-07-18): tasks already had `estimated_minutes` with an overrun badge against logged work-log time, but todos — the entity closest to tasks in schema/UI — had no way to record an expected duration at all. Added a nullable `estimated_minutes` column to `todos` (`backend/app/db.py`), the same `ge=0`/nullable `TodoPayload` validator pattern as tasks and added to the `todos` CONFIG allowed-fields set (`backend/app/main.py`), an "예상 소요 시간(분)" input in the Today screen's todo add/edit forms plus a row badge (`frontend/src/screens/Today.jsx`); the value carries through duplicate, promote-to-task, and recurrence spawn (`frontend/src/todoDuplicate.js`, `backend/app/main.py` `spawn_recurring_todo`), regression test `test_todo_estimated_minutes_is_persisted_validated_and_carries_to_recurrence_spawn` in `backend/tests/test_api.py`.

- [x] Today work log bulk priority change (2026-07-18): tasks/events/todos all had a bulk-action-bar "우선순위 변경" form for changing several selected items' priority at once, but the Today screen's 업무 기록 (work log) bulk-action bar only had 태그 추가/연기/복제/삭제 even though work logs carry the same `priority` field with a single-item editor. Added an `onBulkPriorityLog` handler mirroring `onBulkPriorityTodo`'s pattern (`frontend/src/App.jsx`) and wired a matching "우선순위 변경" form with undo support into the Today screen's work log bulk-action bar (`frontend/src/screens/Today.jsx`).
- [x] Attachment drag-and-drop (2026-07-18): tasks, calendar events, todos, and work logs all support file attachments, but every one of the four upload widgets only accepted a file via the native "파일 선택" button — there was no way to drag a file from the desktop straight onto the drop area. Added a shared `dropZoneHandlers` helper (`frontend/src/fileDrop.js`) wiring `onDragOver`/`onDrop` to the existing upload handlers, applied to all four attachment widgets with a dashed `.file-dropzone` style and a "또는 파일을 끌어다 놓으세요" hint (`frontend/src/components/TaskForm.jsx`, `frontend/src/screens/Calendar.jsx`, `frontend/src/screens/Today.jsx`, `frontend/src/styles.css`).
- [x] Calendar mobile agenda per-row postpone (2026-07-18): the Tasks screen's overdue Gantt rows and the Today screen's todo/work-log rows all had a one-click "하루 미루기" quick-action button, but the Calendar screen's mobile agenda list row only had pin/duplicate — postponing a single event required opening its edit form. Added a "하루 미루기" button to each agenda row reusing the existing `postponeEvent` handler already wired into the event edit modal (`frontend/src/screens/Calendar.jsx`).
- [x] Calendar event estimated-minutes tracking (2026-07-18): tasks and todos both had a nullable `estimated_minutes` field for planning effort, but calendar events — which also occupy scheduled time — had no equivalent, so the Performance capacity view undercounted planned workload for anyone who schedules effort via calendar events. Added a nullable `estimated_minutes` column to `events` (`backend/app/db.py`), the same `ge=0`/nullable `EventPayload` validator pattern as tasks/todos and added to the `events` CONFIG allowed-fields set (`backend/app/main.py`), an "예상 소요 시간(분)" input in the Calendar event form plus a row badge (`frontend/src/screens/Calendar.jsx`); the value carries through duplicate (`frontend/src/eventDuplicate.js`), regression test `test_event_estimated_minutes_is_persisted_and_validated` in `backend/tests/test_api.py`.

- [x] Task ICS import (2026-07-18): the Tasks screen could already export tasks to ICS and the Calendar screen could both import and export ICS, but Tasks had no import path, so external calendar deadlines (including all-day entries with no end time) had to be re-typed by hand as tasks. Added `icsToTasks` (maps a VEVENT's title/description/start into a task's `title`/`description`/`due_date`/`due_time`) and an "ICS 가져오기" button + file input on the Tasks toolbar reusing the existing `onImport` bulk-create path. Also fixed `parseIcs` to keep all-day VEVENTs that have no `DTEND` (previously dropped), and added a `.filter(e => e.end_at)` guard to Calendar's ICS import so its stricter timed-event requirement is unaffected (`frontend/src/ics.js`, `frontend/src/screens/Tasks.jsx`, `frontend/src/screens/Calendar.jsx`, tests in `frontend/src/ics.test.js`).
- [x] Backup export/import comment & attachment coverage (2026-07-18): the JSON backup (`/api/export`, Settings "JSON 내보내기"/"백업에서 복원") only covered the four entity tables (tasks/events/todos/work_logs), so a user's comment threads and attachments (including images) on any of them were silently dropped from a "full" backup and never restored, even though comments/attachments are fully shipped features elsewhere. Added the 8 comment/attachment tables to `/api/export`, and taught `/api/import` to remap each child row's parent foreign key to the newly-assigned parent id (generalizing the existing tasks-only id-map used for parent/dependency/work-log-task links into a per-table `id_maps` dict) before inserting comments/attachments in a final pass; the Settings import preview now also surfaces a combined 댓글·첨부 count (`backend/app/main.py`, `frontend/src/screens/Settings.jsx`, regression coverage extended in `backend/tests/test_api.py`'s `test_export_import_round_trip_preserves_hierarchy_and_links`).

- [x] Todo/work-log ICS import (2026-07-18): Tasks and Calendar could already import ICS (`icsToTasks`/`parseIcs`), and Today's todo/work-log lists could export ICS (`todosToIcs`/`logsToIcs`), but neither had an import path — an externally-edited or re-downloaded `.ics` file couldn't be brought back in as todos/work logs. Added `icsToTodos`/`icsToLogs` (mapping a VEVENT's title/description/start/end into `title`/`todo_date`/`todo_time`/`memo`, or `content`/`log_date`/`log_time`/`duration_minutes`) and "ICS 가져오기" buttons + file inputs next to the existing CSV import buttons in Today's todo and work-log toolbars, reusing the existing `onImportTodos`/`onImportLogs` bulk-create paths (`frontend/src/ics.js`, `frontend/src/screens/Today.jsx`, tests in `frontend/src/ics.test.js`).
- [x] Gantt bar left-edge resize (2026-07-18): the Gantt chart's task bars already had a right-edge drag handle to adjust `due_date` (and full-bar drag to move both dates together), but there was no way to drag the start date alone — changing just `start_date` required opening the edit form. Added a `resize-start` drag mode (`ganttDragDates`/`ganttDragPreview` in `frontend/src/ganttDrag.js`, clamped so start never passes due) and a matching left-edge `.bar-resize-start` handle on each draggable bar (`frontend/src/screens/Tasks.jsx`, `frontend/src/styles.css`), tests added in `frontend/src/ganttDrag.test.js`.
- [x] Audit Log PDF report (2026-07-18): Tasks/Calendar/Today all had a printable PDF report button alongside CSV export, but the 감사 로그 (Audit Log) screen only had CSV. Added `frontend/src/auditLogReport.js` (same `*Report.js` pattern as the other entities) and wired a PDF button into `frontend/src/screens/AuditLog.jsx`, tests in `frontend/src/auditLogReport.test.js`.
- [x] Search-focus keyboard shortcut (2026-07-19): Tasks/Calendar/Today/Audit Log screens each had a search box, but reaching it always required a mouse click even though the app already has a rich keyboard-shortcut system (N/Shift+N, Ctrl/⌘+K, 1–8, ?). Added a global `/` shortcut (ignored while already typing in a field) that focuses the current screen's search input via a shared `.search input, input.search` selector (`frontend/src/App.jsx`), listed in the shortcuts help panel (`frontend/src/components/KeyboardShortcuts.jsx`).
- [x] Search-box Escape-to-clear (2026-07-19): after the `/` shortcut focuses a screen's search box, there was no keyboard way to leave it — `Esc` did nothing because the input isn't inside `Modal.jsx`'s dialog handler. Added a local `onKeyDown` on the Tasks/Calendar/Today/Audit Log search inputs that clears the query and blurs on `Esc`, mirroring the app's existing "Esc exits current context" convention, and added a row to the shortcuts help panel (`frontend/src/screens/Tasks.jsx`, `frontend/src/screens/Calendar.jsx`, `frontend/src/screens/Today.jsx`, `frontend/src/screens/AuditLog.jsx`, `frontend/src/components/KeyboardShortcuts.jsx`).
- [x] Today inline-edit save/cancel shortcuts (2026-07-19): `Modal.jsx`'s dialogs already support `Ctrl/⌘+Enter` to save and `Esc` to close, but the Today screen's todo/work-log rows use an inline (non-modal) edit mode with no equivalent — after editing several fields on a todo or work-log row, the only way to save or cancel was clicking the small check/X buttons. Added a shared `editKeyDown(item)` handler wired to the todo-row and log-row wrapper `<div>`s (active only while that row is in edit mode) so `Ctrl/⌘+Enter` calls the existing `saveEdit` and `Esc` calls `setEdit(null)`, matching the modal convention (`frontend/src/screens/Today.jsx`, `frontend/src/components/KeyboardShortcuts.jsx`).
- [x] Task row arrow-key navigation (2026-07-19): the Tasks/Gantt screen's rows could only be opened one at a time via mouse click, with no keyboard way to move focus between them. Added `↑`/`↓` navigation on the task title button using a shared `nextRowIndex` helper (`frontend/src/rowNavigation.js`, `frontend/src/screens/Tasks.jsx`, tests in `frontend/src/rowNavigation.test.js`).
- [x] Today row arrow-key navigation (2026-07-19): the Tasks screen's rows supported `↑`/`↓` keyboard navigation between rows, but the Today screen's todo and work-log lists had no equivalent, breaking keyboard-only workflow consistency between the two list screens. Reused the same `nextRowIndex` helper on each row's "수정" button so `↑`/`↓` moves focus to the previous/next row within the todo list or work-log list (`frontend/src/screens/Today.jsx`).
- [x] Quick Capture result arrow-key navigation (2026-07-20): Tasks/Today rows supported `↑`/`↓` navigation, but the 빠른 입력 (Quick Capture, Ctrl/⌘+K) command-palette's screen-jump and search-result buttons had no keyboard navigation between results. Reused the shared `nextRowIndex` helper on each `.palette-result-btn` (`frontend/src/components/QuickCapture.jsx`).
- [x] Recurring task/todo spawn field parity (2026-07-20): completing a recurring task or todo auto-spawns the next occurrence, but `spawn_recurring_task`/`spawn_recurring_todo` only carried a subset of each entity's `CONFIG` fields, silently dropping `estimated_minutes`, `link_url`, `color`, `links`, and `checklist` (todos additionally missing `checklist`/`color`/`links`) on every generated occurrence. Added the missing columns to both INSERT statements, and — matching the existing duplicate-feature convention — reset checklist item `done` flags via a new `_reset_checklist` helper so the new occurrence starts with an unchecked list (`backend/app/main.py`), regression tests `test_recurring_task_spawn_carries_estimate_link_color_checklist_and_resets_checklist` and `test_recurring_todo_spawn_carries_color_links_and_resets_checklist` in `backend/tests/test_api.py`.
- [x] Attachment filename search (2026-07-20): Tasks/Calendar/Today screen search and the Ctrl/⌘+K command palette already matched title/description/tags/checklist text, but a file's own name (e.g. `Q3_budget_final.xlsx`) was invisible to search even though the list endpoints already exposed `attachment_count`. Added an `attachment_names` field (aggregated per item via `GROUP_CONCAT`-style query) to the generated list endpoint (`backend/app/main.py`), and included it in the searched text of `filterTasks` (`frontend/src/taskFilters.js`), `filterEventsByQuery` (`frontend/src/eventSearch.js`), `filterTodosByQuery`/`filterLogsByQuery` (`frontend/src/todaySearch.js`), and `searchItems`'s task/event/todo/log branches (`frontend/src/commandPalette.js`), with regression tests in each file plus `backend/tests/test_api.py`.
- [x] Offline status banner (2026-07-20): the app is installable as a PWA and `api.js` already special-cased network-failure errors, but going offline gave no proactive feedback — a save would just silently fail after a timeout with no warning beforehand. Added a `useOnlineStatus` hook backed by the `online`/`offline` window events (`frontend/src/networkStatus.js`, test `frontend/src/networkStatus.test.js`) and a persistent "오프라인 상태입니다" banner in the app shell, reusing the existing work-log-timer banner's `role="status"` pattern (`frontend/src/components/AppShell.jsx`, `frontend/src/styles.css`).
- [x] Frontend bundle code-splitting (2026-07-20): `vite build` warned that the single production JS bundle (755KB, 203KB gzip) exceeded the 500KB chunk-size guidance because every screen (Today/Tasks/Calendar/Performance/AI/Audit Log/Changelog/Settings) was statically imported into `App.jsx`, so a first-time visitor downloaded every screen's code before seeing any of them. Switched all 8 screen imports to `React.lazy(() => import(...))` and wrapped the two render sites (`/changelog` public route and the main `screens[page]` render) in `<Suspense>` with a shared loading fallback; the initial bundle dropped to 289KB (89KB gzip) with each screen now its own chunk fetched on first navigation (`frontend/src/App.jsx`).
- [x] Task archiving (2026-07-20): the only ways to get a task out of the active list were completing it (still visible until manually filtered) or soft-deleting it into the trash (implies "unwanted, pending purge") — there was no lifecycle state for "done with this, keep it, but stop showing it." Added a nullable `archived_at` column on `tasks` (`backend/app/db.py`), `/api/tasks/{id}/archive`, `/api/tasks/{id}/unarchive`, and `/api/tasks/archived` endpoints mirroring the existing trash restore pattern, and excluded archived tasks from the default task list query (`backend/app/main.py`). Added a "보관" button to each Gantt row and a new 보관함 (Archive) card in Settings for browsing/restoring archived tasks (`frontend/src/screens/Tasks.jsx`, `frontend/src/components/ArchiveSection.jsx`, `frontend/src/screens/Settings.jsx`, `frontend/src/api.js`), regression test `test_archive_task_hides_from_list_and_unarchive_restores_it` in `backend/tests/test_api.py`.
- [x] Todo archiving (2026-07-20): task archiving above only wired the new lifecycle state into tasks, leaving todos — the entity closest to tasks in shape/UI — with no way to hide a "done with this, keep it" item without deleting it. Added a nullable `archived_at` column on `todos` (`backend/app/db.py`), `/api/todos/{id}/archive`, `/api/todos/{id}/unarchive`, and `/api/todos/archived` endpoints mirroring the task pattern, and excluded archived todos from the default todo list query (`backend/app/main.py`). Generalized `ArchiveSection.jsx` to take an `entity` prop and render a second 보관함 card for todos in Settings, and added a "보관" button to each todo row in the Today screen (`frontend/src/screens/Today.jsx`, `frontend/src/screens/Settings.jsx`, `frontend/src/App.jsx`, `frontend/src/api.js`), regression test `test_archive_todo_hides_from_list_and_unarchive_restores_it` in `backend/tests/test_api.py`.
- [x] Event and work-log archiving (2026-07-20): task/todo archiving above left the other two entities — events and work logs — with no lifecycle state between "active" and "deleted," so a past one-off event or a stale work log could only be soft-deleted into the trash. Added a nullable `archived_at` column on `events` and `work_logs` (`backend/app/db.py`), `/api/events/{id}/archive`, `/api/events/{id}/unarchive`, `/api/events/archived`, `/api/work_logs/{id}/archive`, `/api/work_logs/{id}/unarchive`, and `/api/work_logs/archived` endpoints mirroring the task/todo pattern, and excluded archived rows from the default list query (`backend/app/main.py`). Extended `ArchiveSection.jsx` to a 4-entity lookup table (task/todo/event/work_log) rendering all four 보관함 cards in Settings, added a "보관" button to the event edit form and each work-log row in Today (`frontend/src/screens/Calendar.jsx`, `frontend/src/screens/Today.jsx`, `frontend/src/screens/Settings.jsx`, `frontend/src/App.jsx`, `frontend/src/api.js`), regression tests `test_archive_event_hides_from_list_and_unarchive_restores_it` and `test_archive_work_log_hides_from_list_and_unarchive_restores_it` in `backend/tests/test_api.py`.
- [x] Achievements report excludes archived tasks from active-task stats (2026-07-20): the archiving rollout above updated every default list query to exclude archived rows, but `/api/achievements`'s separate all-tasks query (used only for the "active tasks" / "average active progress" summary numbers) was missed, so archiving a still-unfinished task left it counted as active on the Performance screen. Added `archived_at IS NULL` to that query in `backend/app/main.py`, regression test `test_achievement_summary_excludes_archived_tasks_from_active_count` in `backend/tests/test_api.py`.
- [x] Audit log listing had a nondeterministic secondary sort (2026-07-20): `/api/audit-logs` ordered rows by `ORDER BY created_at DESC LIMIT ?` only, but `created_at` is stored at one-second precision, so any two log entries written within the same second (routine under normal request rates) tied and their relative order was left to SQLite's implementation-defined tie-breaking — meaning the most recent action could be silently pushed out of the `LIMIT` window or displayed out of order on the Audit Log screen. Added `id DESC` as a secondary sort key in `backend/app/main.py` so recency is always deterministic; caught via a newly order-dependent test failure while adding the achievements test above.
- [x] Audit log pagination (2026-07-20): the 감사 로그 screen silently hard-capped at the latest 200 entries ("최대 200개의 최신 변경을 보여줍니다.") with no way to see older history beyond narrowing the date range. Added `offset` support to `/api/audit-logs` (`backend/app/main.py`) and a "더 보기" button in `frontend/src/screens/AuditLog.jsx` that fetches the next page and appends it, using `frontend/src/api.js`'s updated `auditLogs(limit, start, end, offset)`, regression test `test_audit_logs_support_offset_pagination` in `backend/tests/test_api.py`.
- [x] Archive bulk unarchive (2026-07-20): the 4-entity archiving rollout above gave every 보관함 (Archive) card a per-row "보관 해제" button, but unlike the sibling 휴지통 (Trash) card it had no search box or multi-select — restoring several archived items one-by-one was tedious. Added a search filter, "전체 선택" checkbox, and a bulk-action-bar with "선택 보관 해제" to `frontend/src/components/ArchiveSection.jsx`, mirroring `TrashSection.jsx`'s existing `allIdsSelected`/`toggleSelectAllIds` (`frontend/src/taskFilters.js`) pattern; no backend change needed since bulk unarchive just runs the existing per-item unarchive calls in parallel.

- [x] Comment input max-length enforcement (2026-07-21): task/event/todo/work-log comment "add" and inline "edit" inputs had no client-side length limit, so a user could type past the backend's 2000-char cap and only discover the rejection after clicking 등록/blur (`HTTPException(422, "댓글은 2000자 이하로 입력하세요.")` in `backend/app/main.py`). Added `maxLength={2000}` to all 8 comment inputs (add + edit, ×4 entities) to match the app's existing `maxLength` convention used elsewhere for capped fields (`frontend/src/components/TaskForm.jsx`, `frontend/src/screens/Calendar.jsx`, `frontend/src/screens/Today.jsx`).
- [x] Yearly recurrence (2026-07-21): tasks, todos, and calendar events supported daily/weekly/biweekly/monthly recurrence but had no once-a-year option, so annually-recurring items (contract renewals, annual reviews, license renewals) had no way to be modeled as a recurring item at all. Added a `yearly` recurrence rule across `TaskPayload`/`TodoPayload` validation, `next_recurrence_date`/`_advance_event_datetime` (backend), `expandRecurringEvent` (frontend), the AI local-rules parser and remote-AI prompt (`backend/app/ai.py`), and every recurrence dropdown/CSV mapping (`frontend/src/components/TaskForm.jsx`, `frontend/src/screens/Today.jsx`, `frontend/src/screens/Calendar.jsx`, `frontend/src/screens/Tasks.jsx`, `frontend/src/csv.js`), regression tests in `backend/tests/test_api.py` and `frontend/src/eventRecurrence.test.js`.

- [x] Work log priority sort (2026-07-21): the Today screen's 업무 기록 정렬 dropdown offered 기본순/시각순/소요 시간순/내용순, but unlike the 할 일 and 일정 sort dropdowns (which both support 우선순위순), work logs had no priority-based sort option despite carrying the same `priority` field with an editable selector, filter, and CSV/template round-trip. Added a `priority` comparator to `LOG_SORT_COMPARATORS` (`frontend/src/logPins.js`, mirroring `todoPins.js`/`eventPins.js`) and the matching option to the sort `<select>` (`frontend/src/screens/Today.jsx`), regression test added in `frontend/src/logPins.test.js`.
- [x] Todo manual drag-and-drop ordering (2026-07-21): the Tasks screen's Gantt chart and Calendar both let a user drag items around freely, but the Today screen's 할 일 list only ever supported fixed comparator sorts (우선순위순/제목순/시간순, plus pin-to-top) — there was no way to arrange todos into an arbitrary custom order. Added a `manual` sort mode backed by a persisted per-id order map (`frontend/src/todoOrder.js`), wired as a new option in `TODO_SORT_COMPARATORS`/`orderTodosByPin` (`frontend/src/todoPins.js`) and drag handlers on each todo row when that mode is active (`frontend/src/screens/Today.jsx`, `frontend/src/styles.css`), regression tests in `frontend/src/todoOrder.test.js` and `frontend/src/todoPins.test.js`.
- [x] Archive undo (2026-07-21): every other bulk/single action that changes or removes data (delete, bulk complete, bulk priority/color/tag, postpone, ...) shows an "실행 취소" undo button on its success toast, but archiving a task/todo/event/work log — single or bulk — did not, even though the `unarchiveTask`/`unarchiveTodo`/`unarchiveEvent`/`unarchiveLog` endpoints already existed and were only reachable from the separate 보관함 screen. Added shared `undoArchive`/`undoBulkArchive` helpers mirroring the existing `undoDelete`/`undoBulkDelete` pattern and wired them into all 6 archive/bulk-archive handlers (`frontend/src/App.jsx`); the two todo/log single-archive handlers were switched from the optimistic `mutateLocal` path to `mutate` (matching how task/event archive already worked) since only `mutate` supports the undo toast action.

- [x] Gantt critical-path highlighting (2026-07-21): task dependencies, cycle prevention, and a "후속 업무" (dependents) list already existed, but nothing computed or visually marked which chain of dependent tasks actually determines the overall schedule — a delay on a task off that chain doesn't slip anything, but the UI treated all dependency badges the same. Added `criticalPathTaskIds` (`frontend/src/taskHierarchy.js`), a pure function that walks the dependency graph of unfinished tasks and finds the chain with the largest total duration (using each task's date span, defaulting to 1 day), and wired a "핵심 경로" badge into the Gantt row next to the existing 선행 업무 대기 badge (`frontend/src/screens/Tasks.jsx`).
- [x] Task custom fields (2026-07-21): a commercial-gap audit found tasks had no way to record team/project-specific structured data (client name, contract number, project code, ...) — everything had to be crammed into the free-text memo. Added a nullable `custom_fields` JSON column on `tasks` (`backend/app/db.py`), following the existing `links` array pattern (`_clean_custom_fields` validator, `TaskPayload.custom_fields`, generic `CONFIG`/`normalize()` wiring in `backend/app/main.py`, copied through on recurrence spawn). Added a label/value add-remove editor to the task form and a Gantt row count badge (`frontend/src/taskFormPayload.js`, `frontend/src/components/TaskForm.jsx`, `frontend/src/screens/Tasks.jsx`), regression tests in `backend/tests/test_api.py` and `frontend/src/taskFormPayload.test.js`.
- [x] Custom field search (2026-07-21): task custom fields (added above) were invisible to the Tasks screen's search box and the Ctrl/⌘+K command palette, unlike checklist text and attachment filenames which both sources already searched — a task findable only by its custom field (e.g. client name "어크미") couldn't be found. Added `custom_fields` label/value text to `filterTasks` (`frontend/src/taskFilters.js`) and the task entry of `ITEM_SOURCES` (`frontend/src/commandPalette.js`), regression tests in `frontend/src/taskFilters.test.js` and `frontend/src/commandPalette.test.js`.
- [x] Todo/log manual-order keyboard move buttons (2026-07-21): the "직접 정렬" manual sort mode added above for todos and work logs was drag-only, unlike the checklist-item reorder pattern elsewhere in the app (task/todo/log checklists) which pairs drag with explicit 위로/아래로 이동 buttons — keyboard and screen-reader users had no way to reorder items in manual mode. Added move-up/move-down buttons to each todo and work-log row when its sort mode is `manual`, reusing the existing `moveTodoBefore`/`moveLogBefore` pure functions with the adjacent item as the target (`frontend/src/screens/Today.jsx`), regression tests in `frontend/src/todoOrder.test.js` and `frontend/src/logOrder.test.js`.
- [x] Task CSV import duplicate detection (2026-07-21): the Tasks screen's "CSV 가져오기" bulk-created every parsed row unconditionally, so re-importing a previously-exported file (or a file imported twice by mistake) silently created full duplicate tasks with no warning or skip option. Added `dedupeImportedTasks` (`frontend/src/csv.js`), which compares each parsed row's title/start date/due date against both existing tasks and earlier rows in the same file and drops matches, wired into `frontend/src/screens/Tasks.jsx`'s import handler with a "건너뜀" count reusing the existing import-result toast, regression tests in `frontend/src/csv.test.js`.
- [x] Todo custom fields (2026-07-21): task custom fields (added above) had no equivalent on the Today screen's 할 일 list, so the same "record a client name/project code without cramming it into memo" gap that motivated the task feature still existed for todos — a commercial gap given todos and tasks otherwise share the checklist/links/estimate feature set. Added a nullable `custom_fields` JSON column on `todos` (`backend/app/db.py`), reusing the existing `_clean_custom_fields` validator (`TodoPayload.custom_fields`, `CONFIG`/`normalize()` wiring, copied through on recurrence spawn in `backend/app/main.py`). Added a label/value add-remove editor to the todo edit row and a row count badge (`frontend/src/screens/Today.jsx`), regression test in `backend/tests/test_api.py`.
- [x] Todo add-form links/custom fields (2026-07-21): the previous entry added 링크/사용자 정의 필드 editing to the todo *edit* row, but the new-todo quick-entry form on the Today screen had no equivalent inputs, so a link or custom field could only be attached to a todo after first creating it and reopening it in edit mode. Added the same 첨부 링크/사용자 정의 필드 add-remove editors to the new-todo form and wired `onAddTodo` in `App.jsx` to forward `links`/`custom_fields` through to `api.createTodo` (`frontend/src/screens/Today.jsx`, `frontend/src/App.jsx`), reusing the existing `normalizedLinks`/`normalizedCustomFields` helpers (already covered by `frontend/src/taskFormPayload.test.js`).
- [x] Gantt drag-to-reparent (2026-07-21): moving a task under another task (or promoting a subtask back to top level) required opening the edit form and picking a parent from a dropdown, or using the bulk-action bar. Added drag-and-drop reparenting directly on the Gantt rows — dragging a task's title onto another row's title makes it a child, reusing the existing cycle-prevention validation (`frontend/src/taskHierarchy.js`'s `canReparentTask()`, `frontend/src/screens/Tasks.jsx`, `frontend/src/styles.css`), tests in `frontend/src/taskHierarchy.test.js`.
- [x] Task Excel export (2026-07-21): the Tasks screen could export to CSV/ICS/PDF but not a native Excel workbook, so opening the list in Excel with proper formatting meant round-tripping through CSV first. Added an "Excel" export button that downloads a SpreadsheetML (.xls) workbook built from the same header/row data as the CSV export (`frontend/src/csv.js`'s `taskHeaders`/`taskRows`, new `frontend/src/xlsx.js` with `rowsToSpreadsheetXml`/`tasksToExcelXml`, `frontend/src/screens/Tasks.jsx`), tests in `frontend/src/xlsx.test.js`.
- [x] Events/todos/work-logs Excel export (2026-07-21): the previous entry only added Excel export to the Tasks screen, leaving Calendar (일정) and Today (Todo/업무 기록) exporting to CSV/ICS/PDF only — an inconsistent gap for a commercial tool where Excel round-tripping is a common ask across every list. Split `csv.js`'s inline CSV row builders into reusable `eventHeaders`/`eventRows`, `todoHeaders`/`todoRows`, `workLogHeaders`/`workLogRows`, and added `eventsToExcelXml`/`todosToExcelXml`/`workLogsToExcelXml` to `frontend/src/xlsx.js` reusing `rowsToSpreadsheetXml`. Added matching "Excel" buttons next to each screen's CSV button (`frontend/src/screens/Calendar.jsx`, `frontend/src/screens/Today.jsx`), tests in `frontend/src/xlsx.test.js`.
- [x] Todo/log/event custom field search (2026-07-21): the "Custom field search" entry above only covered the Tasks screen's search box and command palette; the Today screen's 할 일/업무 기록 search and the Calendar's 일정 search still ignored `custom_fields`, so a todo/log/event findable only by its custom field (e.g. a client name) couldn't be found there — the same gap, missed for three of four entities. Added `custom_fields` label/value text to `filterTodosByQuery`/`filterLogsByQuery` (`frontend/src/todaySearch.js`) and `filterEventsByQuery` (`frontend/src/eventSearch.js`), regression tests in `frontend/src/todaySearch.test.js` and `frontend/src/eventSearch.test.js`.
- [x] Todo deep-link copy (2026-07-21): the Tasks screen's Gantt rows already had a "링크" button copying a shareable internal deep link (`taskDeepLink`, `?page=tasks&taskId=`) that `App.jsx` auto-opens in the task edit modal on load, but the Today screen's 할 일 list had no equivalent — a todo could only be shared by describing it in words. Added `todoDeepLink` (`frontend/src/todoFilterPresets.js`, `?page=today&todoId=`), a matching "링크" button on each todo row (`frontend/src/screens/Today.jsx`), and a `todoId`-handling effect in `App.jsx` that switches to the Today screen and opens the matching todo's inline edit mode once todos are loaded, regression test in `frontend/src/todoFilterPresets.test.js`.
- [x] Event/work-log deep-link copy (2026-07-21): tasks and todos both had a "링크" button copying an internal deep link that auto-opens the item on load, but calendar events and work logs still had no equivalent, so sharing a specific meeting or billable entry meant describing it in words. Added `eventDeepLink` (`frontend/src/eventFilterPresets.js`, `?page=calendar&eventId=`) and `logDeepLink` (`frontend/src/logFilterPresets.js`, `?page=today&logId=`), a "링크 복사" button in the event edit form (`frontend/src/screens/Calendar.jsx`) and on each work-log row (`frontend/src/screens/Today.jsx`), and matching `eventId`/`logId`-handling effects in `App.jsx` that open the matching event/log once data is loaded, regression tests in `frontend/src/eventFilterPresets.test.js` and `frontend/src/logFilterPresets.test.js`.
- [x] Recurring task/todo next-occurrence preview (2026-07-21): a task or todo's 반복 (recurrence) select had no feedback about what date the next occurrence would actually land on until after saving and letting it spawn or skip — the only place that logic existed was server-side (`next_recurrence_date` in `backend/app/main.py`, used by skip-recurrence). Added a client-side pure equivalent `nextRecurrenceDate` (`frontend/src/recurrencePreview.js`, with month/year-end day clamping), wired into a "다음 회차" hint shown live in the task form (`frontend/src/components/TaskForm.jsx`) and both the todo quick-add and inline-edit forms (`frontend/src/screens/Today.jsx`), regression tests in `frontend/src/recurrencePreview.test.js`.
- [x] Calendar event manual sort order (2026-07-21): the Today screen's todo and work-log lists both had a "직접 정렬" (manual) sort mode with drag-and-drop and ▲▼ keyboard reordering (`todoOrder.js`/`logOrder.js`), but the Calendar screen's event agenda list only offered time/priority/title sort with no manual option — the one entity where users couldn't set their own display order. Added `frontend/src/eventOrder.js` (mirroring `todoOrder.js`'s persisted swap/move logic) and a `manual` comparator in `frontend/src/eventPins.js`, wired into a new "직접 정렬" option plus drag/▲▼ row controls in `frontend/src/screens/Calendar.jsx`'s mobile agenda list, regression tests in `frontend/src/eventOrder.test.js` and `frontend/src/eventPins.test.js`.
- [x] Task badge visibility toggle (2026-07-21): the Tasks screen's Gantt rows always rendered up to 10 badge types (subtasks, checklist, estimate, recurrence, links, custom fields, comments, attachments, blocked, critical path) with no way to hide the ones a user didn't care about, making dense task lists noisy. Added `frontend/src/taskBadgeVisibility.js` (persisted per-badge visibility set) and a "표시 항목" dropdown in the task toolbar (`frontend/src/screens/Tasks.jsx`) gating each badge's render, regression test in `frontend/src/taskBadgeVisibility.test.js`.
- [x] Event public share link (2026-07-21): tasks had a "공유 링크 만들기" feature (`public_token`, `/api/public/tasks/{token}`, `PublicTask.jsx`) letting anyone view a read-only task without logging in, but calendar events only had the internal "링크 복사" deep link, which still requires a WorkManager login — no way to send an external client/vendor a link to a meeting's details. Added `public_token` to `events` (`backend/app/db.py`), `POST/DELETE /api/events/{id}/share` and unauthenticated `GET /api/public/events/{token}` (`backend/app/main.py`), a matching share-link editor in the event form (`frontend/src/screens/Calendar.jsx`) and a `PublicEvent.jsx` read-only view routed at `/public/events/:token` (`frontend/src/App.jsx`), regression test in `backend/tests/test_api.py`.
- [x] Todo public share link (2026-07-21): tasks and events both had a "공유 링크 만들기" feature letting anyone view a read-only item without logging in, but the Today screen's 할 일 (todo) list only had the internal deep-link copy button, which still requires a WorkManager login — no way to send an external collaborator a read-only view of a single to-do. Added `public_token` to `todos` (`backend/app/db.py`), `POST/DELETE /api/todos/{id}/share` and unauthenticated `GET /api/public/todos/{token}` (`backend/app/main.py`), a matching share-link editor in the todo inline-edit form (`frontend/src/screens/Today.jsx`) and a `PublicTodo.jsx` read-only view routed at `/public/todos/:token` (`frontend/src/App.jsx`), regression test `test_todo_share_link_exposes_read_only_view_and_can_be_revoked` in `backend/tests/test_api.py`.
- [x] Work log public share link (2026-07-21): tasks, events, and todos all had a "공유 링크 만들기" feature letting anyone view a read-only item without logging in, but the Today screen's 업무 기록 (work log) list had no equivalent — sharing a completed-work summary externally required copying the text out manually. Added `public_token` to `work_logs` (`backend/app/db.py`), `POST/DELETE /api/work_logs/{id}/share` and unauthenticated `GET /api/public/work_logs/{token}` (`backend/app/main.py`), a matching share-link editor in the work-log inline-edit form (`frontend/src/screens/Today.jsx`) and a `PublicWorkLog.jsx` read-only view routed at `/public/work_logs/:token` (`frontend/src/App.jsx`), regression test `test_work_log_share_link_exposes_read_only_view_and_can_be_revoked` in `backend/tests/test_api.py`. Closes public-share-link parity across all 4 entities.
- [x] Per-tag color customization (2026-07-21): tags were usable for filtering and search across all 4 entities, but a tag itself had no visual identity — every `#tag` chip looked the same, making it hard to spot categories like "urgent" or a client name at a glance in a dense list. Reused the existing `app_settings` key-value store (a `tag_colors` JSON map per user, no new table) via `load_tag_colors`/`PUT /api/tags/color` (`backend/app/main.py`), which also extends `GET /api/tags` to return each tag's `color`. Added a color picker to each chip in the 태그 관리 screen (`frontend/src/components/TagManager.jsx`) and a color-dot render in the shared `TagFilter` chip component (`frontend/src/components/TagsInput.jsx`), wired into the Tasks/Today/Calendar/Performance screens' tag filters via a small `deriveTagColorMap` helper (`frontend/src/tagColors.js`), regression tests `test_tag_color_set_get_and_clear` in `backend/tests/test_api.py` and `frontend/src/tagColors.test.js`.

## Removed (2026-07-10): Team/Assignee Features

WorkManager is a single-user personal tool; the team roster, per-member capacity, workload/reassignment UI, and mandatory task ownership built up over earlier iterations didn't fit that scope and were removed at the user's request (see docs/IMPROVEMENT_PLAN.md Wave 2 item 21). Assignee/collaborator info now goes in a task's memo or tags instead of a dedicated field. This removed everything previously listed here: team/member assignment and workload ownership, per-member daily capacity limits, quick reassignment, team roster management, assignee-required validation on active/done tasks, assignee reuse suggestions, assignee workload filtering, unassigned-task alerts, assignee load/capacity previews, and role-based permissions for managers/members/viewers (the last was never implemented, and is now explicitly out of scope — see the plan's "범위 밖" section).
