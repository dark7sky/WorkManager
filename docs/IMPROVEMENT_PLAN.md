# WorkManager 실사용 개선 플랜

최종 갱신: 2026-07-10

목표: 개인 업무 관리(오늘 할 일·일정·진행도·업무 기록·성과 보고)를 매일 안정적으로 쓸 수 있는 수준으로 끌어올리고, 이후 상업 프로그램 수준의 완성도를 갖춘다.

## 현황 진단 (2026-07-10)

- 기능 뼈대(업무 Gantt·캘린더·오늘·성과·AI·감사로그·휴지통)는 갖춰져 있으나, 최근 "AI 설정" 기능이 **인코딩이 깨진 채(한글 → `?`) 커밋**되어 설정 화면 AI 섹션과 백엔드 상태 메시지가 물음표로 표시됨.
- 백엔드 테스트 67개 중 **2개 실패**: `PUT /api/settings/ai` 응답에 `provider_name`/`api_key_set` 누락(계약 위반), `ai.status()` 시그니처 변경에 못 따라간 낡은 테스트.
- 개인용 도구인데 **팀용 승인 워크플로가 강제**: 업무를 완료하면 항상 "승인 대기", 날짜를 바꾸면 항상 "일정 승인 대기"가 되어 혼자 쓰는 사람이 자기 업무를 자기가 승인해야 함 → 일상 사용의 마찰.
- 오늘 화면의 예정 업무 행은 **행 전체가 완료 토글 버튼**이라 모바일에서 실수로 탭하면 업무가 즉시 완료(100%) 처리됨.
- 프런트 테스트 53개는 전부 통과. CI 구성(빌드+테스트+컨테이너)은 정상.

## P0 — 안정성 회복 (즉시)

1. 인코딩 깨짐 복구: `frontend/src/screens/Settings.jsx` AI 섹션, `backend/app/ai.py` `status()` 문자열. 원문은 커밋 `9d92919`에서 복구. — **정정(2026-07-10 재점검)**: 백엔드 `ai.py`는 실제로 정상이었으나 프런트 `Settings.jsx` AI 섹션은 이 항목이 "완료"로 표시된 뒤에도 실제로는 고쳐지지 않은 채 남아 있었음(라벨·상태 배지·플레이스홀더·에러 메시지·진단 목록 전부 `?`). 커밋 `adb3e02`에서 실제로 복구.
2. `PUT /api/settings/ai` 응답을 `GET /api/settings/ai`(=`ai.status()`)와 같은 형태로 통일 → 실패 테스트 1 해결.
3. `test_ai.py`의 낡은 `status()` 호출 테스트를 사용자 단위 시그니처에 맞게 수정 → 실패 테스트 2 해결.
4. 완료 기준: 백엔드 67개·프런트 53개 테스트 전부 통과 + `npm run build` 성공.

## P1 — 개인 실사용성 (즉시)

5. **승인 워크플로 선택화(개인 모드)**: 사용자별 설정 `approval_workflow`(기본: 켜짐=현재 동작). 끄면 완료 시 승인 대기 생성 안 함, 일정 변경 시 일정 승인 대기 생성 안 함. 설정 화면에 토글 추가. 승인 관련 UI는 상태가 실제로 존재할 때만 표시되므로 자연스럽게 사라짐.
6. **오늘 화면 오조작 방지**: 예정 업무 행에서 완료 토글은 체크 아이콘에만 반응하도록 분리.

## P2 — 반응성·모바일 (완료: 7·8 · 남음: 9)

7. 모든 변경 후 전체 재조회(`refresh()`) → Todo 토글·기록 추가 같은 소규모 변경은 부분 갱신/낙관적 업데이트로 전환.
8. 캘린더 주간 보기(현재 월간+모바일 아젠다만 존재).
9. 모바일 폼 입력성 점검(datetime-local, 슬라이더 조작감).

## P3 — 성과 보고 강화 (완료: 10·11 · 남음: 12)

10. 주간/월간 보고서 프리셋: 성과 화면에서 기간+태그 조합 저장, Markdown 내보내기.
11. 업무 기록 ↔ 업무 연결 UI(백엔드 `task_id`는 이미 존재, 입력 UI 없음).
12. AI 성과 요약의 근거 표시(어떤 기록·업무를 사용했는지 목록).

## P4 — 운영 완성도 (완료: 13·14·15·16)

13. 백업 복원 리허설 절차 문서화 + 복원 검증 스크립트.
14. 앱 버전 표시 및 서비스워커 업데이트 안내 개선.
15. e2e 스모크 테스트(로그인 우회 테스트 토큰으로 핵심 CRUD 왕복). — `backend/tests/test_e2e_smoke.py`: 세션 토큰을 직접 발급해 로그인 절차를 우회하고, tasks/events/todos/work_logs 4개 테이블 전체에 대해 생성→목록→조회→수정→삭제→휴지통 확인→복원까지 왕복하며 감사 로그 기록까지 검증.
16. `backend/.venv` 등 로컬 산출물 gitignore 정리.

## 진행 방식

- 저비용 모델(Haiku) 에이전트가 배치 단위로 구현, 메인 세션이 스펙 작성·검증(테스트/빌드/디프 리뷰)을 담당.
- 배치 순서: P0 → P1(5) → P1(6). 파일 충돌 방지를 위해 순차 실행.
- 저장소 관례 유지: 사용자 가시 변경은 `frontend/src/data.js`의 changelog와 `docs/ROADMAP.md` 반영.

---

# Wave 2 — 데이터 안전·1인 사용 정리 (완료)

최종 갱신: 2026-07-10

Wave 1(P0-P4, 위 내용)은 전부 완료·커밋됨(`89b31fa` 기준). 백엔드 71개·프런트 73개 테스트 전부 통과, TODO/FIXME 잔여 없음, 순수 로직 모듈 테스트 커버리지 양호. 아래는 코드베이스 재점검으로 찾은 다음 물결 후보.

## 현황 진단 (2026-07-10)

- ~~휴지통 열람/복원 UI가 아예 없음~~ — 최초 점검 오류로 판명. `frontend/src/components/TrashSection.jsx`(커밋 `ec9e673`)가 이미 `/api/trash`·복원·30일 정리를 구현해 `Settings.jsx`에 렌더링되고 있음. 조치 불필요.
- **AI 성과 요약에 근거 목록 없음**: Wave 1 P3 item 12로 이미 식별됐으나 미착수 상태로 남음. `backend/app/ai.py`의 `period_summary`/`smart_period_summary`가 `highlights`를 문장에 녹여서만 반환하고 구조화된 목록으로 반환하지 않음. `Performance.jsx`는 headline·narrative만 렌더링.
- ~~오프라인 쓰기 실패 메시지가 일반 에러와 구분 안 됨~~ — 이 역시 최초 점검 오류. `frontend/src/api.js:33-36`의 `request()`가 `fetch` 실패(오프라인 포함)를 이미 별도로 잡아 "서버에 연결할 수 없습니다. 네트워크 상태를 확인해 주세요."라는 전용 메시지(`status:0`)로 던지고 있어, 서버 거부 메시지와 이미 구분됨(커밋 `5971a6e`). 조치 불필요.
- **(사용자 지시로 착수·완료) 담당자/팀원 기능이 1인 사용과 맞지 않음**: 사용자가 혼자 쓰는 도구인데 업무마다 담당자 지정을 강제하고, 팀원 명단·담당자별 업무량·14일 부하·재배정 UI까지 갖추고 있었음. 사용자가 이 기능 전체를 없애고, 필요하면 담당자·협업자 정보는 메모나 태그로 남기도록 지시.

## 완료 — AI 요약 근거 표시 (2026-07-10)

18. **AI 성과 요약 근거 표시**: `period_summary`/`smart_period_summary` 응답에 `highlights`(사용한 기록·업무 id/제목/날짜 목록)를 구조화 필드로 추가, `Performance.jsx`에 "근거 N건" 목록으로 렌더링.

## 완료 — 담당자/팀원 기능 제거 (2026-07-10)

21. **담당자·팀원 기능 전체 제거**: 사용자 지시에 따라 1인 사용 도구에 맞지 않는 담당자 지정·팀원 관리 기능을 제거.
    - 백엔드: `tasks.assignee_name`을 CONFIG/Pydantic 모델/검증에서 제외(컬럼 자체는 마이그레이션 정책상 유지, 값은 더 이상 API로 읽거나 쓰지 않음). `task_requires_owner`/`task_has_owner`/`validate_task_ownership`(진행·완료 업무에 담당자 강제) 제거.
    - 프런트: `teamMembers.js`·`teamMemberCapacity.js`(로컬스토리지 팀원 명단·1일 처리 한도) 삭제. `TaskForm`의 담당자 입력·배정 현황 미리보기, `Tasks` 화면의 담당자 필터·업무량/부하 스트립·미지정 알림, `Settings`의 팀원 명단·재배정 UI 제거. CSV·PDF 내보내기에서 담당자 열 제거.
    - `TaskForm`의 메모(설명) 입력란에 "담당자·협업자 등은 메모나 태그로 남겨두세요" 안내 추가.
    - `docs/ROADMAP.md`의 "Commercial Schedule Management Gaps" 절을 제거 사실 기록으로 교체(아래 범위 밖 참고).

## 범위 밖 (의도적 보류)

- 매니저/멤버/뷰어 역할 기반 권한 — 현재 구조는 사용자별로 데이터가 완전히 분리된 1인 계정 모델. 여러 계정이 워크스페이스를 공유하는 기능은 담당자/팀원 기능 제거로 방향이 완전히 반대가 됨. 앞으로도 다루지 않음.

---

# Wave 3 — 코드 감사로 찾은 실사용 버그 (완료)

최종 갱신: 2026-07-10

Wave 2까지 커밋된 상태에서 새 기능 대신 정확성 감사를 진행(백엔드 위험 모듈 정밀 감사 + 프런트 화면 전수 점검). 아래는 실제로 재현 가능한 버그로 확인해 고친 것만 기록.

22. **`Today.jsx`의 "오늘" 날짜가 페이지 로드 시점에 고정됨**: `now`/`dateText`가 모듈 최상단 상수라 브라우저 탭을 자정 너머로 켜 두면(이 앱은 PWA로 계속 켜 두는 용도) "오늘 일정" 필터와 헤더 날짜가 영원히 어제 날짜로 고정됨. 컴포넌트 본문으로 이동해 렌더마다 재계산하도록 수정(커밋 `fe29afa`).
23. **`PATCH /api/tasks/{id}`·`PATCH /api/events/{id}`가 빈 body를 그냥 통과시킴**: `update_item()`이 `updated_at` 갱신값을 먼저 채운 뒤에 "변경 없음" 검사를 해서, tasks/events는 검사가 죽은 코드였음(todos/work_logs는 정상 동작). 검사 순서를 바꿔 네 테이블 모두 동일하게 422를 반환하도록 수정(커밋 `cafc6e5`). `POST /api/ai/apply`가 빈 데이터로 조용히 no-op하는 경로도 함께 막힘.
24. **사이드바가 로그인한 사용자의 실제 이름을 표시하지 않음**: `AppShell.jsx`가 `user?.name`/`user?.user_id`를 읽었지만 `/api/auth/me`는 `{id, email, display_name, picture_url}`을 반환 — 항상 "나의 작업 공간" 기본값과 이메일 기반 이니셜만 보였음. `display_name`/`id`로 수정(커밋 `f6f3387`).
25. **헤더의 종 아이콘 버튼이 아무 동작도 하지 않음**: 앱의 다른 모든 아이콘 전용 버튼과 달리 `onClick`도 `aria-label`도 없는 죽은 UI. 제거(커밋 `264bf51`).
26. **설정 화면 AI 섹션 인코딩 깨짐이 실제로는 안 고쳐져 있었음**: Wave 1 P0 item 1이 "완료"로 기록했지만 프런트 절반은 실제로 미반영. 재점검으로 발견해 실제로 복구(커밋 `adb3e02`). 위 P0 item 1 참고.
27. **CI가 프런트 유닛 테스트를 실행하지 않음**: `frontend` job이 `npm run build`만 실행하고 `npm test`(54개 테스트)는 건너뜀 — 순수 로직 모듈 회귀가 CI로 잡히지 않음. 테스트 스텝 추가(커밋 `c91d030`).
28. **AI 로컬 규칙 파서가 "1월 5일"처럼 연도 없는 날짜를 항상 올해로 해석**: 연말에 내년 날짜를 말하면 이미 지난 날짜가 조용히 만들어짐. 계산한 날짜가 오늘보다 이전이면 내년으로 굴리도록 수정(커밋 `ab27991`).

진행 방식 메모: 이번 웨이브는 새 audit fork가 두 번 잘못된 판단(이미 있는 기능을 "없다"고 보고)을 한 뒤라, 이번엔 fork 보고를 그대로 믿지 않고 직접 파일을 읽어 재현 조건을 확인한 것만 반영. `update_item` 버그(23번)는 fork가 찾았지만 나머지는 전부 직접 읽으며 발견.

---

# Wave 4 — 사용자 요청 기능 (완료)

최종 갱신: 2026-07-10

Wave 3 정적 감사 이후, 실제 헤드리스 브라우저(Playwright + 로그인 우회 세션 토큰)로 화면을 직접 띄워 스크린샷으로 검증하는 절차를 처음 도입. 사용자가 새 기능도 제안해 달라고 해서 후보를 냈고, 사용자가 두 개를 골랐다.

29. **삭제 실행 취소 토스트**: 업무·일정·오늘 할 일·업무 기록을 삭제하면 뜨는 토스트에 "실행 취소" 버튼 추가. 이미 있는 휴지통 복원 API(`api.restoreTrash`)를 그대로 재사용. `notify()`/`mutate()`/`askDelete()`에 선택적 `undo` 콜백을 추가하는 방식으로 네 개 삭제 지점 전부에 한 번에 적용(커밋 `cf70a7c`).
29-2. **Ctrl/⌘+K 빠른 입력**: 어느 화면에서든 단축키(또는 사이드바 "빠른 입력" 버튼)로 모달을 열어 자연어로 업무·일정·할 일·기록을 즉시 생성. 새 백엔드 엔드포인트 없이 AI 도우미 화면과 같은 `api.aiPreview`/`api.aiApply` 미리보기→적용 파이프라인을 재사용(커밋 `47dd36c`). 헤드리스 브라우저로 Ctrl+K → 자연어 입력 → 미리보기 → 적용 → 캘린더에 실제 반영까지 전 과정 확인.
30. **(브라우저 검증 중 추가 발견) `.done-notes` 글자가 세로로 한 자씩 쪼개져 보임**: `.done-notes div`(자손 선택자)가 각 기록 행뿐 아니라 그 안에 중첩된 내용 `<div>`에도 매치되어, `grid-template-columns:24px 1fr auto`가 텍스트 span에 그대로 적용됨 — 24px짜리 첫 칸에 억지로 들어간 글자가 한 자씩 줄바꿈됨. `.done-notes>div`(직계 자식만)로 범위를 좁혀 해결(커밋 `cf70a7c`, `9ebfd23`의 요약-여백 수정과 마찬가지로 정적 코드 리뷰로는 못 찾고 실제 화면을 봐야만 드러난 버그).

진행 방식 메모: 정적 읽기만으로는 30번 같은 버그를 못 찾는다. 이번에 처음으로 실제 dev 서버 두 개(백엔드+프런트)를 띄우고 헤드리스 크로미움으로 화면마다 스크린샷을 찍어 눈으로 확인하는 절차를 썼다 — 앞으로 화면 관련 변경은 이 방식으로 검증할 가치가 있다.

---

# Wave 5 — 실사용자 개선 요청 큐 처리 (완료)

최종 갱신: 2026-07-10

`https://work.ysyoo.link/api/public/changelog`(운영 배포의 공개 변경 이력 API)에 쌓여 있던 대기 중 요청 9건을 실제로 읽어서 처리. 작은 항목은 저비용 모델(Haiku) 서브에이전트에게 배치로 구현을 맡기고 메인 세션이 diff 리뷰·헤드리스 브라우저 검증을 담당, 상태 변경이 걸린 복잡한 두 건(다중 액션 AI, 샘플 로그인)은 메인 세션이 직접 구현.

- **#10 (AI 설정 한글 깨짐)**: 이 세션 앞부분에서 이미 수정(커밋 `adb3e02`). 추가 조치 불필요.
- **#10 (Vendor 선택 시 모델 목록 안 바뀜)**: 두 가지 버그가 겹쳐 있었음 — `aiProvider` 우선순위와 `applyAiConfig()`가 `selected_provider`(저장된 값)를 `provider`(방금 요청한 값)보다 우선해 비동기 응답이 오면 원래대로 되돌아감. 둘 다 수정(커밋 `6ab440f`).
- **#9 (여러 요청 한번에 처리 안 됨)**: `ai.parse_text()`가 `{"items":[...]}`를 반환하도록 계약 변경, 줄바꿈 기준으로 최대 10건 분리 파싱. AI 도우미·빠른 입력 모두 항목별 개별 미리보기/적용 카드로 전환(커밋 `bc8730b`).
- **#8 (일정 종료시간 자동 +1h)**: 시작시간 변경 시 종료시간 미수정 상태면 자동으로 +1시간, 사용자가 종료시간을 직접 바꾸면 그 이후로는 따라가지 않음(커밋 `6ab440f`).
- **#7 (캘린더 일정 시간 역순 표시)**: 날짜 셀 안 일정 목록에 정렬이 아예 없어서 백엔드 반환 순서(사실상 임의)를 그대로 썼던 문제. 시작 시각 기준 오름차순 정렬 추가(커밋 `6ab440f`).
- **#6 (완료 요청은 삭제하고 변경 로그에 남기기)**: 이미 구현되어 있던 기능(`PATCH /api/admin/feature-requests/{id}`가 done 처리 시 `changelog_entries`에 원문 요청과 요청일을 함께 기록, `/api/public/changelog`의 `requests`는 pending/in_progress만 노출). 추가 조치 불필요.
- **#5 (모바일 PWA 자동회전 안 되게)**: `manifest.webmanifest`의 `orientation`을 `"any"`에서 `"portrait-primary"`로(커밋 `6ab440f`).
- **#4 (가입 전 미리보기용 샘플 로그인)**: 범위를 사용자에게 직접 확인(읽기 전용 데모로 결정). 고정 `__demo__` 계정에 오늘 날짜 기준 샘플 데이터를 시딩하고, 모든 쓰기 요청을 한 곳(미들웨어)에서 차단. 로그인 화면에 "샘플로 미리 보기" 버튼 추가(커밋 `b556486`). 이 과정에서 기존 레거시 단일사용자 자동 이관 로직이 데모 계정을 "두 번째 실사용자"로 잘못 세어 깨지는 회귀를 검증 중 발견해 함께 수정.
- **#3 / #2 (모바일에서 설정 메뉴 안 보임 / 감사 들어가면 하단 메뉴 깨짐)**: 근본 원인은 하나 — 모바일 하단 메뉴가 8개인데 이미 튜닝되어 있던 CSS는 7열 고정 높이라 8번째(설정)가 화면 밖으로 잘림. 사용자가 제안한 대로 감사 로그를 최상위 메뉴에서 빼서 설정 화면 안으로 이동(커밋 `6ab440f`).

모든 항목 헤드리스 브라우저로 실제 화면 확인. 검증 중 실제 버그 2개를 추가로 발견해 즉시 수정: (1) Haiku가 짠 벤더 전환 수정이 비동기 재조회 시점에 원상복구되는 걸 놓쳤던 것, (2) 다중 액션 미리보기 카드가 각자의 항목이 아니라 입력 전체를 설명으로 표시하던 것.

---

# Wave 6 — 디자인 개선 (완료)

최종 갱신: 2026-07-10

사용자 요청: "디자인 개선도 해줘. 더 멋지고 사용하기 수월한 디자인을 적용해줘." `frontend/src/styles.css`를 전체 읽고 정리하면서 진행.

31. **디자인 토큰 도입 및 시각 정리**: `--radius-lg`(18px)/`--radius`(13px)/`--radius-sm`(9px)/`--ease` 토큰을 추가하고 카드·버튼·입력창·모달 등 모든 표면에 일관된 모서리 둥글기와 그림자 적용, 진행률 막대를 캡슐형으로, 목록 행에 hover 시 살짝 떠오르는 전환 효과, 빈 상태(empty state)에 장식용 아이콘 원 추가. 죽은 코드(중복 `:root` 블록 중 첫 번째가 두 번째에 덮여 무시되던 것)도 정리.
32. **다크 모드 하드코딩 색상 버그 일괄 수정**: 시각 정리 후 헤드리스 브라우저로 다크 모드 전 화면을 스크린샷 검증하다가, 다크 모드 오버라이드 목록에 빠진 채 밝은 배경이 하드코딩되어 있던 요소 6곳 발견 — 검색창(`.search`), 캘린더 보기 전환 캡슐(`.view-switch`)과 활성 버튼, 이전/다음달 날짜 칸(`.outside`), 요일 헤더 행(`.weekdays`), 간트 헤더 행(`.gantt-head`), 오늘 열 타임라인 강조(`.timeline-days .today`), 캘린더 일정 칩(`.cal-event`). 전부 `var(--surface-2)`/`var(--muted)` 또는 기존 `.ai-card`와 같은 `color-mix()` 패턴으로 교체해 테마를 자동으로 따라가도록 수정(커밋 `318f7d8`).

진행 방식 메모: 시각 변경은 코드만 읽어서는 검증이 안 된다는 걸 재확인 — 다크 모드 스크린샷을 실제로 찍어보고 나서야 흰색 얼룩이 보였다. 하드코딩된 `background:white`/`#fff` 전체를 grep으로 뽑아 다크 모드 오버라이드 선택자 목록과 교차 대조하는 방식으로 빠짐없이 훑었다.

---

# Wave 7 — 개선 플랜 (완료)

최종 갱신: 2026-07-12

진행 현황 (2026-07-12): **완료** — 33 백업 자동화(compose 백업 컨테이너), 34 `/api/import` 복원(미리보기→merge/replace, 계층·의존성·기록연결 보존, 트랜잭션 롤백, Google 무간섭 — 커밋 `dd120b4`), 35 healthcheck, 36 감사로그 보존(180일), 38 Ctrl+K 커맨드 팔레트(통합 검색+화면 이동, `commandPalette.js` 순수모듈+테스트 — `fc1da1a`), 45 지난주 리뷰 프리셋(`5fbe037`), 46 일정 시작 15분 전 알림(`40b8dd5`), 43 태그 관리(사용량 목록·일괄 이름변경·병합·제거, `/api/tags`·`/api/tags/rename` — `731edb7`·`64ac660`), 44 업무 템플릿(`frontend/src/taskTemplates.js` 순수모듈+테스트, `TaskForm.jsx`에 템플릿 선택/저장/삭제 UI, localStorage 저장). 33·35·36은 Codex 자동 루프와 분담. **전부 완료 (2026-07-12)** — 37 서버 오류 패널, 39 간트 막대 드래그(이동+기간조절, `ganttDrag.js` 순수모듈+테스트, 실제 마우스 드래그 검증 — 커밋 `e58edc5`), 40 캘린더 일정 드래그(`calendarDrag.js` — `dac5e09`, 이후 Codex가 주간 뷰까지 확장), 41 스켈레톤 로딩, 42 모바일 폼 점검(입력칸 44px 터치 타깃 — `ce000a0`, 버튼 줄바꿈 수정은 Codex), 44 업무 템플릿. 37·41·44는 Codex 루프 분담.

사용자 요청: "이 프로그램을 개선하기 위한 플랜을 만들어줘. 기능 추가도 좋고, 기존 기능 수정, 개선도 좋아. UI업그레이드도 좋고, 서버 안정성도 좋아."

## 현황 진단 (2026-07-10, 코드 재조사 기준)

이미 있어서 이번 플랜에서 **제외**한 것: 브라우저 알림(오늘 업무 1일 1회, `App.jsx:31`), 데이터 내보내기 UI(설정 화면의 `/api/export` 다운로드), 백업/복원 검증 스크립트(`backend/scripts/backup_db.py`·`verify_backup.py`), 만료 세션 자동 정리(`auth.py:23`), 휴지통 30일 정리 안내, 오프라인 전용 에러 메시지.

실제로 비어 있는 곳:
- 백업 스크립트는 있으나 **아무도 주기적으로 실행하지 않음** — 스케줄러·보존 정책·성공 여부 확인이 전부 없음.
- `/api/export`는 있는데 **`/api/import`(복원)가 없음** — 내보낸 JSON을 되돌릴 방법이 서버 재구축뿐.
- `docker-compose.yml`에 `restart` 정책만 있고 **healthcheck가 없음**(portainer-stack.yml에만 있음) — api 컨테이너가 죽은 채로 살아있는 척해도 감지 못 함.
- `audit_logs`는 **무한 성장** — 보존 한도 없음.
- 서버 500 오류가 로그 파일에만 남고 **사용자(운영자)가 앱 안에서 볼 방법이 없음**.
- Ctrl+K는 자연어 입력 전용 — **통합 검색/화면 이동이 없음**(업무 검색은 업무 화면 안에서만).
- Gantt 막대·캘린더 일정 모두 **드래그 조작 불가** — 날짜 변경은 폼을 열어야만 함.
- 태그는 입력만 가능하고 **관리(목록·빈도·일괄 이름변경)가 없음**.
- 데이터 로딩 중 화면이 통째로 "준비 중" 문구 하나 — **스켈레톤 없음**.
- Wave 1 P2 item 9(모바일 폼 입력성 점검)가 미착수로 남아 있음.

## P0 — 서버 안정성·데이터 보호

33. **백업 자동화**: docker compose에 백업 사이드카(또는 호스트 cron 절차 문서화 + compose 프로필)를 추가해 `backup_db.py`를 매일 실행, 백업 후 `verify_backup.py` 자동 검증, 보존 정책(일 7·주 4). 완료 기준: 컨테이너 재시작 후에도 스케줄이 살아 있고, 실패 시 로그로 확인 가능.
34. **`/api/import` 복원 엔드포인트**: `/api/export` JSON을 업로드해 복원. 2단계(미리보기: 테이블별 건수·충돌 요약 → 확인 후 적용), 기존 데이터 병합/대체 선택. 설정 화면에 업로드 UI. AI apply와 같은 "미리보기→명시적 적용" 관례를 따름.
35. **(완료, 2026-07-10) dev/prod compose에 healthcheck 추가**: `docker-compose.yml`의 `api` 서비스에 `/api/ready` 기반 healthcheck 추가(운영용 `portainer-stack.yml`과 동일한 설정), `backup`·`web`은 `depends_on: api: condition: service_healthy`로 변경해 api가 준비되기 전에 시작하지 않도록 함.
36. **(완료, 2026-07-10) audit_logs 보존 한도**: 180일(기본, `AUDIT_LOG_RETENTION_DAYS`로 조정 가능) 초과분을 `init_db()`(서버 시작 시)에서 삭제하도록 `_prune_audit_logs()` 추가. `backend/tests/test_db_migration.py::AuditLogRetentionTests`로 회귀 커버.
37. **서버 오류 가시성**: 처리되지 않은 예외를 잡아 시각·경로·요약을 경량 테이블(또는 파일)에 남기고, 설정 화면 진단 섹션에 최근 오류 N건 표시.

## P1 — UI 업그레이드

38. **Ctrl+K 커맨드 팔레트 확장**: 기존 자연어 입력에 더해 ① 통합 검색(업무·일정·할 일·기록 제목/내용, 클라이언트 측 — 데이터는 이미 App.jsx에 전부 로드됨) ② 화면 바로가기("성과", "설정" 등 타이핑해 이동). 입력이 검색어인지 자연어 생성인지 모드 전환 탭 또는 접두사로 구분.
39. **Gantt 막대 드래그**: 막대 끌기로 시작/마감 이동, 가장자리 끌기로 기간 조절. 놓는 순간 기존 `api.updateTask` 호출(일정 승인 워크플로 규칙 그대로 통과). 순수 계산(픽셀→날짜)은 루트 모듈로 분리해 `node --test`로 커버.
40. **캘린더 일정 드래그 이동**: 월간 뷰에서 일정 칩을 다른 날짜 셀로 끌어 이동(시간은 유지, 날짜만 변경). Google 동기화 일정도 기존 PATCH 경로를 그대로 타므로 추가 처리 불필요.
41. **(완료, 2026-07-12) 스켈레톤 로딩**: 업무 화면의 간트 목록에 `dataLoading` 상태를 전달해, 데이터가 아직 없고 불러오는 중일 때 빈 상태 문구 대신 회색 블록 애니메이션(`.skeleton`, 기존 설정 화면 패턴 재사용)을 표시하도록 함(`Tasks.jsx`, `App.jsx`, `styles.css`).
42. **모바일 폼 입력성 점검**(Wave 1 item 9 잔여): datetime-local·슬라이더·모달 크기를 실제 모바일 뷰포트 스크린샷으로 점검하고 보정.

## P2 — 기능 추가

43. **태그 관리 섹션**(설정 화면): 전체 태그 목록+사용 빈도, 태그 일괄 이름변경(4개 테이블 tags 필드 일괄 치환), 미사용 태그 정리. 태그 클릭 시 해당 태그 필터로 업무 화면 이동.
44. **(완료, 2026-07-12) 업무 템플릿**: 자주 만드는 업무(제목·우선순위·반복·태그·기간 패턴)를 템플릿으로 저장, 새 업무 폼에서 선택해 미리 채움. localStorage 저장(`frontend/src/taskTemplates.js`), `TaskForm.jsx`에 템플릿 선택/템플릿으로 저장/삭제 UI 추가.
45. **주간 리뷰 프리셋**: 성과 화면에 "지난주 리뷰" 원클릭 — 지난주 월~일 기간의 period-summary(근거 목록 포함)+완료 업무+기록을 한 화면으로, Markdown 내보내기 재사용.
46. **일정 임박 알림**: 앱이 열려 있는 동안 시작 15분 전 일정 브라우저 알림(이미 있는 오늘-업무 알림 로직 확장, 같은 권한 재사용).

## 진행 방식

- 순서: P0(33→37) → P1(38→42) → P2(43→46). 각 항목: 구현 → 테스트/빌드 → 헤드리스 브라우저 검증(화면 변경 시) → 커밋/푸시 → changelog(커밋 시각)·ROADMAP 반영.
- 잘 쪼개지는 항목(35·36·41·44 등)은 저비용 모델 배치 위임 후 메인 세션이 디프 리뷰·실검증. 상태 전이가 걸린 항목(34 복원, 39·40 드래그)은 메인 세션이 직접 구현.
- 드래그 조작(39·40)은 픽셀↔날짜 변환을 순수 모듈로 분리해 유닛 테스트, 실제 드래그는 Playwright `drag_to`로 검증.
