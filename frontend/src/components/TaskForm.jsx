export default function TaskForm({ onSave, onCancel }) {
  const today = new Date().toISOString().slice(0,10)
  const submit = e => { e.preventDefault(); const d = Object.fromEntries(new FormData(e.currentTarget)); onSave({...d, progress: Number(d.progress)}) }
  return <form className="form-grid" onSubmit={submit}><label className="span-2">업무 제목<input name="title" required autoFocus placeholder="해야 할 일을 입력하세요"/></label><label>시작일<input name="start_date" type="date" defaultValue={today}/></label><label>완료 예정일<input name="due_date" type="date" defaultValue={today}/></label><label>상태<select name="status"><option value="todo">할 일</option><option value="in_progress">진행 중</option><option value="done">완료</option></select></label><label>진행도<input name="progress" type="number" min="0" max="100" defaultValue="0"/></label><label>우선순위<select name="priority"><option value="medium">보통</option><option value="high">높음</option><option value="low">낮음</option></select></label><label>분류<input name="category" placeholder="예: 기획"/></label><label className="span-2">메모<textarea name="description" rows="3" placeholder="필요한 내용을 메모하세요"/></label><div className="form-actions span-2"><button type="button" className="secondary" onClick={onCancel}>취소</button><button className="primary">등록하기</button></div></form>
}
